# AI CLI tools installed by the debug cockpit image.
# Format: tool|mode, where mode is "root", "user", or "venv".
# Comments after '#' are ignored.
aider|venv
reasonix|user
openclaw|user
codex|user
claude|root
antigravity|root
copilot|user
opencode|user
