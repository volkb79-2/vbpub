# Load the BFQ I/O scheduler module at boot (required before the udev rule can
# select it). BFQ is the only multi-queue scheduler that enforces cgroup v2
# io.weight / io.bfq.weight; [none] and mq-deadline leave weights inert.
# (io.max hard caps work on ANY scheduler — only the weights need BFQ.)
bfq
