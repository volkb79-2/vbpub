[Unit]
Description=Reconcile wings cgroup resource properties (slices + docker scopes)
After=docker.service
Wants=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/wings-cg-reconcile.sh
