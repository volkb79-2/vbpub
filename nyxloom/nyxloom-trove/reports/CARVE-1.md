{"carved": [{"id": "nyxloom-P24-config-schema-lint", "why": "B2 second half: JSON schema for nyxloom.toml + `nyxloom lint` config validation (bad gate argv / missing worktree_root / unresolved [refs]); config discovery already landed, validation did not", "source_kind": "backlog"}, {"id": "nyxloom-P25-dashboard-project-info-archive", "why": "B4: dashboard surfaces each project's gate/channels/folders from nyxloom.toml + archive-keep-visible collapse UX on the completed listing", "source_kind": "backlog"}], "review_reflection": "The archived P01-P22 (old prose-header format) plus the merged B1 two-channel notify show the core control-plane -- lint, reconcile/tick, wrapper, render, daemon, decisions -- is fully built; the project has entered its self-hosting/polish phase and P23 is the first YAML-frontmatter dogfood package. Two gaps surfaced while carving. (1) B2 landed only its discovery half: ProjectConfig now reads nyxloom-trove/nyxloom.toml, but there is no nyxloom.toml JSON schema and lint.py has no config validation, so a bad gate argv or unresolved [refs] is caught at runtime, not carve time -- P24 closes this. (2) The dashboard config pane (_render_config) shows policy/pause/routing but not the project's gate/channels/folders, and render.py has zero archive UX (grep 'archive' in render.py is empty) despite archive_keep_visible being configured -- P25 closes this. Also flagged: P23, the sole existing YAML handoff, is NOT lint-clean -- L1 requires id==filename-stem, but P23/AUTHORING prescribe a project-prefixed id with a short P<NN>-slug filename; the two rules are mutually incompatible. I named P24/P25 files to match their id (nyxloom-P<NN>-slug.md) so they lint clean; reconciling STANDARD.md/AUTHORING naming with lint L1 is a worthwhile follow-up carve.", "headroom_estimate": 4, "headroom_rationale": "The user-confirmed backlog (2026-07-16) has seven items: B1 (done, merged), B3 (=P23, carved). This carve takes B2 and B4. That leaves three un-carved backlog items with clear code scope -- B5 (migrate topos+dstdns to nyxloom-trove), B6 (self-host activation: frontmatter-convert + registry add), B7 (daemon state = persistent docker volume) -- plus at least one clean follow-up the carve itself surfaced (reconcile L1 stem-check vs STANDARD/AUTHORING naming). The docs/ROADMAP.md M2-M5 milestones are largely already realized by P01-P22, so near-term runway is backlog-driven; I estimate ~4 more carve-able packages before hitting a spec gap or needing a user decision (B5/B6 touch cross-project migration and self-host enrollment, which may open a D- decision).", "outcome": "CANDIDATES_READY"}
