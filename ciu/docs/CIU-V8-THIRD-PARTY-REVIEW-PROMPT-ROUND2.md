# Prompt: round-2 (delta) review of the CIU v8 design set — rev 3.1 / draft.4

*Hand this file, unchanged, to the reviewer — preferably the same reviewer as round 1. Round 1's prompt (`CIU-V8-THIRD-PARTY-REVIEW-PROMPT.md`) still applies for role, constraints, severity scale and tone; this file narrows the scope and changes the output format.*

## 1 What happened since your round-1 review

Your 35 findings (T-01..T-35) and seven alternatives were verified against the text one by one and all produced a change; two were put to the operator, who **kept authoritative state in the checkout** (rejecting Alternative A) and **adopted manifested releases plus activation receipts** (Alternatives B and D). The result is `SPEC-V8.md` **8.0.0-draft.4** (draft.3 corrected in place; Appendix D maps every changed rule to your finding), `CIU-V8-TESTING-GATE-PROPOSAL.md` **rev 3.1**, the corrected demo, and `CIU-V8-THIRD-PARTY-REVIEW-RESPONSE-2026-09-03.md` (the disposition table with reasoning, the operator decisions, the alternatives' disposition, and five corrections to the first review round).

## 2 Scope of this round (do these four things; nothing else)

1. **Audit the dispositions.** For every row of the response document's §1 and the spec's Appendix D: did the change land in the rule text as described, does it actually fix the failure scenario you wrote, and did it introduce a new contradiction elsewhere? Report each as `landed | landed-but-incomplete | not-landed | landed-and-broke-<rule>`. Where you disagree with a **rejected** or **partially accepted** disposition (T-04c, T-08's capability record, T-09's SOPS/Vault Agent, T-11/T-17/T-18/T-19 under the operator's posture, T-15's `verify`, T-16 unsigned receipts, T-22 Docker adapter, T-23 validation placement, T-35 names), say why the reasoning given is wrong — not merely that you would have chosen otherwise.
2. **Read the rewritten and new sections as an implementer and as an attacker** (round-1 method §3.1–§3.2): S17.3–S17.5 (releases, manifest closure, staging, `current`/`previous`, the secrets capsule, receipts and their validity rule), S8.4.1 (the wave algorithm), S8.5.1–S8.5.5 (per-edge predicates, acceptance), S5.3.5–S5.3.6 and `verify`, S6.3.2/S7.4.6 (socket claims), S7.3/S7.3.2 (`realized_by` as a capability), S7.4.7 and S7.8 step 4a (host-network), S7.8 step 5 (`host_bind` rule), S4.1.4 (sentinel bind), S4.5.1–S4.5.3 (ownership labels, adoption), S14.4.3 and S9.5.7 (lock order, acyclic joins), S16.5.3 (directory gate locks), S16.4 and S16.12 (detached state machine, git config, dual mount), S16.6.1–S16.6.5 (admission transaction, exec caps, result serialization), S16.2.1–S16.2.2 (recursive inherit, externals), S16.3/S16.7.2 (minimum judge, `--progress`), S12.1–S12.4 (hook environment, per-entry secrets, validation), S10.7 (secret directories), S13.3–S13.4 (conversions, read-back), S2.4 (secret lint), S6.10 (hook entry object), S6.2 `build`, S6.8.5 (seed), S6.9.2, S19 (`ciu init`), S18.4 (api headers), S3.7.6 (`ciu query`). A known soft spot to attack first: **S17.4.4** compares the provider host's resolved-file digest with "the consumer's own render of the provider host" — host-local facts (`[ciu.host.generated]`: uids, hostname, physical path) make those renders differ, so the digest as specified may never match; say what the digest should cover (release digest + layout + selection?) and what a receipt must contain for the consumer to trust a fact without re-rendering the provider host.
3. **Re-derive the demo** with the now-stated rules and compare with `v8-dstdns-demo/examples/ciu.resolved.toml.example` and the demo files: the prod3 waves by S8.4.1 (the example claims five waves over all 22 Realizations), the prod3 publication table as socket claims with the S6.3.2 collision check (authentik is on 9010 now; is anything else colliding, including wildcard `0.0.0.0` host publications against mesh-bound derived ones on rs1002?), the three binding resolutions, the gates on rs1002 (`[resolved.gates.1]` and `[resolved.gates.3]`), and the `[testing]` table against S16.4.5/S16.6.4 with the tester at 4G. Also check that the twelve `build` tables and the removal of template `build:` blocks are consistent with S6.2/S11.2–S11.4, and that no demo file still uses `published_on`, a Realization name in `realized_by`, or a `.j2` declaration.
4. **List what round 1 missed** that draft.4 makes visible — at most ten new findings, each with a failure scenario; do not restate open items already listed in the response document §5 or the proposal §4.10.

Do **not** re-review the model, the naming, the plain-TOML decision or the in-checkout posture as such; those are settled unless you show a defect the operator did not weigh (state it as "new cost of a settled decision").

## 3 Files (repository `vbpub`, `ciu/docs/`)
`SPEC-V8.md` (draft.4), `CIU-V8-THIRD-PARTY-REVIEW-RESPONSE-2026-09-03.md`, `CIU-V8-TESTING-GATE-PROPOSAL.md` (rev 3.1: §4.1.5, §4.1.8–§4.1.10, §4.3.13, §4.7 X57–X72, §4.8, §4.10), `CIU-V8-THIRD-PARTY-REVIEW-2026-09-02.md` (your own round-1 document), `v8-dstdns-demo/` (README D26–D31; `ciu.toml`; `ciu.site.toml`; `examples/ciu.resolved.toml.example`; `examples/minimal/`; the twelve stacks with `build` tables: applications/{controller,webapp-server,webapp-ui-react,worker-db,worker-io,ddcli}, tools/test-runner, infra/{db-core-seeded,docker-stats-exporter,mock-targets}, infra-global/webhook-listener; `infra/authentik`, `infra/tailscale-node`).

## 4 Output format (binding)
One Markdown document with exactly: **1 Verdict** (≤ half a page: is draft.4 implementable; what still blocks); **2 Disposition audit** — a table with one row per T-01..T-35: `id | claimed disposition | landed? | remark`; **3 New findings** `T2-01`, `T2-02`, … in severity order with the round-1 fields (Severity, Where, Claim, Evidence, Failure scenario, Proposed fix, Challenges a settled decision); include regressions introduced by draft.4 as findings; **4 Demo re-derivation** (waves, claims table, collisions, gates, discrepancies); **5 Not verified**; **6 Machine summary** — a JSON array with `{ "id", "severity", "where", "claim", "regression": true|false, "challenges_settled": true|false }` for section 3, plus a `"dispositions": { "landed": n, "incomplete": n, "not_landed": n, "broke": n }` object. Cite rule ids for every claim; quote where wording matters; no finding without a failure scenario; inline `ASSUMPTION:` where the text does not answer, listed again in section 5.
