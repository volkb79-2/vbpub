# examples/monorepo/appy/ciu.hosts.toml — written by `ciu init` (S7.2.3, S19); gitignored in a
# real checkout (S2.3.1), checked in here as part of the fixture. One host, no SSH facts.
[hosts.localhost]
local = true
