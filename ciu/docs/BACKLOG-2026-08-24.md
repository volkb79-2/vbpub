# CIU Backlog — 2026-08-24

Items derived from the SPEC reconciliation audit. Ordered by priority.

---

## CIU-QOL-1: Validate provisioning refs in single-stack mode

**Priority:** High (bug fix — silent validation gap)
**Effort:** Low
**Status:** ✅ IMPLEMENTED (commit pending)

`validate_stack_provisioning()` is now called inside `engine.main_execution`
after `validate_stack_shape()`. Previously a stack with malformed
`requires = ["bogus:nonsense"]` passed `ciu up --dir infra/foo` because the
check only ran in multi-stack preflight.

**Files:** `engine.py`, `SPEC.md S11`

---

## CIU-QOL-2: Implement `ciu.exit_on` as a closed-vocabulary exit policy

**Priority:** High (dead-key fix + QoL)
**Effort:** Low
**Status:** ✅ IMPLEMENTED (commit pending)

Replaces the previous boolean `fail_fast` with a single closed-vocabulary key:

```toml
[ciu]
exit_on = "WARN"    # "WARN" | "ERROR" | "NEVER" (default: "ERROR")
```

- `"WARN"`: exit on any warning OR error (strictest)
- `"ERROR"`: exit on errors only; warnings logged (**default**)
- `"NEVER"`: log all findings without ever aborting

Resolution: config → `$CIU_EXIT_ON` env var → default. Config always wins.
Invalid values are configuration errors naming the key and vocabulary.

**Files:** `warn_policy.py`, `deploy.py`, `CONFIG.md`, `SPEC.md S10.6`

---

## CIU-QOL-3: Remove `workspace_env_file` dead key

**Priority:** Low
**Effort:** Trivial
**Status:** OPEN — dstdns cleanup

`workspace_env_file` appears in dstdns's `[ciu]` block but CIU never reads it.
The filename is hardcoded via `config_constants.WORKSPACE_ENV`. Remove from
dstdns config; do not implement configurable env filenames (no use case).

---

## CIU-QOL-4: Remove `[ciu.ports]` from dstdns; move to topology/service

**Priority:** Low
**Effort:** Trivial
**Status:** OPEN — dstdns cleanup

dstdns uses `[ciu.ports].custom_http_port = 8080` as a convention referenced
by templates. This belongs under service identity or topology in v8, not
under CIU's reserved namespace. For now, document it as consumer-defined;
in v8 it moves to `[service.<name>.<level>].port`.

---

## CIU-QOL-5: Fix doc drift (ARCHITECTURE function names, FEATURES CLI table)

**Priority:** Medium (documentation accuracy)
**Effort:** Low
**Status:** ✅ IMPLEMENTED (commit pending)

- ARCHITECTURE.md: corrected `ksm.build_shim`→`build`,
  `transport_ssh.connect`→`ssh_exec`, added `activate.py` row,
  corrected `diagnose.diagnose`→`collect`+`run`.
- FEATURES.md: added missing verbs (`layouts`, `capabilities`, `host-secrets`).
- CIU.md: expanded common-options table with all shipped flags.

**Files:** `docs/ARCHITECTURE.md`, `docs/FEATURES.md`, `docs/CIU.md`

---

## CIU-QOL-6: Add `ciu status` verb

**Priority:** Medium (operator QoL)
**Effort:** Medium
**Status:** ✅ FIXED (ciu-P16)

No single command shows "which stacks are running, healthy, what images"
across a profile selection. `diagnose` shows failures; `provenance` shows
commit alignment; `health` gates. None give a dashboard summary.

**Proposed surface:**
```bash
ciu status [--profile NAME] [--json]
```

Read-only. Lists selected stacks → compose projects → running containers →
health status → image reference. JSON output uses a versioned envelope
consistent with worktree/provenance documents.

**Evidence:** `deploy.action_status` (new) resolves the exact `ciu up
--profile` selection chain (`load_global_config` → `resolve_profiles` →
`build_selection`), reuses the extracted `deploy._stack_compose_project`
per-entry helper (shared with `_stack_compose_projects` so the two branches
can never drift), and consumes `diagnose._inspect` + `health_pkg.classify`
read-only — no second docker ps/inspect round trip, no compose
up/down/build/exec anywhere in the path. `cli._status` wires `ciu status
[--profile NAME] [--json]` into the verb dispatch/`_USAGE`/`_VERB_HELP`. A
stack directory missing on disk gets a named row (`compose_project: null`,
empty `containers`) rather than vanishing from the report; a resolved
project with zero containers is reported as a legitimate "not started"
result. A Docker daemon that cannot be reached raises out of
`diagnose._inspect` uncaught by `action_status`, is turned into a clean
`[ERROR] ciu status: <reason>` + exit 2 by `cli._status`, and is NEVER
rendered the same as an empty/healthy report (the false-PASS "absence for
emptiness" anti-pattern this package's review focus names). See
`docs/SPEC.md` S7.10, `docs/CONSUMERS.md` §13 for a worked `--json` example,
and `nyxloom-trove/reports/ciu-P16-status-verb-LOG.md` for the full oracle
proof and gate output.

---

## CIU-QOL-7: Unified `ciu bake` respecting selection model

**Priority:** Medium (consistency)
**Effort:** Medium
**Status:** ✅ FIXED (ciu-P17)

Two build paths existed:
- Public `ciu bake [targets...] [--no-cache]`: passes targets straight to
  buildx; ignores profiles/phases/requires.
- Internal `deploy.action_build` (`--build` flag): filters targets by
  selected stacks under applications/tools/.

**Fix:** make public `ciu bake --profile core` resolve the selection and
build only matching targets. Remove internal action_build path.

**Evidence (ciu-P17):**
- Target-resolution unification: `ciu bake --profile NAME` resolves targets
  via the SAME chain `ciu up --profile` uses (`load_global_config` →
  `resolve_profiles` → `build_selection`, then
  `deploy.collect_bake_targets_from_selection`), feeding the SAME `docker
  buildx bake ... --load` invocation the no-`--profile` path already
  builds; `ciu bake [targets...]` with no `--profile` stays byte-identical;
  `--profile` and explicit positional targets are mutually exclusive
  (prefix-aware, catches `--profile=NAME` too). See `src/ciu/cli.py`'s
  `_bake`, SPEC S7.11, `tests/tests/test_ciu_cli_bake.py`.
- Dead-code removal: `deploy.action_build` (and its `--build`-flag docstring
  references) deleted from `src/ciu/deploy.py` — re-confirmed at deletion
  time it had zero callers and no CLI flag routing anywhere in `src/ciu/`.
  Its 3 dependent tests (`tests/tests/test_ciu_deploy_deeper8.py`, whole
  file, and
  `test_ciu_deploy_actions_remaining.py::test_build_uses_selected_application_targets_and_propagates_docker_failure`)
  were unique to that unreachable code path (own private
  `procutil.docker`/print-message contract, never produced by the real CLI
  path) and were deleted rather than ported — nothing reachable regressed.
  One coverage GAP found in the process was closed instead of silently
  dropped: the real `ciu bake` path's shared `subprocess.call` tail never
  had a test proving what happens when the `docker` binary itself is
  missing; `tests/tests/test_ciu_cli_bake.py::TestCliBakeDockerUnavailable`
  now pins that it propagates uncaught rather than silently succeeding.
  scope.touch was widened (handoff amendment, controller-authorized) to
  cover the 2 dependent test files before this deletion shipped. Full
  before/after evidence and gate output in
  `nyxloom-trove/reports/ciu-P17-unified-bake-LOG.md`.

---

## CIU-QOL-8: Per-service health-timeout override

**Priority:** Medium (operational flexibility)
**Effort:** Low
**Status:** ✅ FIXED (ciu-P15)

`[deploy.health].timeout` applies globally. Authentik needs ~240s; workers
need ~5s. Slow services must be excluded from gating or the global timeout
is set high enough to waste time on fast ones.

**Proposed addition to phase service entries:**
```toml
[[deploy.phases.phase_3.services]]
path = "infra/authentik"
health_timeout = "300s"   # optional; falls back to deploy.health.timeout
```

**Evidence:** `deploy_pkg.phases.service_health_timeout` (new accessor) +
`deploy_pkg.health.wait_for_gate_per_target` (new poll primitive — each
container gets its own deadline within one shared poll loop, instead of
one deadline shared by every container in the call) +
`resolve_selection_health_containers`/`run_container_health_gate` wired to
resolve and use per-container timeouts. See `docs/SPEC.md` S7.7 and
`docs/CONFIG.md`'s phase-service-entry section for the worked Authentik/
worker example, and
`nyxloom-trove/reports/ciu-P15-per-service-health-timeout-LOG.md` for the
combined-axis proof (a fast broken service resolves at its own 5s deadline
in the same gate call where a slow legitimate service resolves
independently at 200s, never waiting on the other).

---

## CIU-QOL-9: Document worktree concurrency budget in consumer docs

**Priority:** Low (discoverability)
**Effort:** Trivial
**Status:** ✅ IMPLEMENTED (ciu-P13)

`[ciu.worktree].max_concurrent_instances` defaults to unlimited and isn't
mentioned in README or CONSUMERS.md. A user who hits OOM won't know the
feature exists.

**Fix:** add a paragraph to README's worktree bullet and a worked example
to CONSUMERS.md showing how to set and verify the cap.

**Evidence:** README.md now points readers at docs/CONFIG.md's
`[ciu.worktree]` table and docs/CONSUMERS.md's existing worked example
(CONSUMERS.md already had the worked example; only README lacked the
pointer).

---

## CIU-QOL-10: Preflight check for silently-empty rendered values

**Priority:** High (correctness)
**Effort:** Medium
**Status:** DEFERRED TO V8 (§2.5 of proposal)

Currently a Jinja2 expression referencing an unknown variable renders as
empty string without error. A typo like `{{ contoller.name }}` produces
empty output that passes TOML parsing and may pass compose rendering until
the container fails at runtime.

**V8 approach:** wrap Jinja2 `Undefined` to raise on attribute access.
Withdraw "unknown variable renders empty." Templates MUST reference keys
that exist or use `{% if x is defined %}` explicitly.

This is a breaking change requiring the v8 revision gate.

---

## CIU-V8-PREP-1: `[app]` / user_tables namespace declaration

**Priority:** Deferred to V8 (breaking schema change)
**Effort:** Medium
**Design:** See proposal §1.14
**Status:** ⚠️ FIXED-partial — the ADDITIVE groundwork shipped in ciu-P21
(SPEC S3.13): an optional `ciu.user_tables = [...]` declaration, a new
`RESERVED_GLOBAL_TABLES` frozenset (distinct from S3.7's
`RESERVED_GLOBAL_NAMESPACES`) naming the top-level tables CIU itself reads
at global scope today, and `validate_user_tables` wired into
`render_global_chain` beside `_validate_deploy_landscape_id`. Absence of
`ciu.user_tables` is a complete no-op — zero behavior change for every
config that does not opt in. **NOT shipped** (the eventual V8 BREAKING
step, still deferred): defaulting `ciu.user_tables` to empty so an
undeclared table is ALWAYS an error even without opting in. There is also
no `[app]` table anywhere in this repo or the proposal itself — only
`ciu.user_tables` ships, per the proposal's own §1.14 example.

Introduce `ciu.user_tables = [...]` so consumers declare which top-level
tables are theirs. Unknown non-reserved tables become validation errors.
Prevents future reserved-key collisions.

---

## CIU-V8-PREP-2: Build version management (`[build]` + `ciu refresh`)

**Priority:** Deferred to V8
**Effort:** Medium-High
**Design:** See proposal §1.13

Structured `[build.python]` namespace with `version` (constraint, never
auto-changed), `chosen_version` (resolved by `ciu refresh` for non-held),
`possible_version_upstream` (always written, even for held), freshness and
channel policies. See proposal §1.13.

---

## CIU-V8-PREP-3: Service identity model refinement

**Priority:** Deferred to V8 (breaking schema change)
**Effort:** High
**Design:** See proposal §3.1 and §1.15
**Status:** ⚠️ FIXED-partial — the additive declaration-only half shipped in
ciu-P22 (SPEC S3.14/S3.15): a global `[service.<name>]` registry
(`type`/`location`/`description`) is now validated at global scope when
declared, plus a WARN-only `ciu check` cross-check (never a refusal) against
what the current profile/phase actually deploys, in both directions. A
declared per-service realness sub-table (proposal §3.2:
`[service.<stack>.<svc>.<level>]`) is REJECTED as a configuration error, not
silently accepted — deliberately reserving that layer for the real V8
rewrite. **NOT shipped** (the eventual V8 breaking step, still deferred):
the per-service realness sub-table layer itself, `[local_stack]`-to-registry
join-key enforcement (V8-PREP-4's §1.16 mapping rule 1), and topology/group
references by compound `<stack>.<service>` key.

**Correction (this row was previously wrong, not just un-shipped):** this
row used to say "Global `[service.*]` registry eliminated." That described
an EARLIER, flat-model draft of the proposal. Rev 1.4 (commit `4440c17e`,
"fix V8 service model to two-level stack.service hierarchy") replaced that
draft: the CURRENT proposal **introduces** `[service.<stack>]` as the global
identity/location layer (§1.15/§3.1) — it does not eliminate it. Per-stack
`[local_stack.<svc>]` (V8-PREP-4, §1.16) is the SEPARATE deployment-wiring
layer underneath it; the two are joined by matching name (§1.16 mapping
rule 1), not merged into one table.

Logical service names decoupled from filesystem paths via `location` field.
Topology references services by logical name once the full two-level model
ships (deferred; see above for what is additive-shipped today).

---

## CIU-V8-PREP-4: Stack-level `[local_stack]` service tables

**Priority:** Deferred to V8 (breaking schema change)
**Effort:** High
**Design:** See proposal §1.16
**Status:** ⚠️ FIXED-partial — the ADDITIVE groundwork shipped in ciu-P21
(SPEC S3.5/S3.7): `validate_stack_shape` recognizes `local_stack` as a
recognized, preferred stack root key name — a stack MAY name its root
table `[local_stack]` instead of a directory-derived one, with zero other
code change (every downstream reader already takes the root key as an
opaque parameter). `local_stack` is deliberately absent from both
`RESERVED_GLOBAL_NAMESPACES` and `RESERVED_GLOBAL_TABLES`. A stack that
keeps its existing root key is completely unaffected. **NOT shipped** (the
eventual V8 BREAKING step, still deferred): making `local_stack` the ONLY
accepted root key (today it is merely *preferred*, not required), any
per-service `[local_stack.<svc>]` wiring, or relocating hooks to
`[local_stack.<svc>.hooks]` — no defined ordering/precedence semantics
exist yet for per-service hooks.

Every stack declares its services under a reserved `[local_stack]` key in
its own config. Replaces the current convention of deriving a root key from
the directory name. Realness is a global concern only — stacks don't know
about it.

---

## CIU-V8-PREP-5: One-shot completion semantics

**Priority:** Deferred to V8
**Effort:** Low
**Design:** See proposal §1.18
**Status:** ⚠️ FIXED-partial — the additive subset shipped in ciu-P23 (SPEC
S13.2/S7.7): a new provisioning-ref terminal `stack:<name>:completed`
(exit-0-based — `State.Running == false` and `State.ExitCode == 0` — and
NEVER reads `.State.Health` under any code path, unlike `:healthy`); a
`[WARN]` (not an abort) on `:healthy`'s existing exit-0-no-healthcheck
fallback naming `:completed` as the replacement; a new optional phase-entry
key `one_shot = true` (`deploy_pkg.phases.service_one_shot`) that is
**declaration + shape validation only** — it is NOT wired into the health
gate's own polling loop; a working resolution for slash-bearing
`stack:<path>:...` selectors (previously guaranteed-broken: the raw
selector was passed straight into container-name resolution and could never
match a real container); and a `lint_graph` fix so a stack-to-stack
dependency declared via a full/bare stack path actually enters cycle
detection (previously silently skipped because the graph is keyed by full
path while the only selector form that ever worked for probing was a bare
basename). **NOT shipped** (the eventual V8 change, still deferred): wiring
`one_shot` into the deploy loop's own post-up wait/polling behavior, and
removing `:healthy`'s deprecated exit-0-no-healthcheck fallback.

The false-positive risk this closes: before ciu-P23, a one-shot stack could
only be referenced via `stack:<name>:healthy`, whose exit-0-no-healthcheck
fallback is satisfied by ANY container with no healthcheck that has exited
0 — including a container WITH a healthcheck that reported `healthy` and
only later exited, which `:healthy`'s `status == 'healthy'` branch accepts
without ever re-checking `Running`. `:completed` closes that gap by never
consulting `Health` at all, so a one-shot stack's completion signal can no
longer be confused with a still-healthy-but-exited container.

`one_shot = true` on phase services; health gate treats exit-0 as satisfied.
Requires use full stack path for provisioning refs.

---

## CIU-V8-PREP-6: Unified configfile fan-out + compose enumeration

**Priority:** Deferred to V8
**Effort:** Medium-High
**Design:** See proposal §1.19
**Status:** ⚠️ FIXED-partial — the additive subset shipped in ciu-P24 (SPEC
S7.5d/S7.5e): an optional service-level `[<root>.<service>] instances = N`
(sibling of `configfile`, validated identically to the pre-existing
per-configfile key) that MUST agree with any configfile-level `instances`
declared for the same service (a disagreement refuses, naming both values —
never a silent preference); a `ciu.instances` compose-template context (a
`{service: N}` map, merged into `ciu` the same way
`selected_profiles`/`deployed_stacks` already are, S3.12) for every service
resolving to N > 1; and a new post-render refusal when a service in
`ciu.instances` renders a bare `<service>` compose key instead of
enumerating `<service>-1`..`<service>-N` (the duplicate-mount trap this
proposal names — separate from, and untouched, the pre-existing S5.3
base-selector `[WARN]` for the opposite direction). **CIU does NOT
auto-generate compose service blocks from `instances`** — the template
author still writes the `{% for %}` loop; this package unifies the
FAN-OUT COUNT the loop reads and makes disagreement between the two
existing mechanisms loud, it does not generate compose YAML. A
migration-safety refusal (S7.5e) closes a real hazard found live during
implementation: a configfile that omits its own `instances` key while its
service declares one > 1 now refuses rather than silently switching from
the old "single shared render, S5.3-fanned" behavior to "N independent
renders" — see CHANGES.md's Upgrade note. **NOT shipped** (the eventual V8
change, still deferred): CIU itself generating/writing the compose
`{% for %}` loop or the service blocks it produces — templates still author
that by hand, just against the unified count.

---

## CIU-V8-PREP-7: Environment variable declarations

**Priority:** Deferred to V8
**Effort:** Low
**Design:** See proposal §1.20
**Status:** ⚠️ FIXED-partial — the additive subset shipped in ciu-P25 (SPEC
S8.2a): an optional `[<root>.<service>] env_required = [...]` declaration
(shape-validated: a list of valid env-var-name strings, no duplicates
within one service's own list — `composefile.resolve_env_required`), and a
collective presence check (`composefile.check_env_required`) that runs
against the ACTUAL environment `docker compose` will see — the dict
`composefile.compose_process_env` itself builds (post-secret-
materialization, pre-invocation) — collecting every missing
`service.VARIABLE` pair into one error rather than stopping at the first
miss. Corrects the original wording above: known machine-identity keys
(`workspace_env.REQUIRED_KEYS_CORE`/`GENERATED_IDENTITY_KEYS`) are **not**
"auto-injected" anywhere new — `{{ env.* }}` already receives the full
process environment (including these keys) on every render path today; no
new Jinja mechanism was added or was needed. Does **not** touch
`${VAR:-fallback}`/`${VAR:?msg}` handling in compose templates — that
withdrawal is the separate, genuinely breaking QOL-10 item. **NOT wired**
to the live `ciu up` pipeline yet: `compose_process_env` gained optional
`config`/`root_key` parameters that activate the check when both are
given, but `engine.py` (the only call site with both in hand, and out of
this package's scope) does not pass them yet — a one-line change deferred
to the real V8 cutover.

---

## CIU-V8-PREP-8: Registry schema enforcement

**Priority:** Deferred to V8
**Effort:** Medium
**Status:** ✅ FIXED (ciu-P19) — **narrowed on purpose; read the scope note.**
**Design:** See proposal §2.6

Option B recommended: CIU ships Pydantic models for the five built-in
provisioning kinds. Consumer-defined registries validated hook-side.

**Delivered (ciu-P19) — SPEC S13.4b, `ciu check` stage 7:**

- Pydantic models for the two `[registry.*]` values **CIU itself reads**:
  `[registry.postgresql].database` (non-empty str — `_probe_pg`'s `psql -d`
  target) and `[registry.consul].token_vault_path` (non-empty str, a valid
  `str.format` template whose only placeholder is `{svc}` — `_probe_consul`'s
  Vault path).
- `pydantic` as a NEW optional extra `ciu[registry]`, mirroring
  `ciu[schema]`/S5.7: lazy import, and a LOUD exit-2 finding naming the extra
  when a validated table is declared without it — never a silent skip.
- The proposal's Option C, as one additive extension point:
  `[ciu].registry_validator` names a module whose
  `validate_registry(config) -> list[str]` polices everything CIU does not.

**⚠️ Scope note — this is NOT "all five kinds got Pydantic models".** The
proposal's list (PostgreSQL, Redis, MinIO, Consul, Vault) is the list of
provisioning REF kinds, not of registry table shapes. Grepping `CONFIG.md`
and `provisioning.py` at implementation time found CIU reading exactly the
two fields above and nothing else: **no Redis, MinIO, Vault, or
PostgreSQL-users registry shape exists anywhere in this repo's code or docs**
to validate against. Inventing one would mean shipping a guessed schema, and
a guessed schema that is wrong REJECTS a legitimate consumer table CIU never
needed to constrain — strictly worse than no schema. Those tables therefore
pass through untouched, and Option C's extension point is how a consumer
validates them. If CIU ever starts reading a third registry field, extend the
models then, against the real consumption site.

---

## CIU-QOL-11: Wire remaining S11 validation checks into engine

**Priority:** Medium (completeness)
**Effort:** Low
**Status:** ✅ IMPLEMENTED (ciu-P14)

S11 catalog documents layout shape (S7.5c), exec-target declaration shape
(S16.7), and vendor_images type checks (S17.5) as validated. The catalog
entries were added, but the actual validation calls for these three are NOT
wired into `engine.main_execution` — only provisioning refs are.

**Fix:** add validation calls for these three checks in the single-stack
pipeline after `validate_stack_shape()`, matching what deploy.py already
does for profile-mode.

**Evidence:** `config_model.validate_declared_features` (new) composes the
three existing validators (`resolve_layout`, `resolve_exec_targets_config`,
plus a new vendor_images shape check) and is now called from BOTH
`engine.main_execution` Step 5 (single-stack) and `deploy.action_check`
(profile-mode, including an empty `selection`) — see
`tests/tests/test_ciu_config_model_layouts_eager.py`.

---

## CIU-QOL-12: Extend `ciu check` to full config validation with hook preflight

**Priority:** High (closes the "typos surface at runtime" gap)
**Effort:** Medium
**Status:** ✅ FIXED — proposal §2.7 stages 1–6 and 8–12 shipped in ciu-P18;
**stage 7 (registry validation) closed by ciu-P19** (SPEC S13.4b), scoped to
the two `[registry.*]` fields CIU actually reads plus a consumer-declared
`validate_registry` extension point for everything else — see CIU-V8-PREP-8's
scope note before reading a green stage 7 as "all five registry kinds are
modelled". They are not, deliberately.

Currently `ciu check` only validates the provisioning graph. It does NOT:
- Walk hooks to verify they load and export the right interface
- Call optional `validate_config()` on hooks that define it
- Validate registry shapes against built-in models
- Render configfiles or compose templates for validation

**Design:** see V8 proposal §2.7. The `validate_config` hook contract is
additive (hooks that don't define it are skipped), so it can ship now.

**Implementation plan:**
1. Add `validate_config` to HookContext documentation (S9) — ✅ done (S9.5)
2. Extend `action_check` in deploy.py to walk stages 1–12 from §2.7 — ✅ done
   (1–6 and 8–12 in ciu-P18; stage 7 in ciu-P19)
3. Add built-in Pydantic models for PostgreSQL/Redis/MinIO/Consul/Vault
   registries — ✅ done (ciu-P19) **for the two fields CIU actually reads**
   (`registry.postgresql.database`, `registry.consul.token_vault_path`), NOT
   for all five kinds: no Redis/MinIO/Vault/PostgreSQL-users registry shape
   exists in this repo to validate against, so none was invented. Consumer
   shapes go through `[ciu].registry_validator`. See CIU-V8-PREP-8's scope
   note and SPEC S13.4b
4. Add `--json` output with versioned envelope — ✅ done

This eliminates the need for consumers to run `ciu up --dry-run` as a
validation tool.

**Evidence (ciu-P18):**
- `deploy.action_check` walks stages 2, 3, 5, 6, 8–12 per selected+rendered
  stack, reusing the SAME functions `engine.main_execution` calls at each
  step: `config_model.validate_stack_shape`, `secrets.directives.discover` +
  `find_misplaced`, `governance.resolve_stack_governance`,
  `composefile.guard_config` + `render_compose` + `leak_scan` +
  `validate_consumption`. Stage 4 keeps the existing `lint_graph` path
  unchanged.
- **Side-effect-freedom is the load-bearing property** and is proved by a
  test that snapshots the whole `tmp_path` tree (paths, modes, sizes, bytes)
  before and after a check of a stack that on a real `ciu up` would create a
  hostdir, materialize four secrets, render a configfile + compose + overlay
  and execute three hook `run()` bodies — and asserts the tree is IDENTICAL
  (`test_check_leaves_the_filesystem_byte_for_byte_unchanged`), with a
  companion test proving the snapshot helper is not vacuous. Building this on
  `engine.main_execution(dry_run=True)` was rejected and is pinned as a
  negative test: dry-run still creates hostdirs and still runs hooks.
- Bytecode writing is suppressed during hook imports — CPython otherwise
  drops a `__pycache__/` beside every imported hook, which the tree snapshot
  caught as a real write into the consumer's tree.
- New `hooks_runner.load_hook_for_check(path) -> (run, validate_config|None)`
  loads a hook module ONCE and extracts both entry points; `load_hook` now
  shares the same extracted `_load_hook_module`. A per-run cache keyed by
  resolved path means a file declared at several points, or by several
  stacks, is imported exactly once.
- New optional hook entry point `validate_config(config, ctx) -> list[str]`
  (SPEC S9.5): called only by `ciu check`, never by `ciu up`; `run()` is
  located but never invoked; a bool/str return is a contract violation, not a
  verdict; an exception is that hook's own finding and never aborts the run.
- Exit-code discipline held: every new stage's failure is exit 2; exit 1
  remains reserved for `--live`'s probe failures, which are not attempted
  once a static stage is red.
- Named decisions: `ctx.secret_file` raises `KeyError` for EVERY name during
  check (nothing is materialized), and a declared-but-unconsumed secret stays
  a WARNING (matching `engine.py` Step 14's own precedent) rather than exit 2.
  Both are documented in SPEC S13.4a / S9.5 and in the ciu-P18 LOG.

- Render fidelity: the PURE halves of two steps the check does not run are
  applied in memory so templates still render — `engine.auto_generate_values`
  (Step 7, called directly, read-only) and a hostdir declaration→path rewrite
  (Step 8's path half only; nothing is created, seeded or chowned). Verified
  against the real dstdns workspace: before this, five real stacks failed
  compose-render with `'auto_generated' is undefined` and one crashed the run
  with an uncaught `yaml.YAMLError` from a hostdir inline table rendered as a
  Python dict repr. After: all stages green except three genuine pre-existing
  graph-lint findings, and `git status` in that workspace stayed clean.

**Follow-ups filed:**
- CIU-QOL-12a — extract the two blocks ciu-P18 had to mirror because their
  home modules were outside its scope: the S5 configfile
  declaration-shape/existence checks inlined in
  `composefile.render_configfiles` (a rendering, disk-writing function), and
  the hostdir path resolver inlined in `engine.create_hostdirs` (a
  directory-creating function). Both should become side-effect-free helpers
  that the real pipeline and `deploy.py`'s check path share.
- CIU-QOL-12b — `ciu check --json` writes its document to stdout after the
  orchestrator's own `[INFO]` prose, because `ciu check` routes through
  `deploy.main`. `ciu status --json` is clean because `cli.py` calls its
  action directly. Consider giving `check` the same direct handler so the
  document is the only thing on stdout.

---

## CIU-QOL-13: Hook template library with revision tracking

**Priority:** High (enables config check + consistent adoption)
**Effort:** Medium
**Status:** ⚠️ FIXED-partial — the MECHANISM shipped in full in ciu-P20
(SPEC S19.1): `template_revision`-stamped modules under
`src/ciu/hook_templates/`, `ciu init --hooks NAME1,NAME2` copy+stamp into
every scaffolded stack, unknown-name/no-target refusal before any write, and
each template's `run(config, ctx) -> dict` + optional
`validate_config(config, ctx) -> list[str]` contract enforcement. **Exactly
ONE reference template shipped**, `post_compose_db.py` — deliberately
generic and honest, not a production PostgreSQL bootstrap. The other five
named templates below (Vault/Consul/Redis/Authentik/Tailscale) are
explicitly OUT of scope for ciu-P20 and remain open as follow-up candidates,
one small package each, once a session with visibility into a real
consumer's existing hook can ground one against it (see
`nyxloom-trove/reports/ciu-P20-hook-template-library-LOG.md` for the full
reasoning — this mirrors the CIU-45 withdrawal lesson: an invented
capability claim for a service this repo has no specification for is worse
than none). A future `ciu config check` revision-comparison stage (comparing
a consumer's stamped `rev=N` against upstream) is also NOT built here.

All hooks currently in use by consumers (dstdns) must exist as versioned
templates in CIU upstream. Each template carries a `template_revision`
integer that increments on every behavioral change, enabling adoption
tracking: a consumer can query "am I running the latest revision of this
hook?" without comparing file hashes.

**Proposed structure:**

```
ciu/src/ciu/hook_templates/
  post_compose_db.py            # template_revision = 1
  post_compose_vault.py         # template_revision = 1
  post_compose_consul.py        # template_revision = 1
  post_compose_redis.py         # template_revision = 1
  post_compose_authentik.py     # template_revision = 1
  pre_compose_tailscale.py      # template_revision = 1
  ...
```

Each template implements:
- `run(config, ctx)` — the normal S9 execution entry point
- `validate_config(config, ctx) -> list[str]` — the §2.7 preflight contract

`ciu init --hooks db-core,vault,consul,...` copies the selected templates
into the consumer's stack directories, stamping the current
`template_revision`. A future `ciu config check` stage can compare the
consumer's stamped revision against upstream and report outdated templates.

**Adoption flow:** dstdns migrates from hand-maintained hook scripts to
CIU-managed templates. When CIU updates a template (new revision), dstdns
sees the drift during config check and can adopt selectively.

---

## CIU-QOL-14: Verify dstdns assay 2.3.0 pin includes B014 output capture

**Priority:** Medium (correctness of evidence)
**Effort:** Trivial
**Status:** OPEN — verify before relying on bounded output in verdicts

Assay B014 (bounded stdout/stderr tails on failed/timed-out commands) was
shipped in assay-v2.3.0. The dstdns repo currently pins `assay-2.3.0.pyz`.
Before relying on bounded output in verdicts, run a failing lane and confirm
the verdict contains non-empty stdout/stderr tails.

If the pin predates B014, repin to a released artifact that includes it.
