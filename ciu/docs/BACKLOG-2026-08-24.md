# CIU Backlog — 2026-08-24

Items derived from the SPEC reconciliation audit. Ordered by priority.

---

## CIU-QOL-1: Validate provisioning refs in single-stack mode

**Priority:** High (bug fix — silent validation gap)
**Effort:** Low
**Status:** ✅ IMPLEMENTED (commit pending)

`validate_stack_provisioning()` is now called inside `engine.main_execution`
after `validate_stack_shape()`. Previously a stack with malformed
`requires = ["bogus:nonsense"]` passed `ciu up --dir infra/foo` because the
check only ran in multi-stack preflight.

**Files:** `engine.py`, `SPEC.md S11`

---

## CIU-QOL-2: Implement `ciu.exit_on` as a closed-vocabulary exit policy

**Priority:** High (dead-key fix + QoL)
**Effort:** Low
**Status:** ✅ IMPLEMENTED (commit pending)

Replaces the previous boolean `fail_fast` with a single closed-vocabulary key:

```toml
[ciu]
exit_on = "WARN"    # "WARN" | "ERROR" | "NEVER" (default: "ERROR")
```

- `"WARN"`: exit on any warning OR error (strictest)
- `"ERROR"`: exit on errors only; warnings logged (**default**)
- `"NEVER"`: log all findings without ever aborting

Resolution: config → `$CIU_EXIT_ON` env var → default. Config always wins.
Invalid values are configuration errors naming the key and vocabulary.

**Files:** `warn_policy.py`, `deploy.py`, `CONFIG.md`, `SPEC.md S10.6`

---

## CIU-QOL-3: Remove `workspace_env_file` dead key

**Priority:** Low
**Effort:** Trivial
**Status:** OPEN — dstdns cleanup

`workspace_env_file` appears in dstdns's `[ciu]` block but CIU never reads it.
The filename is hardcoded via `config_constants.WORKSPACE_ENV`. Remove from
dstdns config; do not implement configurable env filenames (no use case).

---

## CIU-QOL-4: Remove `[ciu.ports]` from dstdns; move to topology/service

**Priority:** Low
**Effort:** Trivial
**Status:** OPEN — dstdns cleanup

dstdns uses `[ciu.ports].custom_http_port = 8080` as a convention referenced
by templates. This belongs under service identity or topology in v8, not
under CIU's reserved namespace. For now, document it as consumer-defined;
in v8 it moves to `[service.<name>.<level>].port`.

---

## CIU-QOL-5: Fix doc drift (ARCHITECTURE function names, FEATURES CLI table)

**Priority:** Medium (documentation accuracy)
**Effort:** Low
**Status:** ✅ IMPLEMENTED (commit pending)

- ARCHITECTURE.md: corrected `ksm.build_shim`→`build`,
  `transport_ssh.connect`→`ssh_exec`, added `activate.py` row,
  corrected `diagnose.diagnose`→`collect`+`run`.
- FEATURES.md: added missing verbs (`layouts`, `capabilities`, `host-secrets`).
- CIU.md: expanded common-options table with all shipped flags.

**Files:** `docs/ARCHITECTURE.md`, `docs/FEATURES.md`, `docs/CIU.md`

---

## CIU-QOL-6: Add `ciu status` verb

**Priority:** Medium (operator QoL)
**Effort:** Medium
**Status:** OPEN

No single command shows "which stacks are running, healthy, what images"
across a profile selection. `diagnose` shows failures; `provenance` shows
commit alignment; `health` gates. None give a dashboard summary.

**Proposed surface:**
```bash
ciu status [--profile NAME] [--json]
```

Read-only. Lists selected stacks → compose projects → running containers →
health status → image reference. JSON output uses a versioned envelope
consistent with worktree/provenance documents.

---

## CIU-QOL-7: Unified `ciu bake` respecting selection model

**Priority:** Medium (consistency)
**Effort:** Medium
**Status:** OPEN

Two build paths exist:
- Public `ciu bake [targets...] [--no-cache]`: passes targets straight to
  buildx; ignores profiles/phases/requires.
- Internal `deploy.action_build` (`--build` flag): filters targets by
  selected stacks under applications/tools/.

**Fix:** make public `ciu bake --profile core` resolve the selection and
build only matching targets. Remove internal action_build path.

---

## CIU-QOL-8: Per-service health-timeout override

**Priority:** Medium (operational flexibility)
**Effort:** Low
**Status:** ✅ FIXED (ciu-P15)

`[deploy.health].timeout` applies globally. Authentik needs ~240s; workers
need ~5s. Slow services must be excluded from gating or the global timeout
is set high enough to waste time on fast ones.

**Proposed addition to phase service entries:**
```toml
[[deploy.phases.phase_3.services]]
path = "infra/authentik"
health_timeout = "300s"   # optional; falls back to deploy.health.timeout
```

**Evidence:** `deploy_pkg.phases.service_health_timeout` (new accessor) +
`deploy_pkg.health.wait_for_gate_per_target` (new poll primitive — each
container gets its own deadline within one shared poll loop, instead of
one deadline shared by every container in the call) +
`resolve_selection_health_containers`/`run_container_health_gate` wired to
resolve and use per-container timeouts. See `docs/SPEC.md` S7.7 and
`docs/CONFIG.md`'s phase-service-entry section for the worked Authentik/
worker example, and
`nyxloom-trove/reports/ciu-P15-per-service-health-timeout-LOG.md` for the
combined-axis proof (a fast broken service resolves at its own 5s deadline
in the same gate call where a slow legitimate service resolves
independently at 200s, never waiting on the other).

---

## CIU-QOL-9: Document worktree concurrency budget in consumer docs

**Priority:** Low (discoverability)
**Effort:** Trivial
**Status:** ✅ IMPLEMENTED (ciu-P13)

`[ciu.worktree].max_concurrent_instances` defaults to unlimited and isn't
mentioned in README or CONSUMERS.md. A user who hits OOM won't know the
feature exists.

**Fix:** add a paragraph to README's worktree bullet and a worked example
to CONSUMERS.md showing how to set and verify the cap.

**Evidence:** README.md now points readers at docs/CONFIG.md's
`[ciu.worktree]` table and docs/CONSUMERS.md's existing worked example
(CONSUMERS.md already had the worked example; only README lacked the
pointer).

---

## CIU-QOL-10: Preflight check for silently-empty rendered values

**Priority:** High (correctness)
**Effort:** Medium
**Status:** DEFERRED TO V8 (§2.5 of proposal)

Currently a Jinja2 expression referencing an unknown variable renders as
empty string without error. A typo like `{{ contoller.name }}` produces
empty output that passes TOML parsing and may pass compose rendering until
the container fails at runtime.

**V8 approach:** wrap Jinja2 `Undefined` to raise on attribute access.
Withdraw "unknown variable renders empty." Templates MUST reference keys
that exist or use `{% if x is defined %}` explicitly.

This is a breaking change requiring the v8 revision gate.

---

## CIU-V8-PREP-1: `[app]` / user_tables namespace declaration

**Priority:** Deferred to V8 (breaking schema change)
**Effort:** Medium
**Design:** See proposal §1.14

Introduce `ciu.user_tables = [...]` so consumers declare which top-level
tables are theirs. Unknown non-reserved tables become validation errors.
Prevents future reserved-key collisions.

---

## CIU-V8-PREP-2: Build version management (`[build]` + `ciu refresh`)

**Priority:** Deferred to V8
**Effort:** Medium-High
**Design:** See proposal §1.13

Structured `[build.python]` namespace with `version` (constraint, never
auto-changed), `chosen_version` (resolved by `ciu refresh` for non-held),
`possible_version_upstream` (always written, even for held), freshness and
channel policies. See proposal §1.13.

---

## CIU-V8-PREP-3: Service identity model refinement

**Priority:** Deferred to V8 (breaking schema change)
**Effort:** High
**Design:** See proposal §3.1 and §1.15

Logical service names decoupled from filesystem paths via `location` field.
Global `[service.*]` registry eliminated; each stack owns its own identity.
Topology references services by logical name.

---

## CIU-V8-PREP-4: Stack-level `[local_stack]` service tables

**Priority:** Deferred to V8 (breaking schema change)
**Effort:** High
**Design:** See proposal §1.16

Every stack declares its services under a reserved `[local_stack]` key in
its own config. Replaces the current convention of deriving a root key from
the directory name. Realness is a global concern only — stacks don't know
about it.

---

## CIU-V8-PREP-5: One-shot completion semantics

**Priority:** Deferred to V8
**Effort:** Low
**Design:** See proposal §1.18

`one_shot = true` on phase services; health gate treats exit-0 as satisfied.
Requires use full stack path for provisioning refs.

---

## CIU-V8-PREP-6: Unified configfile fan-out + compose enumeration

**Priority:** Deferred to V8
**Effort:** Medium-High
**Design:** See proposal §1.19

Root-level `instances = N` drives both configfile fan-out and compose
service enumeration. Eliminates duplicate per-replica sections.

---

## CIU-V8-PREP-7: Environment variable declarations

**Priority:** Deferred to V8
**Effort:** Low
**Design:** See proposal §1.20

Inline list `env_required = [...]` on local_stack entries. CIU validates
before render. Known machine identity keys auto-injected into Jinja context.

---

## CIU-V8-PREP-8: Registry schema enforcement

**Priority:** Deferred to V8
**Effort:** Medium
**Design:** See proposal §2.6

Option B recommended: CIU ships Pydantic models for the five built-in
provisioning kinds. Consumer-defined registries validated hook-side.

---

## CIU-QOL-11: Wire remaining S11 validation checks into engine

**Priority:** Medium (completeness)
**Effort:** Low
**Status:** ✅ IMPLEMENTED (ciu-P14)

S11 catalog documents layout shape (S7.5c), exec-target declaration shape
(S16.7), and vendor_images type checks (S17.5) as validated. The catalog
entries were added, but the actual validation calls for these three are NOT
wired into `engine.main_execution` — only provisioning refs are.

**Fix:** add validation calls for these three checks in the single-stack
pipeline after `validate_stack_shape()`, matching what deploy.py already
does for profile-mode.

**Evidence:** `config_model.validate_declared_features` (new) composes the
three existing validators (`resolve_layout`, `resolve_exec_targets_config`,
plus a new vendor_images shape check) and is now called from BOTH
`engine.main_execution` Step 5 (single-stack) and `deploy.action_check`
(profile-mode, including an empty `selection`) — see
`tests/tests/test_ciu_config_model_layouts_eager.py`.

---

## CIU-QOL-12: Extend `ciu check` to full config validation with hook preflight

**Priority:** High (closes the "typos surface at runtime" gap)
**Effort:** Medium
**Status:** OPEN — implementable in current CIU without schema changes

Currently `ciu check` only validates the provisioning graph. It does NOT:
- Walk hooks to verify they load and export the right interface
- Call optional `validate_config()` on hooks that define it
- Validate registry shapes against built-in models
- Render configfiles or compose templates for validation

**Design:** see V8 proposal §2.7. The `validate_config` hook contract is
additive (hooks that don't define it are skipped), so it can ship now.

**Implementation plan:**
1. Add `validate_config` to HookContext documentation (S9)
2. Extend `action_check` in deploy.py to walk stages 1–12 from §2.7
3. Add built-in Pydantic models for PostgreSQL/Redis/MinIO/Consul/Vault registries
4. Add `--json` output with versioned envelope

This eliminates the need for consumers to run `ciu up --dry-run` as a
validation tool.

---

## CIU-QOL-13: Hook template library with revision tracking

**Priority:** High (enables config check + consistent adoption)
**Effort:** Medium
**Status:** OPEN — planned before v8

All hooks currently in use by consumers (dstdns) must exist as versioned
templates in CIU upstream. Each template carries a `template_revision`
integer that increments on every behavioral change, enabling adoption
tracking: a consumer can query "am I running the latest revision of this
hook?" without comparing file hashes.

**Proposed structure:**

```
ciu/src/ciu/hook_templates/
  post_compose_db.py            # template_revision = 1
  post_compose_vault.py         # template_revision = 1
  post_compose_consul.py        # template_revision = 1
  post_compose_redis.py         # template_revision = 1
  post_compose_authentik.py     # template_revision = 1
  pre_compose_tailscale.py      # template_revision = 1
  ...
```

Each template implements:
- `run(config, ctx)` — the normal S9 execution entry point
- `validate_config(config, ctx) -> list[str]` — the §2.7 preflight contract

`ciu init --hooks db-core,vault,consul,...` copies the selected templates
into the consumer's stack directories, stamping the current
`template_revision`. A future `ciu config check` stage can compare the
consumer's stamped revision against upstream and report outdated templates.

**Adoption flow:** dstdns migrates from hand-maintained hook scripts to
CIU-managed templates. When CIU updates a template (new revision), dstdns
sees the drift during config check and can adopt selectively.

---

## CIU-QOL-14: Verify dstdns assay 2.3.0 pin includes B014 output capture

**Priority:** Medium (correctness of evidence)
**Effort:** Trivial
**Status:** OPEN — verify before relying on bounded output in verdicts

Assay B014 (bounded stdout/stderr tails on failed/timed-out commands) was
shipped in assay-v2.3.0. The dstdns repo currently pins `assay-2.3.0.pyz`.
Before relying on bounded output in verdicts, run a failing lane and confirm
the verdict contains non-empty stdout/stderr tails.

If the pin predates B014, repin to a released artifact that includes it.
