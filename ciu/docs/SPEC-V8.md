# CIU v8 Specification

**Version:** 8.0.0-draft.6 · **Status:** DRAFT (ground-up specification for CIU v8; companion to `CIU-V8-TESTING-GATE-PROPOSAL.md` revision 3.x; worked examples `v8-dstdns-demo/`) · **Date:** 2026-09-03
**Supersedes:** 8.0.0-draft.4 (2026-09-03), draft.3 (2026-09-02) and draft.2 (2026-08-30). draft.3 was a fresh text after the first adversarial review (`CIU-V8-ADVERSARIAL-REVIEW-2026-09-02.md`, R-01..R-78): declaration files are plain TOML, consumers declare *bindings* instead of reading *routes*, secrets are structured tables, the contract of a LogicalService is derived, the instance lock is the checkout directory, and the gate has a zero-instance mode. draft.4 was draft.3 corrected in place after the independent third-party review (`CIU-V8-THIRD-PARTY-REVIEW-2026-09-02.md`, T-01..T-35; dispositions with reasoning in `CIU-V8-THIRD-PARTY-REVIEW-RESPONSE-2026-09-03.md`). draft.5 is draft.4 corrected in place after the same reviewer's round-2 delta audit (`CIU-V8-THIRD-PARTY-REVIEW-ROUND2-2026-09-03.md`: the disposition audit of T-01..T-35 and the new findings T2-01..T2-10): receipts have a portable subject and an activation id (S17.4.3–S17.5), acceptance is a partition (S8.5.5), every key, flag, state and reason draft.4 introduced is in its closed set and a conformance test guards the definition (S3.8.5), hook secrets are phase-restricted (S6.10), releases have a computable closure, image transport and a candidate pointer (S17.3), host-network endpoints declare `listen` (S6.3, S7.8), gate admission is keyed by the capacity object and every run has its own directory (S16.6.1, S16.9.4), `--move` needs the owner token (S4.5.3), the cgroup conversions are exact (S13.3), and the lint exemption is per value (S2.4.2) — plus two additions the operator decided on 2026-09-03: the canonical lock keys with `ciu lease` (S14.4.7–S14.4.8) and `[ciu] inherit` for a closed list of policy tables (S3.1.5). draft.6 is draft.5 corrected in place after the same reviewer's round 3 (`CIU-V8-THIRD-PARTY-REVIEW-ROUND3-2026-09-03.md`, T3-01..T3-10): every mutable file lives in a **state root** outside any release (S2.6) — the class defect behind the reviewer's first-deploy scenario; receipts are checked against an activation manifest's entry for the provider, never against the consumer's own release (S17.4); the activation state machine is prepare → apply → health → receipt → switch in both directions (S17.4.1); inheritance and build contexts may leave a child root but not the worktree, and inherited policy is flattened into a release (S3.1.5, S6.2); a lock matrix says which verbs take a Realization's stack lock, and lease records are lock-free files (S14.4.8–S14.4.9); a move is cold and a copied tree is refused, not adopted (S4.1.2); host-network reachability is probed from the consumer's vantage (S8.5.2a); run ids are random and `ciu gate --resume` is withdrawn (S16.9.4); the image map is per reference (S17.6.1); every hard cap aborts on read-back mismatch and every admitted lane declares `memory_max` (S13.3.2, S16.6.1); and S3.8.6 makes the document's own enumerations a tested output. Appendix D lists every changed rule with the finding that caused it, rounds 1–3.

This document is self-contained. It defines what a v8 CIU implementation MUST do, in normative language (MUST / MUST NOT / SHOULD / MAY as in RFC 2119). Rules are numbered `S<section>.<rule>` so a refusal message, a test, or a review can cite exactly one rule. Every refusal an implementation emits MUST name the rule it enforces in the form `[S<n>.<m>]`. Rule numbers of draft.2 that were withdrawn are not reused; a rule that moved is cross-referenced in Appendix C.

---

## S1 Scope, vocabulary, invariants

### S1.1 What CIU does
CIU deploys a **project** — a set of Docker Compose stacks plus external and borrowed services — as isolated **instances** (one per checkout), on one or several **hosts**, at a chosen **realness** level per capability, and runs the project's **gate** (test lanes and judge lanes) either against a deployed instance or, for a project with no stacks, against nothing at all. CIU owns: configuration layering, identity, ordering, the resolution and delivery of every dependency a consumer declares (a *binding*), secret materialization and delivery, host placement and remote push, resource governance, preflight validation, and gate execution. CIU does not own: the contents of a service's compose stanzas beyond what it injects (S11), application configuration semantics, secret rotation inside applications, certificate issuance, proxy policy, or the judgment of test evidence (the judge is `assay`, an external program that CIU invokes only when a lane asks for it).

### S1.2 Glossary and entities
The word **service** is used in exactly these senses, always qualified when ambiguous: a *LogicalService* (`[service.<n>]`, a capability); a *RealizedService* (`[ciu_stack.<svc>]`, one container of a stack); the *variant service* (which RealizedService of a multi-service stack carries a capability, S5.2).

| entity | identity | meaning |
|---|---|---|
| **Project** | `project.name` | the consumer repository |
| **Instance** | `instance_id` | one checkout's deployment; the primary checkout is an instance too; a project with no Realizations has none (S16.11) |
| **LogicalService** | `[service.<n>]` | a capability consumers bind to; its **contract** is derived from what is bound (S5.3) |
| **RealnessVariant** | `[service.<n>].<level>` | which Realization (and which of its services) satisfies the capability at a level |
| **Realization** | `[realization.<n>]` | a concrete provider: `ciu_stack`, `external`, `joined` |
| **RealizedService** | `[ciu_stack.<svc>]` in a stack file | one deployable service of a `ciu_stack` |
| **Endpoint** | `endpoints.<e>` on a RealizedService or an external Realization | a reachable port or URL |
| **Binding** | `binds.<local>` on a RealizedService or a gate environment | a consumer's declared dependency on a LogicalService (optionally one of its endpoints), under a local name, with a wait rule and a delivery |
| **Resolution** (derived) | (consumer, local name) | how CIU satisfied a binding: network, host, port, URL, TLS facts, readiness prerequisites |
| **TypedFact** | `kind:selector` string | a provable statement about live infrastructure |
| **Host** | `[hosts.<h>]` | a machine with one address per address-plane Network |
| **Network** | `[network.<n>]` | a reachability domain: an address plane, or a proxy |
| **Bundle** | `[bundles.<b>]` | a set of LogicalServices that deploy together |
| **Layout** | `[layouts.<l>]` | placement of bundles on hosts and the networks each host reaches |
| **Identity** (derived) | per RealizedService/replica | container name, hostname, compose key, compose project, network |
| **Wave** (derived) | ordinal | a set of Realizations deployed together |
| **Environment** (gate) | `[testing.environments.<e>]` | where a lane's process runs; also the target of `ciu instance exec` |
| **Lane** (gate) | `[testing.lanes.<l>]` | one command, one judge invocation, or a sequence of lanes, with preconditions and caps |

### S1.3 Invariants (each later rule serves at least one)
- **I1 One source per fact.** A fact is declared in exactly one place; every other appearance is derived from it and marked derived.
- **I2 Fail at the earliest checkable point.** Schema → `ciu check` → deploy. No silent defaults for facts that exist elsewhere; the only defaults are policy defaults (values that are correct in the absence of information and shadow no fact — S3.4.4 names them).
- **I3 Derived values are visible.** Every derived value is written as data into a rendered, cat-able file.
- **I4 Mechanical checkability.** Closed vocabularies, referential integrity and graph completeness are verified by the tool, not by a reader.
- **I5 Nothing hidden.** No hidden directories; no ambient process environment as a configuration source; all machine-owned files are visible and gitignored by a fixed list.
- **I6 One derivation per identity.**
- **I7 Declaration is separate from resolution.** Needs are declared by capability under a consumer-chosen local name; how they are satisfied is computed and recorded.
- **I8 Declarations are data.** No declaration file is a template. Templates exist only for artifacts CIU hands to other programs (compose files, application config files).

### S1.4 Names and grammars
- `name` := `^[a-z][a-z0-9_]*$` — LogicalService, Realization, RealizedService, Endpoint, Network, Binding local names, secret keys, config-file names, hostdir purposes. Bundle, Layout, Environment and Lane names additionally allow `-` (`^[a-z][a-z0-9_-]*$`).
- `hostname` := `^[a-z][a-z0-9-]*$` for Host names (one DNS label).
- `dns_name` := one or more RFC 1123 labels (`[a-z0-9]([a-z0-9-]*[a-z0-9])?`, each ≤ 63 bytes) joined by `.`, ≤ 253 bytes, no trailing dot in stored form — for every `fqdn` key (S7.2, S7.3) and for `url` hosts.
- `envname` := `^[A-Z][A-Z0-9_]*$` for environment-variable names and `env_prefix`.
- `duration` := `^[0-9]+(ms|s|m|h)$`. `size` := `^[0-9]+(K|M|G|T)?$` (bytes, binary units) or `"max"`.
- `level` := `live` | `seeded` | `simulated` | `mock`.
- `version-floor` := `^>=[0-9]+(\.[0-9]+){0,2}$`.
- `target` (a binding's `to`) := `<name>` (a LogicalService) or `<name>.<name>` (a LogicalService and one of its endpoints).
- TypedFact grammar (S5.6): `pg:db/<name>`, `pg:role/<name>`, `pg:schema/<name>`, `minio:user/<name>`, `minio:bucket/<name>`, `vault:secret/<path>[#field]`, `http:<path>`, `pki:issuer/<network>`.
- Reserved names: a Realization MUST NOT be named `hosts` or `ciu`; a RealizedService MUST NOT be keyed `secrets`; a consumer sub-table of a service MUST NOT be named `identity`, `health`, `endpoints`, `binds`, `hostdir`, `configfile`, `secrets`; a binding local name MUST NOT equal another binding's local name of the same service.

### S1.5 Root resolution
S1.5.1 The checkout root is the nearest ancestor of the working directory (inclusive) that contains `ciu.toml`; `--root <dir>` overrides it. No environment variable names the root. S1.5.2 The nearest wins, always. A checkout root that has an ancestor directory containing `ciu.toml` (a subproject of a monorepo) is reported once as INFO (`[S1.5] nested inside ciu root <path>`) unless its own file declares `[ciu] standalone_root = true`, which marks the nesting intended and silences the notice; neither form reads the ancestor's tables — only `[ciu] inherit` does that, explicitly and for a closed list (S3.1.5).

---

## S2 Files

### S2.1 Project root (every path relative to the checkout root)
| file | committed | written by | role |
|---|---|---|---|
| `ciu.toml` | yes | consumer | the project's declarations (S3); plain TOML |
| `ciu.site.toml` | yes, optional | consumer | sparse site override of any declared table, including re-rooted stack tables (S3.1.3) |
| `ciu.instance.toml` | **no** | operator (and `ciu instance init/add`) | per-instance operator declarations: layout, bundles, label, joins, host-port overrides (S14.2); plain TOML, round-trip-edited by CIU only to append or replace whole tables |
| `ciu.instance.generated.toml` | no | ciu | CIU-owned facts merged after the instance file that are true on every host of a layout: instance identity (`instance_id`, `owner_id`), build facts (S14.2); travels with a release unchanged |
| `ciu.host.toml` | no | ciu (`instance init [--host]`) | host-local facts `[ciu.host]` merged last (S14.2); lives in the **state root** (S2.6), never in a release |
| `ciu.resolved.toml` | no | ciu | the rendered merged configuration plus every derived table under `[resolved]` (S3.7); written atomically; the machine interface for every reader |
| `ciu.instance.json` | no | ciu | instance registry record (S14.7), including the realness records (S9.4); state root |
| `ciu-leases/` | no | ciu (`lease acquire`) | one atomic holder record per lease (S14.4.8); state root |
| `ciu.hosts.toml` | no | operator | host inventory (S7.2, S17.1) |
| `ciu.secrets.toml` | no | ciu | materialized secrets store (S10.6) |
| `assay.toml` | yes | consumer | judge lanes (owned by assay; CIU never parses it, S16.7) |
| `ciu.gate.<lane>.json` | no | ciu | the last LaneResult per lane (S16.9); shared-resource locks are directory locks (S16.5.3), not files |
| `ciu.receipt.json` | no | ciu | activation receipt of the last successful `up` on this host (S17.4.3) |
| `ciu.release.json` | no (in a release) | ciu | release manifest of a pushed release (S17.3.1); present only under `<bundle_dir>/releases/<digest>/` |
| `<evidence_dir>/` (default `ciu-gate-evidence/`) | no | ciu | gate artifacts, verdicts, LaneResult history (S16.2) |
| `ciu-data/` | no | ciu | generated host directories (S6.8) |
| `ciu.env` | no | ciu (`ciu env print > ciu.env`) | derived shell export for humans and legacy tooling; **never read by ciu** |

User-global: `~/.config/ciu/hosts.toml` (S17.1), `$XDG_CACHE_HOME/ciu/` (build caches).

### S2.2 Stack directory (`location` of a `ciu_stack` Realization)
| file | committed | written by | role |
|---|---|---|---|
| `ciu.stack.toml` | yes | consumer | the stack's declarations (S6); plain TOML |
| `ciu.compose.yml.j2` | yes | consumer | compose template (S11) |
| `ciu.compose.yml` | no | ciu | rendered compose file, injections applied; the file compose runs |
| `ciu.state.toml` | no | ciu (hook outputs) | hook-persisted non-secret state (S6.10, S12.3) |
| `ciu.rendered/<svc>/<mirrored target path>` | no | ciu | rendered config files, mounted by parent directory (S6.9) |
| `ciu.secret-copy.<svc>/<key>` | no | ciu | per-service secret directories, bind-mounted at `/run/secrets` (S10.7) |
| hook scripts, config templates | yes | consumer | referenced by name from the stack file |

There is no per-stack rendered TOML: the stack's tables as CIU resolved them are in `ciu.resolved.toml` under `realization.<R>` (S3.6).

### S2.3 Gitignore list
S2.3.1 The following patterns MUST be ignored in every checkout, and `ciu check` stage 1 MUST verify each is ignored (`git check-ignore`) when the checkout is a git work tree: `ciu.resolved.toml`, `ciu.instance.toml`, `ciu.instance.generated.toml`, `ciu.host.toml`, `ciu.instance.json`, `ciu-leases/`, `ciu.compose.yml`, `ciu.state.toml`, `ciu.rendered/`, `ciu.secret-copy.*`, `ciu.secrets.toml`, `ciu.secrets.transport.toml`, `ciu.hosts.toml`, `ciu.gate.*`, `ciu.receipt.json`, `ciu.release.json`, `ciu.activation.json`, `ciu.inherited.toml`, `ciu.env`, `ciu-data/`, and the configured `testing.evidence_dir`. S2.3.2 A directory named `.ciu` anywhere under the checkout is an ERROR (`[S2.3] legacy .ciu directory present; run ciu migrate`), except during `ciu migrate`. S2.3.3 CIU MUST NOT create any hidden file or directory inside a checkout. S2.3.4 Deleting these files while an instance exists (`git clean -x`, a manual `rm`) is **destructive**, as it was in v7: `ciu.secrets.toml` (generated values that exist nowhere else), `ciu.instance.toml`, `ciu.instance.json`, the realness records in the generated file and `ciu-data/` cannot be regenerated; `ciu render` recreates only `ciu.resolved.toml`, `ciu.compose.yml`, `ciu.rendered/`, the temp copies and `ciu.env`; `ciu instance init` re-derives the same `instance_id` from the physical path (S4.1.1) and adopts still-running containers by their `ciu.checkout` label (S4.5.3), but their secrets are gone. The lock (S14.4) is unaffected. `ciu check` stage 1 prints this once per checkout the first time it runs; `ciu instance backup` and `ciu instance restore` (S14.8) copy the non-regenerable files out of and back into the tree. The operator chose this posture over a registry outside the checkout (third-party review T-11, response 2026-09-03) because every file stays visible and the hazard is the same one v7 documented.

### S2.4 Secret lint (a heuristic, never a certification)
S2.4.1 Every committed template (`ciu.compose.yml.j2`, config-file templates) and every declaration file is **linted** before use. Two classes: (a) **high-confidence** — a PEM block (`-----BEGIN`), a value matching a known credential format (`ghp_…`, `hvs.…`, `AKIA…`, `xox[abp]-…`, a JWT `eyJ….eyJ…`), or an `Authorization:` header with a literal credential — is an ERROR (`[S2.4]`); (b) **heuristic** — a key whose last `_`-separated component is one of `password`, `token`, `secret`, `credential`, `passphrase`, or whose last two components are `api_key`, `private_key`, `secret_key`, `access_key` (a bare `key` is not sensitive: `work_stream_key = "dstdns:streams:…"` is a Redis key name), paired with a literal string value of 8 or more characters that is not a `{{ … }}` or `${…}` reference, not a `/run/secrets/` path, and not a `/`-bearing KV path (`^[a-z0-9_.-]+(/[a-z0-9_.-]+)+$`) — is a WARN naming the key, suppressible per table path in `[ciu] secret_lint_allow = ["<table path>", …]` (never per value). S2.4.2 After rendering, `ciu.compose.yml` and every file under `ciu.rendered/` MUST be compared against every **store value** (S10.6) of 8 or more characters — every file against every value, with no file-wide exemption. A verbatim match — the one comparison that is exact — is an ERROR and the artifact MUST be deleted before the refusal is reported, with exactly one suppression: a match in a rendered config file whose store coordinate is a `delivery = "configfile"` secret declared for that file's service **and** whose key the template actually requested through `secret("<key>")` during this render (CIU records the keys each render called, S10.2.6). A match of any other store value in the same file — the Vault root token rendered through a context bug next to a legitimately delivered password — remains an ERROR. S2.4.3 CIU's output for this stage names exactly which comparisons ran and how many authorized matches it omitted ("PEM/known formats: 0 hits; heuristic keys: 2 warnings; store values: 41 compared, 0 hits, 3 authorized configfile matches omitted") and MUST NOT describe a file as "secret-free".

### S2.5 File modes
`ciu.secrets.toml` 0600; `ciu.secret-copy.*` per the secret's `mode`/`uid` (default 0400, `instance.user_uid`); rendered files 0644; `ciu.instance.toml`, the generated file and the host file 0644; lease records 0644; LaneResults written on failure 0600 (S16.9.3); a release's state root 0700 (S2.6).

### S2.6 State root — where mutable state lives
S2.6.1 Every instance has one **state root**. For a git or plain checkout it is the checkout root itself — the in-checkout posture the operator chose (S2.3.4). For a **release** — a checkout whose root carries `ciu.release.json` (S17.3.1), at `<bundle_dir>/releases/<digest>/` — it is `<bundle_dir>/state/`: the release directory's grandparent plus `state/`, derived, never configured, created 0700 by `prepare` (S17.4.1). A release directory is immutable after verification (S17.3.3): nothing CIU writes at runtime lands in it except the regenerable render outputs of S2.2 (`ciu.compose.yml`, `ciu.rendered/`, `ciu.secret-copy.*`), which belong to that release and are deleted with it. S2.6.2 The state-root files are: `ciu.host.toml` (S14.2), `ciu.instance.json` (S14.7, carrying the realness records of S9.4), `ciu.secrets.toml` (S10.6), `ciu.receipt.json` (S17.4.3), `ciu-leases/` (S14.4.8), `ciu-data/` (S6.8), `<evidence_dir>` and `ciu.gate.<lane>.json` (S16), and — in a release only — the per-stack `ciu.state.toml` at `<state root>/stacks/<R>/ciu.state.toml` (in a checkout it stays next to its stack, S2.2, so a developer sees it where the hooks are). Every rule that names one of these files means its state-root location; `ciu instance show` prints the state root. S2.6.3 All releases of one instance share one state root — which is what lets a release switch or a rollback (S17.4.1) keep secrets, data, records and leases — and the state root never travels and is never inside a release. Nothing else is stateful: a file not listed here and not in S2.2's render outputs is either a committed input or a bug.

---

## S3 Configuration model

### S3.1 Layers and merge
S3.1.1 The global configuration is the deep-merge, in order, of: the inherited policy tables (S3.1.5, if any) → `ciu.toml` → `ciu.site.toml` (if present) → `ciu.instance.toml` → `ciu.instance.generated.toml` → `ciu.host.toml` (S14.2, from the state root). All are plain TOML; none is rendered (I8). S3.1.2 Merge semantics: tables merge recursively; scalars and lists REPLACE; a table cannot be deleted by a later layer — a service, secret, binding, endpoint, lane or environment is removed with `enabled = false` (S6.7), and `{}` is a no-op. S3.1.3 A stack's declarations are the deep-merge of the global configuration with `<location>/ciu.stack.toml`, whose tables are **re-rooted** per S3.6 before the merge; a site or instance layer MAY therefore carry `[realization.<R>.services.<svc>.…]`, `[realization.<R>.secrets.…]`, `[realization.<R>.hooks]` or `[realization.<R>.governance]` tables, which merge over the stack file's tables — the one override mechanism for stack declarations. S3.1.4 The instance file and the generated file MUST exist for every mutating verb except `instance init`; absence is an ERROR (`[S3.1] no instance file; run ciu instance init`) — except in a zero-instance project (S16.11), where neither exists and no verb but `check`, `gate`, `schema`, `doctor`, `version`, `show effective` and `init` is permitted. S3.1.5 **Inheritance of policy tables.** `[ciu] inherit = "<path>"` (S3.4.7) names another `ciu.toml` by a path relative to the declaring file — typically an ancestor in a monorepo (`"../ciu.toml"`). The path MAY leave the checkout root (a child project's root is nearest, S1.5) but MUST stay inside the **containing worktree**: the git top level of the checkout (`git rev-parse --show-toplevel`), or the checkout root itself for a non-git checkout; it is canonicalized at load, and a path outside that boundary is an ERROR (`[S3.1] inherit ../../elsewhere/ciu.toml leaves the worktree /workspaces/vbpub`). In a release (S17.3.1) the reference is not resolved at all: `ciu push` **flattens** the inherited tables into `ciu.inherited.toml` at the release root — `[ciu.inherited] sources = [{ path = "<worktree-relative>", sha256 = "…" }, …]` followed by the merged inheritable tables — and a release's `ciu.toml` `inherit` key is satisfied by that file alone, so no release depends on a mutable `../` file on the target. Before the layering of S3.1.1, the **inheritable tables** of the named file — the closed list `governance`, `project.health`, `testing.environments`, `testing.judge`, `testing.cgroup_slice`, and nothing else — are merged *under* the declaring file with S3.1.2 semantics (the nearer file wins per key); the named file's own `inherit` is followed first, recursively, with a canonical-path visited set that refuses a cycle by printing the chain (`[S3.1] inherit cycle: nyxloom/ciu.toml → ciu.toml → nyxloom/ciu.toml`). Lanes, externals, services, realizations, networks, bundles, layouts, realness, vault, secrets, the evidence directory, `history` and every `[ciu]` key are never inherited; a table outside the list in the inherited file is simply that project's own. Every path-valued field of an inherited environment (`extra_mounts` host sides, `image` build contexts) is normalized relative to **the file that declared it** before the merge, so a mount declared in the estate file mounts the estate's directory, not the leaf's; an inherited environment that names `exec_in` is an ERROR at stage 12 (it names a LogicalService of another project). The inherited file is read as data only — whether it is itself a valid project is checked only when CIU runs in it — and it MAY be a zero-instance project that carries `[governance]` for its inheritors (S16.11.1). `ciu show effective [--json]` (S18) prints the fully merged configuration with every inherited table marked with its source file, for consumers that want no inheritance at runtime. This is the one sharing mechanism between projects: no walk-up, no meta-root, no table a file did not name (I2, I5).

### S3.2 Templates
S3.2.1 Only `ciu.compose.yml.j2` and the config-file templates named by `configfile.<n>.template` are rendered, as Jinja2 with `StrictUndefined`: any reference to an undefined name, attribute or item is an ERROR naming the template and the expression (`[S3.2] undefined 'binds.databse' in applications/controller/ciu.compose.yml.j2:31`). S3.2.2 The render context contains ONLY the names listed in S3.5 and the Jinja built-ins. There is NO `env` mapping and NO access to the process environment from a template. S3.2.3 In compose templates `${VAR}` sequences are left for Docker Compose interpolation and MUST name a variable in S11.6's set; `$$` is an escaped dollar. `${VAR:-…}` and `${VAR:?…}` forms are an ERROR (S11.3). S3.2.4 `{% set %}`, loops and conditionals are permitted (data expansion); `{% include %}`, `{% import %}`, `{% extends %}`, custom filters and functions are NOT provided (`[S3.2] template loader disabled`). S3.2.5 Rendering is deterministic: the same inputs MUST produce byte-identical outputs. S3.2.6 A `.j2` suffix on a declaration file name (`ciu.toml.j2`, `ciu.stack.toml.j2`, `ciu.global.*.toml.j2`) is an ERROR naming Appendix A (`[S3.2] declaration files are plain TOML in v8; run ciu migrate`).

### S3.3 Top-level tables of the global configuration
Closed set. Any other top-level table is an ERROR (`[S3.3] unknown top-level table 'foo'`) unless it is named in `ciu.user_tables`.

| table | defined in | owner |
|---|---|---|
| `project` | S3.4 | consumer |
| `service` | S5.2 | consumer (+ instance-file rows for joins) |
| `realization` | S5.4 | consumer (+ instance-file rows for joins; + site/instance overrides of re-rooted stack tables, S3.1.3) |
| `network` | S7.3 | consumer |
| `bundles` | S7.5 | consumer |
| `layouts` | S7.6 | consumer |
| `realness` | S9.2 | consumer |
| `vault` | S10.3 | consumer |
| `registry` | S3.4.6 | consumer |
| `governance` | S13 | consumer |
| `testing` | S16 | consumer |
| `ciu` | S3.4.7 | consumer |
| `ciu.instance` | S14.2 | operator (instance file) + ciu (generated file) |
| `ciu.host` | S14.2 | ciu (host file, state root) |
| `ciu.inherited` | S3.1.5 | ciu (`push`; a release only) |
| `resolved` | S3.7 | ciu only (rendered file) |

`hosts` is the top-level table of `ciu.hosts.toml` (S7.2), never of the global configuration.

### S3.4 `[project]` and `[ciu]`
S3.4.1 `[project]`: `name` (name, required, literal), `revision` (integer, required, MUST equal `8` — `[S3.4] config revision 7 is not 8`), `log_level` (`DEBUG|INFO|WARN|ERROR`, default `INFO`), `landscape_id` (`^[a-z][a-z0-9-]{0,62}$`, optional; consumer fact bound as `instance.landscape_id`). S3.4.2 `[project.registry]`: `url` (string, `""` = local daemon), `namespace` (name; required when any stack image or `ciu build` references it). S3.4.3 `[project.compose_env.<VAR>]`: string values exported to every compose process (S11.6). `[project.control.<flag>]`: booleans only (S6.7). There is no image list on the project: an image reference is project-built iff some service of the deploy set declares `build` for it (S6.2) — services that name the same reference without `build` share that build — otherwise it is pulled and recorded by digest. S3.4.4 `[project.health]`: `interval` (default `10s`), `timeout` (default `5s`), `start_period` (default `60s`), `retries` (default `6`), `gate_timeout` (optional) — the defaults merged into every RealizedService's `health` table (S6.6). These four are **policy defaults** (I2): they shadow no fact and are correct for a service that declares nothing. S3.4.5 There is no `[project.env.defaults]`: template data that CIU attaches no semantics to lives in a consumer user table. S3.4.6 `[registry.*]`: free-form consumer metadata; CIU reads only `registry.postgresql.database` (target database of `pg:schema/*` probes); the rest is validated by the consumer's `ciu.registry_validator` (S15.3 stage 14). S3.4.7 `[ciu]`: `standalone_root` (bool, S1.5.2), `inherit` (path to another project's `ciu.toml`, relative to this file, optional; S3.1.5), `require_fqdn` (bool; `instance init` refuses when the rendered host declares no `fqdn`, S7.2), `auto_connect_network` (bool, default true; S7.8.7), `exit_on` (`WARN|ERROR|NEVER`, default `WARN`), `user_tables` (list of names, default `[]`), `registry_validator` (path, optional), `secret_lint_allow` (list of table paths, default `[]`; S2.4.1). `[ciu.instances]`: S14.6.

### S3.5 Template contexts
S3.5.1 **Compose and config-file render** of the stack bound to Realization `R` sees exactly: `project` (S3.4), `instance` (S3.5.2), `host` (S3.5.3), `ciu_stack` (this stack's services with derived `identity`, merged `health`, resolved `endpoints` incl. `allow_from_resolved` and `publications`, and — per service — `binds.<local>` bound to that binding's resolution (S7.8) when its delivery is `template`), `realization` (the merged view of every Realization in the deploy set, S3.6 — read-only), `networks` (the derived network table, S7.9 — `networks.edge.fqdn`), `state` (S6.10), `stack_dir` (the physical host path of the directory being rendered), the stack's own consumer top-level tables by name, every global user table by name, `registry`, `vault.paths`, and — config-file templates only — the function `secret("<key>")` for secrets declared with `delivery = "configfile"` (S10.2.6). Inside a service's replica loop, `replica` is bound to the replica's identity row (S6.5). S3.5.2 `instance` is a read-only mapping assembled from `[ciu.instance]`, `[ciu.instance.generated]`, `[ciu.instance.build]` and `[resolved]`: `id`, `project`, `label`, `layout`, `environment` (S7.6), `network` (the instance network's name), `landscape_id`, `services` (the list of selected LogicalService names, for template guards), `build_version`, `build_time`. S3.5.3 `host` is a read-only mapping of `[ciu.host]` (S14.2) — `name`, `hostname`, `env_type`, `user_uid`, `user_gid`, `docker_gid` — plus the derived `repo_root` and `physical_repo_root` of the checkout being run (S14.2), `fqdn` and `addresses.<network>` from the host inventory. S3.5.4 A binding with `delivery = "env"` or `"none"` is NOT bound in any template; referencing it is an undefined-name ERROR (S3.2.1) whose message names the binding's delivery. S3.5.5 A stack's own top-level consumer table whose name equals a global top-level table (S3.3), a global user table, or a name in S3.5.1's context is an ERROR.

### S3.6 Re-rooting and the merged view
S3.6.1 A stack file declares its services under the fixed root `[ciu_stack.<svc>]` (S6.1), MAY declare stack-level secrets under `[ciu_stack.secrets.<key>]` (S10.2), and MAY declare the reserved tables `[hooks]`, `[governance]`. S3.6.2 When the stack bound to Realization `R` is loaded, CIU MUST place its service tables at `realization.R.services.<svc>`, its stack-level secrets at `realization.R.secrets`, its reserved tables at `realization.R.hooks|governance`, its state (S6.10) at `realization.R.state`, and its consumer top-level tables at `realization.R.<table>`. S3.6.3 A consumer top-level table named `services`, `secrets`, `hooks`, `governance`, `state`, `kind`, `location`, `per_host`, `provides`, `endpoints`, `instance`, `service` or `hosts` is an ERROR (`[S3.6] reserved stack table 'services'`). S3.6.4 The merged view is what `ciu.resolved.toml` carries under `[realization.<R>]` and what templates and hooks read as `realization`.

### S3.7 Derived tables — `[resolved]`
S3.7.1 CIU MUST write, on every render, the derived tables specified in S4.4 (identities and endpoints), S7.7 (placement), S7.8 (binding resolutions), S7.9 (networks), S8.8 (waves, gates, edges), S9.3 (selection), S13.4 (effective governance), S17.3 (bundle contents when rendered with `--layout`) into `ciu.resolved.toml` under `[resolved]`, together with `api = "ciu/resolved"`, `api_version = 1`, `layout`, `host`, `environment` and `rendered_at`, and — for machine readers that start from a capability — an index `[resolved.capabilities.<logical>] level, realization, service, container_name, endpoints = { <e> = { host, port } }` (the selected variant's identity and endpoints, S3.7.6). S3.7.2 A `resolved` table in any INPUT layer is an ERROR (`[S3.7] resolved is derived`). S3.7.3 The derived tables are regenerated identically from the same inputs; they carry no state. The file's only time- or host-dependent content is `rendered_at` and `[ciu.host]` (merged from the state root's host file, S14.2); no rule compares whole-file digests (receipts compare a subject built from the plan, S17.4.3). S3.7.4 The rendered file is emitted in canonical order: input tables in S3.3 order, then `[resolved]` sub-tables in the order listed in S3.7.1, keys sorted within each table. S3.7.5 The rendered file is written by temp-file and atomic rename (S14.4.2); a reader therefore sees a complete file or none. S3.7.6 `ciu query <kind> … --json` (S18) answers narrow questions from the rendered file with the S18.4 header — `identity --capability main_db`, `identity --realization db_core --service postgres`, `binding --consumer controller.controller --name database`, `publications --layout prod3`, `capability main_db`, `lanes` — so that scripts depend on a stable query, not on the TOML layout.

### S3.8 Validation of the schema itself
S3.8.1 Every table in this specification has a closed key set; an unknown key is an ERROR naming the table and the key (`[S3.8] unknown key 'memroy_max' in [governance]`). Consumer data is admitted only where a rule says "consumer data" (sub-tables of a service, user tables, `[registry.*]`, `[vault.paths]`, stack-level consumer tables). S3.8.2 Types and vocabularies are as stated per rule; a violation is an ERROR naming the rule. S3.8.3 Referential rules (a name that must resolve) are enforced by `ciu check` stage 4 (S15.3). S3.8.4 The closed key sets MUST be defined once, declaratively; the validator, the documentation and `ciu schema --json` (a JSON Schema for `ciu.toml`, `ciu.site.toml`, `ciu.instance.toml`, `ciu.stack.toml` and `ciu.hosts.toml`) are generated from that single definition (I1). Referential and graph rules are not expressible in JSON Schema and are not claimed by it. S3.8.5 The single definition of S3.8.4 MUST be exercised by a table-driven conformance test generated from it: for every key, every closed-vocabulary value, every CLI flag a rule names, and every result field, state and reason of S16.8–S16.9, one accepted and one refused example; a rule that introduces a key, flag, state or reason without extending the definition fails that test — the defect class draft.4 exhibited (round-2 review T2-03: eight surfaces required by one rule and rejected by another) is thereby caught by the build, not by a reader. S3.8.6 **The specification's own enumerations are checked, not trusted.** The implementation emits every closed surface from the single definition — `ciu schema --json` for keys and vocabularies, `ciu version --surfaces --json` for the `api` names and their `api_version`, the environment variables it reads, the CLI flags per verb, the NOT_RUN reasons, the gate vocabularies (`probes.result`, `assumed.reason`) and the LaneResult fields — and the implementation's test suite parses this document's enumerating rules (S3.4.7, S6.10, S8.5.4, S13.1, S16.8, S16.9, S18, S18.2, S18.4) and fails when a set in the text differs from the emitted one. draft.5 was the proof that two lists are not kept equal by hand (round-3 review T3-06: `unprobed`, `ciu/backup`, `XDG_*` were required in one place and absent in the other); from V8-1 on, the document and the implementation are compared by a test.

---

## S4 Identity

### S4.1 Instance identity
S4.1.1 `ciu instance init` derives `instance_id` as the first 6 hex characters of SHA-256 over the checkout's **physical** absolute path (the host path, resolved through the container's mount table when running inside a container), and writes it into `[ciu.instance.generated]` of `ciu.instance.generated.toml`, together with a random 128-bit **owner token** `owner_id` (hex) generated once at that moment and never derived from anything; host-local facts go into `ciu.host.toml` in the state root (S14.2, S2.6). The token is not an identity — it appears in no name — and it is not a proof of continuity either: it is the mark that tells this instance's resources from a colliding checkout's (S4.5.3). S4.1.2 **Moves and copies.** Without `--host`, re-running `init` on a checkout whose physical path changed is an ERROR (`[S4.1] physical path changed from /work/a to /work/b; instance identity would change`) unless `--move` is given. `--move` is a **cold** operation: it is refused while any container, network or named volume carries the old `ciu.owner` token (`[S4.1] 7 resources of owner 3f9c… are live under /work/a; ciu down && ciu clean there first — named volumes cannot follow a move`); when none does, it derives the new `instance_id` from the new path, mints a **new** `owner_id`, keeps the instance file, the store and the other state-root files, re-registers the record (S14.7), and prints both ids. There is no live move: Docker object labels are immutable for an object's lifetime, so resources cannot be re-stamped in place, and a token that lives in a copyable file cannot prove which of two trees is the original — the refusal is exactly what makes the copy case safe: a copied tree (`cp -a`, a backup restored at another path) finds the origin's resources live and is refused. `ciu instance init --fresh` in such a tree discards the copied identity (a new `instance_id` from its own path, a new token), keeps the operator's instance file, and WARNs that the copied store shares secret values with the origin (`ciu secrets reset` re-mints). With `--host <h>` in a release (S17.4.1 `prepare`), only the host file is written and no path comparison is made: identity comes from the release's generated file. S4.1.3 The primary checkout is an instance like any other. S4.1.4 **The physical path is proven, not assumed.** `physical_repo_root` is the path the Docker daemon sees; on a native host it is the checkout path, in a container it is read from the container's mount table, and on a daemon that runs in another namespace (Docker Desktop's VM, a remote daemon) neither is provable locally. `instance init` therefore verifies the candidate path by a **sentinel bind**: it writes a random file into the checkout, runs a throw-away container that bind-mounts the candidate path and reads the file, and refuses when the file is not visible (`[S4.1] daemon cannot see /workspaces/dstdns as /home/vb/dstdns; pass --physical-root or fix the mount`). A path that fails the sentinel is never written to the generated file.

### S4.2 The derivation function
S4.2.1 Let `R' = R.replace("_", "-")` and `svc' = svc.replace("_", "-")`. For every RealizedService `svc` of a `ciu_stack` Realization `R` in project `P` with instance id `I`:
- `compose_project = "{P}-{I}-{R'}"`
- `compose_key = "{R'}-{svc'}"` when `svc ≠ R`, else `"{R'}"`
- `container_name = hostname = "{P}-{I}-{R'}-{svc'}"` when `svc ≠ R`, else `"{P}-{I}-{R'}"`
- with `instances = N > 1`: replica `k` (1..N) has `container_name = hostname = <base>-{k}` and `compose_key = <base_key>-{k}`; the service-level `compose_key` (`<base_key>`) is injected as an alias on every replica (S11.4) so it resolves to all replicas
- `network = "{P}-{I}-network"` (the instance network; created by CIU, S14.1.2)
S4.2.2 The service component is omitted when `svc == R`. S4.2.3 No other code path in CIU, in a template, in a hook, or in the gate MAY form these strings; they are read from `[resolved.identities.R.svc]` (S4.4) or from the `ciu_stack.<svc>.identity` render binding. S4.2.4 A `per_host` Realization (S7.6.5) has the same identity on every host it runs on (names are per daemon). S4.2.5 The `_` → `-` mapping is injective per name but the concatenation is not: `db_core` + `postgres`, `db` + `core_postgres` and a single-service Realization `db_core_postgres` all derive `…-db-core-postgres`. Uniqueness is therefore **checked, not structural** (S4.3.1); the refusal MUST print both derivations and name the ambiguous boundary.

### S4.3 Uniqueness and enforcement
S4.3.1 `ciu check` stage 8 MUST assert, per host: `compose_project` is unique **among Realizations** (every service and replica of one Realization shares its one `compose_project`); `container_name` (= `hostname`) is unique among all services and replicas of all Realizations; `compose_key` and every injected network alias (service key, replica key, `container_name`, `aliases`) are unique on the instance network across all Realizations. The check runs at the maximum replica index and after `_` → `-` mapping and elision (`[S4.3] 'dstdns-98535c-db-core-postgres' is derived for db_core.postgres and for db.core_postgres`). S4.3.2 A rendered compose service that declares `container_name:` or `hostname:` with a value different from the derived one is an ERROR (`[S4.3] container_name 'x' differs from derived 'y'`); equal values are tolerated and removed; a `host_network = true` service (S6.2) has no injected `hostname`. S4.3.3 A derived `container_name` (replica suffix included) longer than 63 characters is an ERROR naming the service.

### S4.4 Identity and endpoint facts (derived table)
```toml
[resolved.identities.<R>.<svc>]
container_name = "…"  hostname = "…"  compose_key = "…"  compose_project = "…"  network = "…"
replicas = [ { index = 1, container_name = "…", compose_key = "…" }, … ]   # present only when instances > 1
[resolved.identities.<R>.<svc>.endpoints.<e>]
port = 5432  protocol = "tcp"  publish = "instance"  host_port = 5432  path = "/…"   # S7.4; joined instances resolve by this
publications = [ { scope = "network", network = "mesh", bind = "100.64.0.11", port = 5432, protocol = "tcp" } ]   # socket claims (S7.4.6); [] when nothing is published
```
S4.4.1 `hostname` always equals `container_name`. S4.4.2 The identity mapping is bound as `ciu_stack.<svc>.identity` in the stack's compose and config-file renders.

### S4.5 Labels
S4.5.1 CIU stamps every container, network and named volume it creates with the fixed labels `ciu.project`, `ciu.instance` (the 6-hex id), `ciu.owner` (the instance's `owner_id`, S4.1.1), `ciu.checkout` (the physical checkout path the id was derived from), `ciu.realization`, `ciu.service`, `ciu.replica` (when replicated), `ciu.managed-by=ciu`. `ciu clean`, `ciu instance reap`, `ciu diagnose` and the gate enumerate resources by these labels only, never by name pattern; the label namespace does not depend on any consumer setting, so a consumer change can never orphan a resource. S4.5.2 A template label whose key starts with `ciu.` is an ERROR (`[S4.5] label namespace ciu.* is reserved`). Consumer labels are authored in templates from `ciu_stack.<svc>.identity.*`. S4.5.3 **Ownership before deletion; adoption.** A mutating verb that removes or recreates a resource MUST verify that its `ciu.project`, `ciu.instance` **and** `ciu.owner` labels equal this instance's; a resource carrying this instance's id but another owner belongs to another checkout that collided on the 24-bit id, and is refused naming both `ciu.checkout` paths (`[S4.5] container dstdns-98535c-vault is owned by /home/vb/other/dstdns, not this checkout`). `ciu instance init` in a checkout whose derived id matches existing resources **adopts** them when the generated file is present and its `owner_id` equals their `ciu.owner` (an ordinary re-init), or when the generated file is absent and their `ciu.checkout` equals this checkout's *proven* physical path (S4.1.4) — the S2.3.4 recovery in place, where the daemon itself witnesses that this is the same tree and the token is read back from the labels into a new generated file; it **refuses** otherwise, and no flag overrides that refusal (round-2 review T2-08; round-3 T3-05). Labels are never rewritten (S4.1.2). The token defends against accidents — a colliding id, a stale clone at the same id — not against a user who already holds daemon access, who can do anything the daemon allows regardless of labels.

---

## S5 Services, realizations, the derived contract

### S5.1 Separation
S5.1.1 A consumer declares needs as bindings to LogicalServices (`[service.*]`) and providers as Realizations (`[realization.*]`); the only link is `realized_by` on a RealnessVariant. A stack file never names a LogicalService it *realizes* and never names a Realization (I7); it names LogicalServices only as binding targets.

### S5.2 `[service.<n>]`
Keys: `description` (string, optional); `verify` (list of TypedFacts, optional; probed at the end of `up`, S5.3.6); one entry per level (at least one): `live`, `seeded`, `simulated` — either a string naming a Realization (`live = "db_core"`) or a table `{ realized_by = "<r>", service = "<svc>", unchecked = <bool> }` where `service` names a service key of that `ciu_stack` Realization — the **variant service**: its health stands for the capability, its endpoints are the capability's endpoints, it is the exec target and image source (default = the Realization's primary service, S8.6) — and `unchecked` (default false; permitted on `seeded` and `simulated` variants only) is the explicit acknowledgement that the variant is accepted on health alone when its derived contract is empty (S5.3.6); `mock` is an empty table (`mock = {}`) and MUST NOT carry `realized_by` (S9.1). S5.2.1 A level outside S1.4's vocabulary is an ERROR. S5.2.2 A `realized_by` that does not resolve is an ERROR (`[S5.2] service 'main_db' level 'live' realized_by 'db_cor' is not a realization`); a `service` that is not a service of that Realization is an ERROR. S5.2.3 The same Realization MAY back several LogicalServices and several levels. S5.2.4 There is no `contract` key: the contract is derived (S5.3).

### S5.3 The derived contract and conformance
S5.3.1 The **contract** of LogicalService `X` is derived from consumption across the deploy set (every RealizedService and every gate environment in the selected bundles and lanes): `endpoints(X)` = the set of endpoint names `e` for which some binding declares `to = "X.e"`; `facts(X)` = the union of `facts` lists of bindings whose `to` names `X` (S6.4). S5.3.2 For every declared variant `service.X.<level> → R` (levels other than `mock`, whether selected or not), `R` MUST provide the contract: every `e ∈ endpoints(X)` is an endpoint of `R` (for a `ciu_stack`, of any of its services — names are unique per stack, S6.3.1); every `f ∈ facts(X)` is in `provides(R)` — for a `ciu_stack` the union of the declared `provides` of its services (S6.2), the `provides` of its hooks (S6.10), and the **derived** facts `vault:secret/<path>` for every secret with `from = "generate", store = "vault"` in `R` (S10.1.2); for `external` and `joined`, `provides` on the Realization (S5.4). A missing endpoint or fact is an ERROR naming the variant (`[S5.3] service 'main_db' variant 'seeded' (db_core_seeded) does not provide endpoint 'sql' bound by controller.database`). S5.3.3 A fact or endpoint some Realization provides that no binding consumes is INFO, never a finding (unlike draft.2's "unclaimed fact" WARN — the provider's list is not a contract). S5.3.4 An empty contract means: depending on `X` is depending on the variant service's health (S8.6). S5.3.5 A binding to `X` selected at `mock` yields no edge and no resolution; if its `delivery` is not `none` or it lists `facts`, it is an ERROR at check time naming the layout and the consumer (`[S9.3] 'payment_api' is mocked in layout local; binding controller.payments has delivery env`) — a mock never silently leaves a variable unset. S5.3.6 A LogicalService **selected** at `seeded` or `simulated` whose derived contract in the selected bundles is empty would be accepted on the variant service's health alone — which proves nothing about the prepared data or the simulated protocol. That selection is therefore an ERROR at check time (`[S5.3] main_db is selected at seeded with an empty contract; declare verify = [...] on the service or unchecked = true on the variant`) unless the LogicalService declares a non-empty `verify = [<TypedFacts>]` (S5.2; probed in the selected variant's provider container at the end of `up`, S8.5.5, for every level, so that a prepared or simulated variant is checked for behaviour even without consumers) or the variant table declares `unchecked = true` — an acceptance choice the author makes explicitly, never a WARN that a mutating verb ignores (S15.2). A `live` variant with an empty contract needs neither: it is the real thing, and its health is exactly what a consumer would depend on.

### S5.4 `[realization.<n>]`
Common key: `kind` ∈ `ciu_stack | external | joined` (required). Per kind:
- `ciu_stack`: `location` (a directory under the checkout root, relative to it, containing `ciu.stack.toml` and `ciu.compose.yml.j2`, required — never outside the checkout and never shared by two projects: the directory holds the stack's rendered artifacts (S2.2) and is its lock key (S16.5.3), so sharing it would alias two instances; what a monorepo shares is an image build, through `build.context` (S6.2)); `per_host` (bool, default false; S7.6.5).
- `external`: `provides` (list of TypedFacts, default `[]`; assertions, recorded as such); `probe` (list of `http:<path>` TypedFacts, default `[]`; probed by `ciu up` against the Realization's first `http`/`https` endpoint before the first wave that binds it, within `project.health.gate_timeout`; a failed probe is an ERROR naming the fact — the checked counterpart of `provides`); `[realization.<n>.endpoints.<e>]`: `url` (URL, required; scheme default ports apply when the URL carries none), `tls` (`none|tls|mtls`, default from the scheme), `ca` (path, optional).
- `joined`: `instance` (an instance label per S14.7.2, or an absolute path), `service` (LogicalService name in the reference instance), `provides` (optional; default = the reference's `provides` for that service's selected Realization, read at check/up time).
S5.4.1 A key from another kind's set is an ERROR (`[S5.4] 'location' is not valid for kind 'external'`). S5.4.2 A `ciu_stack` whose `location` lacks either required file is an ERROR. S5.4.3 Two Realizations MUST NOT share a `location`. S5.4.4 Plain compose files without a stack file are not a Realization kind: wrap them in a `ciu_stack` (a stack file with one service per compose service).

### S5.5 Deploy set
S5.5.1 The deploy set of an instance is the set of Realizations reached from the selected bundles: bundles → LogicalServices → selected variant (S9.3) → Realization. `external` and `joined` Realizations are in the deploy set but are not brought up (S8.7). S5.5.2 A Realization no selected variant reaches is not deployed and not rendered; a `ciu_stack` in that state is still loaded and schema-checked (I2). S5.5.3 A Realization all of whose services are disabled (S6.7) leaves the deploy set; a LogicalService whose selected Realization left it is an ERROR only when some binding names it. S5.5.4 A binding target `X` that is in no selected bundle is an ERROR naming the bundles that contain it (`[S5.5] controller.tracing binds 'tracing', selected by no bundle (bundle 'observability' has it)`).

### S5.6 TypedFacts
| kind | selector | probe (live) |
|---|---|---|
| `pg:db/<name>` | database name | `SELECT 1 FROM pg_database WHERE datname=…` inside the provider container as `probe_user` |
| `pg:role/<name>` | role | `pg_roles` |
| `pg:schema/<name>` | schema in `registry.postgresql.database` | `information_schema.schemata` |
| `minio:user/<name>` | IAM user | `mc admin user info` inside the provider |
| `minio:bucket/<name>` | bucket | `mc ls` |
| `vault:secret/<path>[#field]` | KV path | read via CIU's Vault client at the resolution of CIU's own binding to the `vault` LogicalService (S10.3); `#field` is ignored for minter matching (S8.3) |
| `http:<path>` | GET `<path>` on the provider's first `http`/`https` endpoint returns 2xx | HTTP |
| `pki:issuer/<network>` | the provider issues certificates for network `<network>` (S10.5) | Vault read of `pki/<network>/ca` |
S5.6.1 The **provider container** of a fact is the RealizedService whose `provides` lists it, whose vault-stored generated secret derives it, or whose hook entry's `provides` lists it (S6.10); for facts of an empty-listing provider it is the variant service. CIU MUST NOT assume any service key. S5.6.2 `probe_user` (S6.2) names the database superuser used by `pg:*` probes; absent when a `pg:*` fact is probed → ERROR. S5.6.3 A probe result MUST distinguish `absent` (fact not present), `unreachable` (container not running / exec failed), and `starting` (container exists, not healthy), and report which.

---

## S6 Stack file — `ciu.stack.toml`

### S6.1 Root
S6.1.1 A stack file's service tables live under `[ciu_stack.<svc>]`; the same root is bound in the stack's compose render (`{{ ciu_stack.<svc>.… }}`). S6.1.2 Any other top-level table is either reserved (`hooks`, `governance`) or consumer data (S3.5.5). S6.1.3 A stack file MUST NOT contain `stack_name`, `name` or `location` keys at service level (the Realization name is bound by the registry; a service key equal to the Realization name is the S4.2.2 pattern and is fine). S6.1.4 A stack file is plain TOML; a `{{` sequence anywhere in a string value is a WARN naming the value (a migrated template expression that will not be rendered).

### S6.2 Service keys (closed set; consumer scalars are NOT admitted here — put them in a sub-table)
| key | type | default | meaning |
|---|---|---|---|
| `image` | string `name[:tag][@digest]` | required | the one image declaration; whether the reference is project-built or pulled is decided once per reference in the image map (S17.6.1), never per service |
| `build` | table `{ context = "<dir relative to the stack>", dockerfile = "<path relative to context>" (default `Dockerfile`), args = { … }, target = "…" }` | — | the service's image is **project-built**: `ciu build` builds it from `context`, tags it as `image`, stamps provenance (S17.6); CIU injects the compose `build:` stanza (S11.4) so `docker compose build` agrees; ownership is declared next to the image, never inferred from its name. `context` MAY lie outside the stack directory and outside the checkout root, but MUST stay inside the containing worktree (the git top level, or the checkout root for a non-git checkout — the same boundary as `[ciu] inherit`, S3.1.5; `[S6.2] build context ../../../x leaves the worktree /workspaces/vbpub`), canonicalized at load — the monorepo pattern: a child project's two-file stack builds from the monorepo's shared Dockerfile directory. Build contexts never travel: images do (S17.3.6). Several services of the deploy set MAY name the same `image` reference; exactly one of them declares `build` and the others share the built image (S3.4.3); two `build` tables for one reference that differ are an ERROR naming both services |
| `instances` | integer ≥ 1 | 1 | replica fan-out (S4.2.1) |
| `one_shot` | bool | false | runs to completion; gates wait for exit 0 |
| `primary` | bool | true iff the stack has one service | S8.6 |
| `enabled` | bool or `project.control` flag name | true | conditional inclusion (S6.7) |
| `requires` | list of LogicalService names | `[]` | sugar for bindings without data (S6.4.4) |
| `binds.<local>` | table | — | S6.4 |
| `provides` | list of TypedFacts | `[]` | facts this service brings into existence by means other than a vault-stored generated secret (S5.3.2) |
| `depends_on` | list of sibling service keys | `[]` | S8.2, S11.4 |
| `probe_user` | string | — | S5.6.2 |
| `aliases` | list of DNS labels | `[]` | extra aliases on the instance network (S11.4) |
| `host_network` | bool | false | `network_mode: host`; no hostname/network injection (S11.4) |
| `endpoints.<e>` | table | — | S6.3 |
| `health` | table | merged from `project.health` | S6.6 |
| `hostdir.<purpose>` | string or table | — | S6.8 |
| `configfile.<name>` | table | — | S6.9 |
| `secrets.<key>` | table | — | S10.2 |
| `<sub-table>` | table | — | consumer data (any name not reserved by S1.4) |
S6.2.1 A scalar key not in this table is an ERROR (`[S6.2] unknown key 'database' on [ciu_stack.postgres]; consumer data goes in a sub-table`). S6.2.2 A consumer sub-table named `identity`, `health`, `endpoints`, `binds`, `hostdir`, `configfile` or `secrets` is an ERROR. S6.2.3 The keys `init_requires`, `init_provides`, `uses`, `after` are refused with a message naming their v8 form (Appendix A).

### S6.3 Endpoints — `[ciu_stack.<svc>.endpoints.<e>]`
`port` (integer 1..65535, required), `protocol` (`tcp|udp|http|https`, default `tcp`), `publish` (`instance|host|proxy`, default `instance`; S7.4), `host_port` (integer, default `port`; the host port used whenever the endpoint is published), `host_bind` (IP address; only with `publish = "host"`, default `0.0.0.0`), `listen` (IP address — `0.0.0.0`, `::`, `127.0.0.1` or a host address; **required** on every endpoint of a `host_network = true` service, where it states the address the process itself binds, and refused elsewhere — `[S6.3] listen is only for host_network services`), `allow_from` (list of `network.<n>` | `host.<h>`, optional), `path` (URL path, optional; required for `publish = "proxy"`; used by proxy resolutions and `http:` facts). S6.3.1 Endpoint names MUST be unique across all services of one stack (bindings target `<service>.<endpoint>`, S6.4). S6.3.2 Publications are compared as **socket claims** `(host, protocol, bind address, port)` (S7.4.6). Bind addresses are canonicalized to address sets before comparison: `0.0.0.0` is every IPv4 address of the host; `::` is every IPv6 address **and**, conservatively, every IPv4 address (a Linux dual-stack socket accepts mapped IPv4 unless `bindv6only` is set, which CIU does not inspect); a concrete address is itself. Two claims of one host collide when protocol and port are equal and their address sets intersect — two declared networks that resolve to the same host address therefore collide, and a wildcard collides with everything of its family; `tcp` and `udp` on one port never collide. There is no symbolic wildcard: a `host_network` service's claim carries its declared `listen` address (S7.4.7). Claims of one layout on one host MUST be collision-free (S7.4.5); before `up`, a claim already held by another registered instance on the same machine (its `ciu.instance.json` records its claims while it is up, S14.7.1) or already bound on the daemon/host (a bind probe) is an ERROR (`[S6.3] tcp 0.0.0.0:8080 in use by instance a1b2c3`); claims are recorded in the instance record under the instance lock before containers start and released after `down`/`clean` confirmed them stopped, so two concurrent starts cannot both see a port as free. The instance file MAY override host ports per endpoint (S14.2). S6.3.3 `allow_from` targets MUST resolve.

### S6.4 Bindings — `[ciu_stack.<svc>.binds.<local>]`
A binding is the one way a consumer depends on a LogicalService. Keys:
| key | type | default | meaning |
|---|---|---|---|
| `to` | `target` (S1.4) | required | `"<service>"` (no data: ordering and facts only) or `"<service>.<endpoint>"` (data: the endpoint is resolved and delivered) |
| `wait` | `healthy \| started \| none` | `healthy` | `healthy`: an ordering edge; the provider's variant service (and the endpoint's owning service) must be healthy — or completed for `one_shot` — before this service's wave; `started`: an edge, the provider's containers must be running; `none`: no edge, a resolution only (a runtime-only dependency: a tracing collector, a proxy's backends) |
| `delivery` | `env \| template \| none` | required when `to` names an endpoint; MUST be absent or `none` otherwise | `env`: CIU injects `<env_prefix>_HOST`, `<env_prefix>_PORT`, `<env_prefix>_URL` (when the resolution has a `url`), `<env_prefix>_PATH` (when it has a `path`), `<env_prefix>_TLS_CERT/_KEY/_CA` (when it has TLS paths) into the service's compose `environment:` (S11.4) and into a gate lane's process (S16.4); `template`: the resolution is bound as `ciu_stack.<svc>.binds.<local>` in the stack's templates (S3.5.1); `none`: nothing is delivered (the edge and facts still apply) |
| `env_prefix` | `envname` | required for `env` | the variable prefix |
| `facts` | list of TypedFacts | `[]` | facts this consumer relies on from the target; they enter the target's contract (S5.3) and are probed before this service's wave (S8.5.2) |
| `enabled` | bool or `project.control` flag name | true | a disabled binding derives nothing |
| `probe` | `auto \| none` | `auto` | permitted only on a binding whose target endpoint is `udp` on a `host_network` provider: `none` acknowledges that the gate cannot prove that listener reachable by connecting (S8.5.2a) and accepts it unprobed; anywhere else the key is an ERROR |
S6.4.1 `to` MUST name a LogicalService of the deploy set (S5.5.4) and, when an endpoint is named, that endpoint MUST exist on the selected variant (S5.3.2 covers every variant statically). S6.4.2 A service MUST NOT bind a LogicalService its own Realization realizes (self-binding is an ERROR). S6.4.3 Two bindings of one service MAY target the same LogicalService (different endpoints or different deliveries). S6.4.4 `requires = ["x", "y"]` is exactly equivalent to `binds.x = { to = "x" }`, `binds.y = { to = "y" }` (local name = target name, `wait = "healthy"`, no data); declaring both a `requires` entry and a `binds` entry with the same local name is an ERROR. The rendered file shows the expanded form (I3). S6.4.5 `env` delivery requires that the resolution can be formed at render time for the rendered host (S7.8); a resolution that depends on a joined instance's live state is read at render time under the reference's shared lock (S9.5.4). S6.4.6 The variables an `env` binding injects MUST NOT collide with any `env_name` of an `env`-delivered secret or with another binding's variables of the same service (`[S6.4] DATABASE_HOST injected twice`). S6.4.7 `env` delivery injects into the declaring service's compose block only; services of one stack that share an environment block (a compose anchor) or that name the variables differently use `template` delivery and write the variables themselves.

### S6.5 Replicas
S6.5.1 With `instances = N`, CIU renders one compose service per replica from ONE template block iterated by the template over `ciu_stack.<svc>.identity.replicas` (S11.3), renders each config file once per replica (S6.9.3), and derives per-replica identities (S4.2.1). S6.5.2 Health, endpoints and bindings are per service; resolutions to a replicated provider use the service-level `compose_key` (S7.8.6); a replicated service is healthy when every replica is (S8.6.3).

### S6.6 Health
S6.6.1 `[ciu_stack.<svc>.health]` keys: `interval`, `timeout`, `start_period` (durations), `retries` (integer), `gate_timeout` (duration). S6.6.2 CIU MUST merge `project.health` into every service's `health` table before any render, so `ciu_stack.<svc>.health.<k>` always resolves. S6.6.3 `gate_timeout` default = `start_period + interval × retries + 30s`. S6.6.4 The compose `healthcheck.test` is authored in the template; CIU injects `interval`, `timeout`, `retries` and `start_period` from the merged `health` table (S11.4) and refuses a template value that differs from them (`[S6.6] healthcheck.interval 30s differs from health.interval 10s`). CIU reads container health from Docker (S8.6.3).

### S6.7 `enabled`
S6.7.1 A bool, or the name of a `[project.control]` flag (the flag's value applies). Expressions are not permitted. A disabled service is omitted from the graph, and CIU REMOVES its block from the rendered compose after the template renders (templates need not guard it, S11.4); its tables stay bound in the render context with `enabled` resolved to `false`, so a template MAY guard shared blocks (`{% if ciu_stack.tls_proxy.enabled %}`) and a config file of a disabled service is not rendered; a `depends_on` naming a disabled sibling is dropped with a WARN; a binding whose target's variant service is disabled is an ERROR at check time; a Realization all of whose services are disabled leaves the deploy set (S5.5.3). S6.7.2 `enabled` on a secret (S10.2), a binding (S6.4), a lane or an environment (S16) has the same form.

### S6.8 Host directories — `[ciu_stack.<svc>.hostdir]`
`<purpose> = ""` (CIU-generated path `<state root>/ciu-data/<R>/<svc>/<purpose>` — the state root's physical path, S2.6, so that data survives a release switch), or `<purpose> = "/abs/path"`, or `<purpose> = { path = "…", uid = 1000, mode = "0750", seed = "relative/dir" }`. S6.8.1 CIU creates missing directories before compose runs and applies ownership from `uid` (default `instance.user_uid`) and mode through its privileged helper. S6.8.2 The resolved absolute host path is what the template reads as `ciu_stack.<svc>.hostdir.<purpose>`. S6.8.3 `ciu clean --vanilla` removes generated `ciu-data/<R>` directories through the same helper; `ciu clean` keeps them. S6.8.4 A v7 `vol-<service>-<purpose>` directory under a stack whose service declares a generated hostdir of the same purpose is an ERROR at `up` (`[S6.8] legacy data directory infra/db-core/vol-postgres-data exists; run ciu migrate --hostdirs or pin hostdir.data to it`); `ciu migrate --hostdirs` moves it into `ciu-data/` (same filesystem: rename; otherwise copy and verify) and never deletes the source until the copy is verified. S6.8.5 `seed` is an idempotent transaction: the source is a directory relative to the stack (checked into git), copied — without following symlinks, ownership and mode mapped per `uid`/`mode` — into a temporary sibling of the target, then renamed into place **only when the target is absent or empty**; a non-empty target is left untouched and the seed's SHA-256 (recorded in `ciu.state.toml` under `hostdir.<purpose>.seed_digest`) is compared: a changed seed against a non-empty target is a WARN, never an overwrite.

### S6.9 Config files — `[ciu_stack.<svc>.configfile.<name>]`
`template` (path relative to the stack directory, required), `target` (absolute in-container path, required), `mode` (default `"0440"`), `schema` (path to a JSON Schema, optional). S6.9.1 CIU renders `template` with the compose render context (S3.5.1) to `<location>/ciu.rendered/<svc>/<target path with its leading slash removed>` and injects a read-only bind mount of the **parent directory** `<location>/ciu.rendered/<svc>/<dirname of target>` to `<dirname of target>` in the container (absolute physical paths, S11.4). Mounting by directory means a missing rendered file can never be turned into an empty directory by the daemon (the v7 S5.3a hazard). S6.9.2 Two config files of one service whose targets share a parent directory are rendered into the same mounted directory; within **one service** a config-file mount directory that is a parent or child of another of its mounts or of one of its hostdirs is an ERROR (mount namespaces are per container, so the same target in two services is not a collision). A target whose parent directory is a symlink in the image is shadowed by the mount like any other path; CIU does not inspect images. Because the mounted directory hides whatever the image ships there, a config file for a program whose directory the image populates (`/etc/nginx/mime.types`) targets a subdirectory (`/etc/nginx/ciu/nginx.conf`) and the template points the program at it (`command: nginx -c …`); `ciu check` WARNs when a target's parent is a well-known image-populated directory (`/etc/nginx`, `/etc/postgresql`, `/usr/share/nginx/html`). S6.9.3 With `schema`, the rendered file MUST validate against it (ERROR otherwise). S6.9.4 Replicated services get `ciu.rendered/<svc>-<k>/…` per replica, rendered with `replica` bound to the replica's identity row.

### S6.10 Reserved tables and state
- `[hooks]`: a table whose closed key set is the three phase lists `pre_secrets`, `pre_compose`, `post_compose` and `env_allow` (list of `envname`, default `[]`: the stack's allow-list of variables forwarded from CIU's environment into every hook, S12.1). Each phase list's entries are either a script path (string, equivalent to `{ run = "<path>" }`) or a table with exactly these keys: `run` (path, required), `service` (a service key of this stack, optional; default = the primary service; the service in whose container the entry's facts are probed and which `hookkit.docker_exec` targets by default), `provides` (list of TypedFacts the script creates, default `[]`), `secrets` (list of this stack's secret keys the script may read, default `[]`, S12.2 — **phase-restricted**: a `pre_secrets` entry may list only keys whose source is `file`, `ask`, `host`, or `generate` with `store = "local"`, the sources CIU can materialize without Vault, containers or other hooks, and CIU materializes exactly those keys before the entry runs; any other source there is an ERROR at check time naming the phase and the source (`[S6.10] pre_secrets entry fetch_token.py lists 'admin_token' (from = vault); Vault values do not exist before step 3`); `pre_compose` and `post_compose` entries may list any key step 3 materialized, S8.7), `inputs` (list of repo-relative paths **outside** the stack directory that the script reads — fixture files, shared scripts — so that a release's closure carries them, S17.3.1; default `[]`; a path under the stack directory is redundant and a WARN). Any other key is an ERROR (S3.8.1). `service` MUST name an enabled service of the stack (`[S6.10] hook entry names service 'minoi'; the stack has minio`).
- `[governance]`: per-stack override of the global governance base (S13); shallow merge.
- `[ciu_stack.secrets.<key>]`: stack-level secrets (S10.2).
- State: hook-persisted non-secret data lives in `<location>/ciu.state.toml` in a checkout and in `<state root>/stacks/<R>/ciu.state.toml` in a release (S2.6; written by CIU from hook outputs, S12.3), is bound as `state` in the stack's render contexts and hook contexts, and is preserved across renders, release switches and `ciu clean`; `--vanilla` removes it. A key whose name matches S2.4.1's sensitive names is an ERROR (`[S6.10] secret-shaped key in state; use the secrets output`). A `[state]` table in a stack file is an ERROR.

---

## S7 Topology

### S7.1 Model
Distance is never declared on a consumer or a provider. It is derived from **placement** (S7.6) × **networks** (S7.3) × the provider's **endpoint** (S6.3) × the provider's **kind** (S5.4). The only declarations that carry distance are an endpoint's `publish`, `host_port` and `allow_from`.

### S7.2 Hosts — `[hosts.<h>]` (in `ciu.hosts.toml`; S17.1 for lookup order)
Keys: `local` (bool; the machine running ciu; no SSH keys required), `fqdn` (`dns_name`; the host's public name, optional — required for the rendered host when `ciu.require_fqdn = true`), `ssh_host`, `ssh_user` (default `root`), `ssh_port` (default 22), `ssh_key` (path), `known_host` (string, required unless `local` or `CIU_SSH_INSECURE_TOFU=1`), `bundle_dir` (default `/opt/ciu`; the directory that holds `releases/`, `candidate`, `current` and `previous`, S17.3–S17.4 — never itself the `current` symlink), `push_mode` (`auto|rsync|scp`), `bundle_excludes` (list, default `[".git"]`), `docker_optional` (bool; `activate health` skips Docker checks on this host, and no images travel to it, S17.3.6), `[activate] bootstrap|apply|health` (command strings run inside a release directory, S17.4.1; there is no `rollback` command — rollback is CIU's own state machine, and a host cannot redefine it), `[secrets.<entry>]` (host-scoped secrets: tables with `from ∈ ask | generate | file` and the keys of S10.1; stored under `[secrets.hosts.<h>]`, S10.4), `[addresses.<network>]` (string address per address-plane Network the host sits on). S7.2.1 Exactly one host MAY be `local = true`. S7.2.2 Every host named by a layout MUST exist. S7.2.3 There is no built-in host; `ciu init` writes `[hosts.localhost] local = true` (S19).

### S7.3 Networks — `[network.<n>]`
Keys: `kind` (`address | proxy`, required), `realized_by` (**LogicalService** name whose selected variant provides the transport or the proxy — `mesh_node`, `reverse_proxy`; optional for `address`, required for `proxy`), `tls` (`none|tls|mtls`, default `none`), `pki` (LogicalService name; required when `tls ≠ none`), `fqdn` (`dns_name`; required for `proxy`), `description` (optional; CIU attaches no semantics to the wording). S7.3.1 The network `instance` exists implicitly for every instance (kind `address`, name per S4.2.1) and MUST NOT be declared. S7.3.2 A network with `realized_by` is **ready** on a host only when the **variant service** of `realized_by`'s selected Realization (S5.2, S9.3) is healthy on that host (S8.3); naming a LogicalService rather than a Realization makes that service unique even when one Realization backs several capabilities. A `realized_by` whose selection is `mock` is an ERROR. S7.3.3 A `proxy`-kind network is **address-free**: hosts carry no address on it; it is selectable in `reach` by any host, and resolving through it yields the network's `fqdn` (S7.8). S7.3.4 `tls ≠ none` requires `pki`, whose provider MUST provide `pki:issuer/<n>` (S10.5).

### S7.4 Endpoint publication
S7.4.1 `publish = "instance"` (default): the endpoint is reachable on the instance network, and — **derived from the layout** — additionally published on the provider host, bound to the provider host's address on network `N`, for every network `N` over which some cross-host **binding resolution with data** (a binding whose `to` names this endpoint, S7.8 step 5) reaches it: `ports: ["<addresses[host(R)][N]>:<host_port>:<port>/<tcp|udp>"]` (`http`/`https` map to `tcp`). A binding without an endpoint never publishes anything. On a single-host layout nothing is published. S7.4.2 `publish = "host"`: always published as `["<host_bind>:<host_port>:<port>/<tcp|udp>"]` in every layout. S7.4.3 `publish = "proxy"`: consumers reach the endpoint through a `proxy`-kind network in their `reach`; the **proxy itself** (the network's `realized_by`) reaches the endpoint by S7.8 rule 4 when placed on the same host, otherwise by S7.4.1's derived publication. What the proxy does with a resolution (TLS termination, authentication guards, rate limits) is the proxy stack's own configuration, rendered by its templates from its own `binds.*`; CIU carries no proxy policy. S7.4.4 `allow_from` is **declared admission data, not enforcement**: CIU resolves each entry to the set of addresses (all addresses of the named network's hosts, or the named host's addresses), binds it as `ciu_stack.<svc>.endpoints.<e>.allow_from_resolved` (list of strings) for the stack's own templates, and uses it in S7.8 step 5 only to decide which consumer hosts a resolution admits. CIU does not program firewalls; the stack's templates must consume the list (an nginx `allow` block, an application allow-list). `ciu check` stage 7 WARNs when a stack declares `allow_from` and none of its templates references `allow_from_resolved` (`[S7.4] controller.http declares allow_from but no template consumes allow_from_resolved — not enforced`), so a declared restriction can never look enforced when it is not. S7.4.5 The socket claims published on one host of one layout MUST be collision-free per S6.3.2. S7.4.6 Every publication of the rendered host — declared (`publish = "host"`) or derived (S7.4.1, S7.4.3) — is written as a **socket claim** `{ scope = "host" | "network", network = "<n>" (scope network), bind = "<address>", port = <host_port>, protocol = "tcp" | "udp" }` into `[resolved.identities.<R>.<svc>.endpoints.<e>] publications = [ … ]` (S4.4) and listed by `ciu check --layout L` as the publication table (S15.4). Claims are the unit of the collision check (S7.4.5, S6.3.2). S7.4.7 An endpoint of a `host_network = true` service is never published by CIU (the process listens on the host's interfaces itself): `publish`, `host_port` and `host_bind` are refused on it (`[S7.4] host_network service 'tailscale' cannot declare host_port`) and `listen` is required (S6.3); its socket claim is `{ scope = "host", bind = <listen>, port = <port>, protocol }` — canonicalized by S6.3.2 like every other claim, so a wildcard listener collides with every published port of its family — and consumers resolve it per S7.8 step 4a.

### S7.5 Bundles — `[bundles.<b>]`
`services` (list of LogicalService names; MAY be empty), `includes` (list of bundle names whose effective service sets are added; expanded recursively to any depth; a cycle is an ERROR naming it), `compose_profiles` (list, optional), `compose_env.<VAR>` (strings, optional). S7.5.1 The effective service set of a bundle is `services` ∪ the effective sets of every bundle in `includes`, computed transitively. S7.5.2 Conflicting `compose_env` values across selected bundles are an ERROR.

### S7.6 Layouts — `[layouts.<l>]`
`environment` (free-form string, optional; bound as `instance.environment`; CIU attaches no semantics), `description` (optional), `hosts.<h>` (table per host, at least one): `bundles` (list of bundle names; MAY be empty only in a zero-instance project), `reach` (list of network names, non-empty; `instance` allowed and means "this host only"). S7.6.1 A layout is always explicit; an instance with no `layout` selected (S14.2) is an ERROR; when a project declares exactly one layout, `ciu instance init` selects it and says so. There is no built-in layout; `ciu init` writes `[layouts.local]` (S19). S7.6.2 Host declaration order is the push order (S17.2). S7.6.3 A Realization that is not `per_host` and is reached from the bundles of two hosts of one layout is an ERROR (`[S7.6] realization 'db_core' placed on both 'gstammtisch' and 'rs1002'`). S7.6.4 Every network in `reach` other than `instance` and `proxy`-kind networks MUST be one the host has an address on. S7.6.5 A `per_host = true` Realization (a transport daemon, a node exporter) MAY be reached from the bundles of several hosts of one layout and is deployed on each of them; no binding with data may target its capability (`[S7.6] per_host realization cannot be a binding target with an endpoint`); an endpoint-less binding (`requires`) to its capability targets the copy placed on the **consumer's host**, and is an ERROR when that host's bundles do not place it (`[S7.6] controller@rs1002 requires mesh_node, but rs1002 places no tailscale_node`); network readiness (S7.3.2) and network/pki edges (S8.3) are evaluated per host against that host's copy.

### S7.7 Placement (derived)
`[resolved.placement.<R>] hosts = ["<h>", …]` for every `ciu_stack` Realization in the deploy set (one element unless `per_host`); `external` and `joined` Realizations have no host.

### S7.8 Binding resolution (derived) — `resolve(C, b)` for consumer `C` (a Realization, a gate environment, or the pseudo-consumer `ciu`) and binding `b` with `to = X[.e]`
1. Resolve `X` to its selected Realization `R` (S9.3) — `mock` → no resolution (S5.3.5). Without an endpoint the resolution carries `service`, `realization` and `requires` only (step 6) and stops here. With `e`: for a `ciu_stack`, `e` is an endpoint of the service of `R` that declares it (names are unique per stack, S6.3.1); for `external`, an endpoint of the Realization. (A target whose Realization is `per_host` is an ERROR, S7.6.5.)
2. `R.kind == joined`: read the reference instance's rendered `[resolved.identities.<R'>.<svc>]` for the service that declares `e` (S4.4); `network = <reference instance network name>`, `host = <that identity's container_name>` (or service `compose_key` when replicated), `port = e.port`, `requires = []`. The consumer's containers are attached to the reference network (S11.4).
3. `R.kind == external`: `url` as declared; `host`/`port`/`tls` parsed from it; `requires = []`.
4. `host(C) == host(R)` and the providing service is not `host_network`: `network = "instance"`, `host = identity(R, svc).container_name` (or service `compose_key` for replicated services), `port = e.port`. 4a. `host(C) == host(R)` and the providing service has `host_network = true`: it listens on the host's own interfaces at the endpoint's declared `listen` address (S6.3), not on the instance network. A consumer on the instance network gets `network = "host"`, `host = "ciu-host"` (CIU injects `extra_hosts: ["ciu-host:host-gateway"]` into the consumer's compose block, S11.4), `port = e.port` — admitted only when `listen` is a wildcard (`0.0.0.0`, `::`), because a loopback- or address-bound process is unreachable through the bridge gateway (`[S7.8] tailscale.api listens on 127.0.0.1; controller cannot reach it through ciu-host`); a consumer that is itself `host_network` gets `host = "127.0.0.1"`, admitted when `listen` is a wildcard or loopback. Cross-host consumers of a `host_network` provider continue with step 5 using `port = e.port`, and step 5 admits an `address` network `N` only when `listen` is a wildcard or equals `addresses[host(R)][N]` (nothing is published: the process already listens on the host's addresses; `host_port` and `host_bind` are refused on such an endpoint, S7.4.7). A declared `listen` is a claim about the process, not a proof: before a dependent's wave the gate probes the resolved address live (S8.5.2a).
5. Otherwise iterate `reach(host(C))` in order and pick the first network `N` that admits the pair: a `proxy`-kind `N` admits when `e.publish == "proxy"` and `C` is not `N.realized_by`; an `address`-kind `N` admits when both hosts have an address on `N` and `e.allow_from` (if set) admits `host(C)` on `N`. If `N` is `proxy`-kind: `host = N.fqdn`, `port` = the `host_port` of `N.realized_by`'s variant service's `publish = "host"` endpoint of protocol `https` (ERROR if it has none), `path = e.path`, `url = "https://<fqdn><path>"`, `requires += [N.realized_by]`. Else: `host = addresses[host(R)][N]`, `port = e.host_port`, and the endpoint is published on `N` (S7.4.1) — for a `publish = "host"` endpoint no extra publication is derived, so its `host_bind` MUST be `0.0.0.0` or `addresses[host(R)][N]`, else ERROR (`[S7.8] consul.http binds 127.0.0.1 on gstammtisch; controller@rs1002 cannot reach it over mesh`). No admitting `N` → ERROR (`[S7.8] no route from 'controller'@rs1002 to 'main_db.sql'@gstammtisch: no shared network in reach ["mesh"]`).
6. `requires` += `N.realized_by` (if any, for `address` networks) and `N.pki` (when `tls ≠ none`); `tls` from `N`; `cert`, `key`, `ca` = the `/run/secrets/tls_*` paths of the derived TLS secrets (S10.5) when `tls ≠ none`.
7. `path = e.path` is always copied when declared. For **direct** resolutions (steps 2–5 over an `address` network) `url` is emitted for `http`/`https` as `<scheme>://<host>:<port>` — the path is NOT appended, because a proxy prefix is not an application base path — and for `udp` as `udp://<host>:<port>`; for `tcp` no `url` is emitted. For **proxy** resolutions `url = "https://<fqdn><path>"` includes the path.
S7.8.1 Resolutions are written as `[resolved.bindings.<C>.<local>]` (for a stack, `<C>` is `<R>.<svc>`; for a gate environment, `env.<e>`; for CIU itself, `ciu`) with keys `service, realization, endpoint?, network?, host?, port?, url?, path?, tls?, cert?, key?, ca?, requires, delivery, variables?` (`variables` = the names an `env` delivery injects). S7.8.2 In the render of stack `C`, `ciu_stack.<svc>.binds.<local>` is bound to the resolution for every `template`-delivered binding of `<svc>`. S7.8.3 A resolution exists only for a declared binding; there is no other way for a consumer to obtain an address (I7). S7.8.4 Cross-host resolutions require the network to be **ready** (S7.3.2) before the consumer's wave starts (S8.3). S7.8.5 The resolution function is the only code path forming addresses for service-to-service reachability; CIU's own Vault client (S10.3), probes (S5.6) and the gate (S16) obtain addresses through bindings of the pseudo-consumer `ciu` or of an environment. S7.8.6 Replicated providers: `host` is the service-level `compose_key` on the instance network (compose DNS), or the host address cross-host; per-replica resolutions are not derived. S7.8.7 **CIU's own vantage point**: the pseudo-consumer `ciu` (and every `host`-mode gate environment) is placed on the host running CIU; its bindings are derived implicitly (to `vault` when any vault-sourced secret exists; to every `requires.healthy` target of a host lane); when the result is on the instance network, CIU resolves `container_name` to the container's address via `docker inspect` on a native host, or attaches its own container to the instance network when `ciu.auto_connect_network = true` and `env_type = devcontainer`.

### S7.9 Networks (derived)
`[resolved.networks.<n>] name, kind, realized_by?, fqdn?, tls?` for every declared network and the implicit `instance` network (`name` = S4.2.1).

---

## S8 Ordering: init graph, waves, gates

### S8.1 Nodes
The graph's nodes are the RealizedServices of every `ciu_stack` Realization in the deploy set. `external` and `joined` Realizations are sinks that are always satisfied (their facts are asserted by `provides`, optionally probed).

### S8.2 Declared edges
- **bind** — for every binding `b` of service `s` with `wait ≠ none` and target `X[.e]`: an edge from `s` to the variant service of `X`'s selected Realization, to the service that declares `e` (when named), and to every RealizedService of that Realization whose provides intersect `b.facts`. `wait = "started"` edges are satisfied by `Running`; `wait = "healthy"` edges by S8.6.3. `requires` entries are bindings (S6.4.4).
- **depends** — `depends_on` siblings; also rendered into compose (S11.4).
- `wait = "none"` bindings derive resolutions only, never edges.

### S8.3 Derived edges (never declared; always listed in `edges`)
- **facts** — every fact in a service's `provides`, in a hook entry's `provides`, and every `vault:secret/<path>` derived from a `from = "generate", store = "vault"` secret is attributed to its service — for a stack-level secret (`[ciu_stack.secrets.<key>]`) and for a hook entry without `service`, to the stack's primary service; S5.3 conformance and minter resolution use the union.
- **secret→vault** — from every service that declares a secret with `from = "vault"` or `store = "vault"` to the variant service of the Realization selected for the `vault` pointer (S10.3). Exempt: services of that Realization itself.
- **secret→minter** — from every service with a `from = "vault"` secret at path `P` to the service in the deploy set whose facts contain `vault:secret/P` (field ignored); a `pki/<N>/…` path (S10.5) is satisfied by the provider of `pki:issuer/<N>`. No minter and no `external`/`joined` Realization providing it → ERROR (`[S8.3] nobody mints vault:secret/db/postgres/controller_password`).
- **network** — for every resolution of a consumer Realization `C` over an `address` network `N` with `realized_by = T`: from every service of `C` to `T`'s variant service on `host(C)`, and — when `T` is `per_host` — also on `host(R)`.
- **pki** — from every service with a resolution over a `tls ≠ none` network, and from every service with an endpoint reached over such a network, to the variant service of the `pki` LogicalService's Realization.

### S8.4 Waves
S8.4.1 The **Realization graph** is built as follows: every edge of S8.2–S8.3 is directed **consumer → provider**; an edge is lifted to the Realizations of its two services; an edge whose two ends lie in the same Realization is discarded (`depends_on` and same-stack bindings order containers, not waves); a `wait = "none"` binding contributes no bind edge but its resolution still contributes the network and pki edges of S8.3 when they apply. `wave(R) = 0` when `R` has no outgoing edge to another Realization, else `1 + max(wave(provider))` over its providers. The example in `v8-dstdns-demo/examples/ciu.resolved.toml.example` is derived by this rule and `ciu check --graph` MUST reproduce it. A Realization is deployed as a unit. A cycle at Realization level is an ERROR naming the cycle and the remedy (`[S8.4] cycle db_core → vault → db_core through db_core.postgres_init→vault.vault and vault.vault→db_core.postgres; split one realization or turn one binding into wait = "none"`), even when the service-level graph is acyclic — deliberate: units deploy whole. S8.4.2 Wave `k` is the set of Realizations of level `k`. Within a wave, per host, stacks are brought up in name order. S8.4.3 Cross-host: wave `k` on any host starts only after every provider of an edge into wave `k` has passed its gate on its own host (S8.5); CIU running on one host waits for a remote provider by probing its resolutions (S8.5.3), not by SSH or `docker exec`. `ciu activate apply` runs hosts serially in layout order (S17.4), so under the supported flow a consumer host's first wave begins after its provider hosts completed every wave; concurrent hand-started `up` runs on several hosts rely on S8.5.3 alone.

### S8.5 Gates
S8.5.1 After bringing up wave `k`, CIU evaluates, per provider service `p` on this host, the **strongest predicate** requested by the edges into `p` from later waves: an edge from a `wait = "started"` binding requires `Running`; an edge from a `wait = "healthy"` binding, a `requires` entry, a `depends_on`, or a derived edge (S8.3) requires **healthy** (S8.6.3), which for a `one_shot` provider means **completed**; a binding's `facts` add the fact probes of S8.5.2. Each predicate must hold within `p.health.gate_timeout` (S6.6.3); timeout → ERROR naming `p`, the predicate, its last observed state, and the budget. The predicates evaluated are written as `[resolved.gates.<k>]` (S8.5.4). S8.5.2 Before starting wave `k+1`, every TypedFact required by an edge into `k+1` — the `facts` of its bindings, the `vault:secret/<path>` fact of every secret→minter edge and the `pki:issuer/<n>` fact of every pki edge (S8.3): a consumer whose Vault secret is not yet minted fails later and worse, at materialization — whose provider is on THIS host is probed (S5.6) with a bounded poll of the same budget; `starting` and `unreachable` are retried within the budget, `absent` is retried within the budget and reported as absent when it expires. S8.5.2a Every resolution a service of wave `k+1` holds to a `host_network` provider (S7.8 step 4a) is additionally probed live within the same budget **from the consumer's vantage**, because a reachability claim is true only for the namespace that will use it: for a consumer on the instance network CIU starts a throw-away probe container attached to that network with the same `extra_hosts` injection and connects to `ciu-host:<port>` from inside it; for a `host_network` consumer a host-namespace helper (a `network_mode: host` throw-away container, or CIU itself when it runs natively on the host) connects to `127.0.0.1:<port>`; for a cross-host consumer the consumer host's CIU connects to the resolved address (S8.5.3). `tcp`, `http` and `https` endpoints are probed by TCP connect; a `udp` endpoint cannot be proved by connecting and is admitted only when the consumer's binding declares `probe = "none"` (S6.4) — else ERROR at check time (`[S8.5] worker.dns binds resolver.dns (udp on a host_network service); declare probe = "none" on the binding or provide it on the instance network`). Every probe is recorded in `[resolved.gates.<k>] probes` (S8.5.4); a failed probe → ERROR naming the consumer, the endpoint, its declared `listen`, the vantage and the address. S8.5.3 For a provider on ANOTHER host CIU probes reachability (TCP connect / TLS handshake / HTTP GET per endpoint protocol against the derived resolution) and takes the provider's facts and completions **only** from that host's **receipt** (S17.4.3–S17.4.4); reachability never stands for a fact. A required fact or completion with no valid receipt entry is an ERROR (`[S8.5] pg:role/controller on gstammtisch: no receipt for this activation; pass --receipts DIR, or --allow-assumed to proceed on reachability alone`); under `ciu up --allow-assumed` it is recorded as `assumed` (S8.5.4, S17.5) and WARNed instead. `external` and `joined` providers' facts are **assertions** (`provides`), recorded as such; an `external` Realization's `probe` facts (S5.4) are probed, and a `joined` provider's facts come from the reference's own receipt. S8.5.4 The gates are written as `[resolved.gates.<k>] healthy = [...]  completed = [...]  facts = [...]  probes = [ { consumer = "<R>.<svc>", endpoint = "<R>.<svc>.<e>", protocol = "tcp" | "udp", address = "<addr>:<port>", vantage = "instance-network" | "host" | "cross-host", result = "passed" | "failed" | "unprobed" }, … ]  assumed = [ { kind = "fact" | "completion", subject = "<fact or R.svc>", provider = "<R>.<svc>", host = "<h>", reason = "no-manifest" | "no-receipt" | "invalid-receipt" | "not-in-receipt" }, … ]` — `probes` lists the S8.5.2a probes (`unprobed` only for a `probe = "none"` binding), `assumed` is non-empty only under `--allow-assumed`, and both vocabularies are part of the S3.8.4 definition. S8.5.5 **Acceptance.** `ciu up` succeeds only when every wave's gate passed and, at the end, the enabled services of every `ciu_stack` Realization brought up on this host satisfy the **partition**: every service that is not `one_shot` is **healthy** per S8.6.3 (a container with a healthcheck has reached `healthy`; one without is `Running`); every `one_shot` service has **completed** with exit 0 (it is not running, and that is correct); no service is judged by both predicates; and every `verify` fact of a selected LogicalService (S5.3.6) probed true — whether or not any consumer binds them. A selected variant service that never becomes healthy, or a one-shot that exits non-zero, fails `up` even when nothing depends on it.

### S8.6 Primary service, variant service, health
S8.6.1 A stack with more than one service MUST mark exactly one `primary = true`; a single-service stack's only service is primary. S8.6.2 The primary service is the default **variant service** (S5.2): unless a variant names another service, it is the exec target and image source (S16.4), the target of endpoint-less bindings (S8.2), the container in which hook-provided facts are probed unless the hook entry names a service (S6.10), and the service whose health stands for the capability in gate predicates. S8.6.3 **healthy**: a container whose rendered block declares a `healthcheck` is healthy when Docker reports `State.Health.Status == "healthy"`; a container without one is healthy when `State.Running` is true; a `one_shot` service is **completed** when its container exited with code 0. A replicated service is healthy when every replica is. Gate providers that are not `one_shot` MUST declare a healthcheck (S11.5).

### S8.7 Bringing up one `ciu_stack` Realization (the pipeline, in order)
For each Realization of a wave, in name order, on the host it is placed on:
1. **hostdirs** — create and chown per S6.8.
2. **`pre_secrets` hooks** (S12) — before each entry, CIU materializes the keys its `secrets` list names (only `file`, `ask`, `host` and local `generate` sources are permitted there, S6.10) and hands them to it; the entry may emit `state` and `secrets`; a `secrets` output here is the only way to satisfy a `from = "hook"` secret (S10.1) in the same run.
3. **secrets** — materialize per S10 (store under the instance lock); write temp copies (S10.7).
4. **`pre_compose` hooks** — may emit `state` and `secrets`; every `state` output is merged into `ciu.state.toml` immediately and is visible to steps 5–8 of this run.
5. **config files** — render per S6.9 with the S3.5.1 context (including step-4 state), validate schemas, scan (S2.4.2).
6. **compose** — render, inject, validate per S11; write `ciu.compose.yml`.
7. **`docker compose -p <compose_project> --project-directory <location> -f ciu.compose.yml up -d --remove-orphans`**.
8. **`post_compose` hooks** — run after every service of this Realization that any edge targets is healthy or completed (S8.5.1's budget); may emit `state`, `secrets`, `facts`.
Then, once every Realization of the wave completed step 8: the wave gate (S8.5.1 for remaining providers) and the fact probes for edges into the next wave (S8.5.2). `external`: optional reachability probe of each endpoint (`ciu up --probe-external`), otherwise nothing. `joined`: verify the reference (S9.5) and attach the consumer's containers to the reference network at compose time (S11.4). `ciu render` performs steps 1, 5 and 6 only (no hooks, no secrets beyond what the store already holds — a missing value is an ERROR naming `ciu up`).

### S8.8 Derived tables
`[resolved] waves = [[…], …]`, `edges = [{from, to, kind}]` with `kind ∈ bind|depends|secret→vault|secret→minter|network|pki`, and `gates.<k>` (S8.5.4).

---

## S9 Realness

### S9.1 Levels
`live` (the real thing; its init graph runs), `seeded` (a prepared Realization the project owns whose `pg:`/`minio:`-class facts hold by construction — declared in `provides`, no one-shot job needed; `vault:secret/*` facts MUST be minted by a hook that writes the prepared credentials to Vault, declared as the hook entry's `provides`; a `one_shot` service it does declare runs normally), `simulated` (a stub implementing the contract's protocol), `mock` (in-process double: no Realization, no resolutions, no edges).

### S9.2 Declaration
`[realness] default = "<level>"` (required; `ciu init` writes `"live"`), `pin.<logical> = "<level>"` (committed per-service pins, optional). `[service.<n>.<level>]` variants per S5.2; `[service.<n>.mock]` is declared as an empty table (`mock = {}`).

### S9.3 Selection
S9.3.1 For every LogicalService in the selected bundles, the level is the first defined of: `--realness <n>=<level>` on the command line; `[realness.pin].<n>`; `[realness].default`. S9.3.2 The chosen level MUST have a variant on the service (`[S9.3] service 'payment_api' has no 'simulated' variant`). S9.3.3 The selection is written to `[resolved.services.<n>] level, realization, service` (`realization`/`service` absent for `mock`). S9.3.4 A binding to a service selected at `mock` yields no edge and no resolution (S5.3.5).

### S9.4 Immutable record (per layout)
S9.4.1 The first `ciu up` for a layout writes the resolved selection of every LogicalService in the deploy set into the instance record `ciu.instance.json` under `realness.<layout>` (S14.7.1; in the state root, S2.6 — never into a file that travels with a release). S9.4.2 The record is a **constraint on selection, not a source**: thereafter, a selection per S9.3.1 that differs from that layout's record for any recorded service is an ERROR naming the source of the difference (`[S9.4] layout 'local' already runs main_db=seeded; --realness main_db=live conflicts; ciu clean --vanilla to reselect`, or `… pin main_db=live conflicts …`). Services not yet in the record (added bundles) are selected per S9.3 and appended. S9.4.3 `ciu clean` preserves the record; `ciu clean --vanilla` clears the current layout's record. S9.4.4 A record never travels: each state root keeps its own, so a target host's first `up` of a layout writes that host's record and the sender's records are irrelevant to it.

### S9.5 Joined Realizations
S9.5.1 `[realization.<n>] kind = "joined" instance = <ref> service = <X>` in an instance file borrows `X` from the reference instance `<ref>` (an instance label per S14.7.2, or an absolute path of a checkout). S9.5.2 The joiner's variant row `[service.<X>] <level> = "<n>"` MUST name the level the reference **actually runs** (read from the reference's rendered `[resolved.services.<X>]` at check/up time); a mismatch is an ERROR (`[S9.5] reference 'primary' runs main_db=seeded, this instance declares live`). CIU records the reference's level in the joiner's record (S9.4). S9.5.3 The reference MUST be up (its rendered file present and the borrowed variant service healthy per S8.6.3) or `ciu up` refuses. S9.5.4 CIU takes a shared lock on the reference's checkout directory (S14.4) while reading its rendered file and, for a joined `vault`, its store (S10.3.3). S9.5.5 `ciu instance add --join <ref> --services a,b,…` appends these tables to `ciu.instance.toml` of the current checkout with a round-trip TOML writer that preserves the operator's other tables and comments (it does not create a git worktree); a hand-written instance file is equivalent. S9.5.6 `ciu clean` on a reference whose instance network still has containers of another instance attached (`docker network inspect`) is an ERROR naming the joiner. S9.5.7 The join graph MUST be acyclic: an instance MUST NOT join a reference that (transitively) joins it; `ciu instance add --join` and `ciu check` stage 10 read the reference's instance file and refuse a cycle (`[S9.5] p146 joins primary, which joins p146`).

---

## S10 Secrets

### S10.1 Sources — the `from` key
| `from` | keys | meaning | store |
|---|---|---|---|
| `vault` | `path` (required), `field` (optional) | read from Vault at deploy time | copied into the store; refreshed on every `up` |
| `generate` | `store` (`local` default, or `vault`), `path` (required when `store = "vault"`), `length` (integer, default 32), `charset` (`alnum` default, `hex`, `printable`) | generate once; with `store = "vault"` only when the Vault path is absent, then read | store (+ Vault) |
| `ask` | `var` (`envname`, required) | from `CIU_SECRET_<var>` in CIU's environment, else the store, else an interactive prompt | store |
| `file` | `path` (absolute, required) | read a pre-provisioned file on the host that runs `up` | not stored (path recorded) |
| `host` | `entry` (required) | the host-scoped secret `[hosts.<h>.secrets.<entry>]` of the host this stack is placed on (S10.4) | read from `[secrets.hosts.<h>]` |
| `ephemeral` | `length`, `charset` | generated per run by the host that runs `up`, never stored | — |
| `hook` | — | produced by a `pre_secrets` hook of the same stack in the same run (a credential fetched at deploy time — a runner registration token); the hook emits it under this key (S12.3); absent at materialization → ERROR naming the key and the hooks that ran | store |
S10.1.1 Secret keys match `name`. S10.1.2 A `from = "generate", store = "vault"` secret at `path` derives the fact `vault:secret/<resolved path>` provided by its service (S8.3). S10.1.3 A `from = "host"` secret on a stack placed by any layout on a host without that entry is an ERROR at check time (`[S10.1] host 'rs1002' declares no secret 'tls_cert_pem'`). S10.1.4 A `from = "ephemeral"` secret consumed by services placed on two hosts of one layout is an ERROR (the value could not agree); use `generate`. S10.1.5 The `path` of a `vault` or vault-stored `generate` secret is either a key of `[vault.paths]` (no `/` in it) or a literal KV path (contains `/`); a `/`-free value that is not a `[vault.paths]` key is an ERROR naming the closest keys (`[S10.1] path 'postgres_controler_password' is not in [vault.paths] (did you mean postgres_controller_password?)`). The resolved literal path is what the store, the facts and the minter edges use. S10.1.6 A key `directive` is refused with a message naming Appendix A's mapping.

### S10.2 Declaration — `[ciu_stack.<svc>.secrets.<key>]` and `[ciu_stack.secrets.<key>]`
Keys: `from` (required, S10.1) and its source keys; `delivery` (`file | env | configfile | native | hook | none`, **required**); `env_name` (`envname`; required when `delivery = "env"`); `mode` (default `"0400"`); `uid` (default `instance.user_uid`); `enabled` (bool or `[project.control]` flag name, default true — a disabled secret is neither materialized nor delivered and derives no edge or fact). S10.2.1 `file`: the value is written to a temp copy (S10.7) and mounted read-only at `/run/secrets/<key>` in the declaring service (service-level) or in every service of the stack whose rendered compose block references `/run/secrets/<key>` (stack-level). S10.2.2 `env`: the value is passed as `env_name` through the compose process environment (S11.6); the rendered compose MUST reference it as `${env_name}`; ciu check stage 9 lists every env-delivered secret (restart-bound). S10.2.3 `native`: the application fetches the secret itself; CIU materializes nothing and delivers nothing; the row exists so the dependency (and the edges) are declared. S10.2.4 `none`: minted or registered by this stack for others; nothing is delivered here and the value is not handed to hooks. S10.2.5 `hook`: materialized and handed to this stack's hooks in their context (S12.2), delivered to no container. S10.2.6 `configfile`: the value is available only through `secret("<key>")` in the declaring service's config-file templates (S3.5.1, S6.9); the rendered config file is then a secret-bearing artifact — mode 0400, owner per `uid`, removed with the temp copies on `down`/`clean`, and still scanned by S2.4.2: CIU records which keys the render called `secret()` for, and only those values' matches are authorized in that file — every other store value found there is a leak. `secret()` is never available in compose templates. S10.2.7 A secret key declared at both stack level and on a service of the same stack is an ERROR. S10.2.8 The keys `consumed_by` and `produced_by` are refused (`[S10.2] produced_by is derived from the minter edge; consumed_by = "hook" is delivery = "hook"`).

### S10.3 Vault pointer and consumer paths
`[vault] service = "<logical>"` (required when any secret with `from = "vault"` or `store = "vault"` exists anywhere in the deploy set — `[S10.3] vault secrets present but [vault].service is not declared`), `token_file` (path, optional; token source #2 after `VAULT_TOKEN`, before the bootstrap token in the store), `[vault.paths.<name>] = "<kv path>"` (a reference table CIU **reads** to resolve `path` keys, S10.1.5; every value MUST be a literal KV path; a value no secret references is INFO). S10.3.1 CIU's Vault client connects through the pseudo-consumer `ciu`'s implicit binding to the `vault` LogicalService's first `http`/`https` endpoint (S7.8.7). S10.3.2 The Vault bootstrap values (root token, unseal keys) are stored at `[secrets.<vault realization>.<variant service>.root_token|unseal_keys]` by the vault stack's hook through the hook output contract (S12.3), never in state. S10.3.3 Token sources, in order: `VAULT_TOKEN`; `token_file`; the bootstrap token in this instance's store; for a `joined` vault, the bootstrap token in the **reference's** store, read under the reference's shared lock (S9.5.4). None → ERROR naming the four sources.

### S10.4 Host-scoped secrets
`[hosts.<h>.secrets.<entry>]` (S7.2) accept `from ∈ ask | generate | file`; they are materialized on the CIU host (prompting if needed) before a push (S17.3), stored under `[secrets.hosts.<h>.<entry>]` (the realization name `hosts` is reserved), and pushed only to host `<h>`. A service consumes one through a secret declared with `from = "host", entry = "<entry>"` and any delivery (S10.1).

### S10.5 TLS material for `mtls`/`tls` networks
S10.5.1 For every Realization `C` with a resolution over a network `N` with `tls ≠ none`, and for every Realization `R` with an endpoint reached over such a network, CIU derives stack-level `file` secrets `tls_cert`, `tls_key` (both) and `tls_ca` (consumers, and providers when `mtls`) with `from = "vault"` at paths `pki/<N>/<realization>/{cert,key,ca}`; resolutions and the provider's endpoint table carry their `/run/secrets/tls_*` paths. S10.5.2 The `pki` LogicalService's provider MUST provide the fact `pki:issuer/<N>` (a hook entry's `provides`); the derived `pki/<N>/…` paths are satisfied by that provider (S8.3), and the **pki** edge orders it. Issuance itself is the hook's job (CIU runs no CA).

### S10.6 The store — `ciu.secrets.toml`
```toml
[secrets.<realization>.<service>.<key>]   # or [secrets.<realization>.<key>] for stack-level secrets
value = "…"  source = "vault:db/postgres/controller_password"  created = 2026-09-02T10:11:12Z   # source: <from>[:<resolved path|var|entry>] or hook:<script>
```
S10.6.1 One file per instance in the state root (S2.6; shared by every release of the instance), mode 0600, written atomically (temp + rename) under the instance lock (S14.4). S10.6.2 `ephemeral` values are never stored. S10.6.3 `ciu secrets show [--values]`, `ciu secrets reset <realization>[.<service>][.<key>]`, `ciu secrets host <h> <entry>` (materialize one host-scoped entry), `ciu migrate --secrets` (imports v7 stores; refuses to re-mint). S10.6.4 A value is refreshed from Vault on every `ciu up` for `from = "vault"`; generated values are generated once and kept.

### S10.7 Temp copies
S10.7.1 For `file` delivery CIU maintains one **secret directory per service**, `<location>/ciu.secret-copy.<svc>/`, containing one file per `file`-delivered secret of that service (stack-level secrets are copied into every consuming service's directory), mode/uid per S10.2, written before compose runs and removed on `ciu down`/`clean`; the directory — not the individual files — is bind-mounted read-only at `/run/secrets` (S11.4), so the paths `/run/secrets/<key>` are unchanged. S10.7.2 A refresh writes the new value to a temporary name in the same directory, `fsync`s it, renames it over `<key>` and `fsync`s the directory: a reader inside the container sees either the old or the new complete file, never a truncated one (an in-place rewrite is not atomic for readers). Applications that hold the file open must reopen it to see a new value; CIU does not signal or restart them (rotation is out of scope, S1.1).

### S10.8 Static rules (ciu check stage 9)
`from` and `delivery` present on every secret; source keys complete per S10.1; `env_name` present and unique per stack for `env` and disjoint from binding variables (S6.4.6); vault pointer per S10.3; every `vault` path resolves (S10.1.5) and has a minter or an asserting Realization (S8.3); `host` entries exist on every placement host (S10.1.3); `ephemeral` not shared cross-host (S10.1.4); secret-shaped keys outside `secrets` tables refused (S6.10); env-delivered secrets listed as WARN.

---

## S11 Compose rendering

### S11.1 Pipeline
For each `ciu_stack` in the deploy set (in wave order), CIU renders `ciu.compose.yml.j2` with the compose context (S3.5.1), parses the result as YAML, removes disabled services (S6.7), applies the injections of S11.4, validates per S11.5, and writes `ciu.compose.yml`. The rendered file is the ONLY file passed to `docker compose` (`-f ciu.compose.yml -p <compose_project> --project-directory <location>`).

### S11.2 Template obligations
A template MUST write, per service: `image` (from `ciu_stack.<svc>.image`), and whatever compose fields the service genuinely needs (`command`, `environment`, `volumes`, `cap_add`, `restart`, `user`, `healthcheck.test`, extra `labels`, `ulimits`, `sysctls`, `devices`; the compose `build:` stanza is injected from the service's `build` table, S6.2, never written by the template). It MUST use the service key as the compose service name (`services: <svc>:`), or `{{ replica.compose_key }}` per replica block (S6.5); CIU renames the former to `compose_key` (S4.2) and **rewrites every compose field that references a service by name** — `network_mode: "service:<key>"`, `ipc: "service:<key>"`, `pid: "service:<key>"` — to the derived `compose_key`; a reference to a replicated service (`instances > 1`) in such a field is an ERROR unless it names one replica key, and a reference to an unknown key is an ERROR naming the YAML path (`[S11.2] services.browser.network_mode references 'pwmcp', not a service of this stack`).

### S11.3 Template prohibitions
A template MUST NOT write: `container_name`, `hostname`, `networks` (service-level or top-level), `secrets`, `depends_on`, `links`, `extends`, `volumes_from`, `build` (S6.2's `build` table is injected), `cgroup_parent`/`mem_limit`/`memswap_limit`/`mem_reservation`/`cpus`/`cpu_shares`/`oom_score_adj`/`pids_limit`, `ports`, `expose`, a label under `ciu.*` (S4.5.2), `name:` under `volumes:` that does not start with `{{ instance.project }}-{{ instance.id }}-`, `${VAR:-default}` or `${VAR:?…}` forms, `{{ env.* }}`, an `environment` entry whose name is injected by a binding (S6.4.6). A violation is an ERROR naming the field (`[S11.3] template sets container_name for 'postgres'`), except `container_name`/`hostname` equal to the derived value (S4.3.2, removed silently). Replicated services (S6.5) MUST be emitted by iterating `ciu_stack.<svc>.identity.replicas`, one block per replica.

### S11.4 Injections (applied to the parsed YAML)
Per service (by service key, and per replica block): `container_name` and `hostname` (S4.2; `hostname` omitted when `host_network = true`); `networks: { <instance network>: { aliases: [<service compose_key>, <replica compose_key>, <container_name>] + aliases } }` plus the reference instance network for services of a Realization that has resolutions to `joined` services (omitted for `host_network`); `labels` (S4.5); `environment` entries `CIU_PROJECT`, `CIU_INSTANCE`, `CIU_REALIZATION`, `CIU_SERVICE`, `CIU_REPLICA` (when replicated), `CIU_IMAGE_REVISION` (the git revision `ciu build` stamped for a project-built image, else absent), and every `env`-delivered binding's variables (S6.4); `depends_on: { <sibling compose_key>: { condition: … } }` from `depends_on` (`service_completed_successfully` when the sibling is `one_shot`, else `service_healthy` when the sibling's RENDERED block carries a `healthcheck`, else `service_started`; a disabled sibling is dropped with a WARN); `healthcheck.interval/timeout/retries/start_period` from the merged `health` table when the template wrote `healthcheck.test` (S6.6.4); a read-only bind mount of the service's secret directory (S10.7.1, absolute physical path) at `/run/secrets` when the service has any `file`-delivered secret (compose's own `secrets:` mechanism mounts single inodes and is therefore not used); `ports:` per S7.4 (derived publications and `publish = "host"`); read-only bind mounts of rendered config directories (S6.9, absolute physical paths); governance fields (S13.3); `network_mode: host` when `host_network = true`. Top-level: `networks: { <instance network>: { external: true, name: … } }` (and the reference network); `name: <compose_project>`. Every path CIU writes into the file is absolute under `physical_repo_root`.

### S11.5 Validation of the rendered compose
Secret-free scan (S2.4.2); every `${VAR}` reference is in the allowed set (S11.6); every service has an `image`; a `healthcheck` is present for every gate provider that is not `one_shot` (`[S11.5] 'postgres' is a provider but declares no healthcheck`); YAML re-serialized deterministically.

### S11.6 Compose process environment and `${VAR}`
S11.6.1 The environment passed to `docker compose` consists of exactly: `project.compose_env.*`, the selected bundles' `compose_env`, `COMPOSE_PROFILES` (from `compose_profiles`), and the `env_name` values of `env`-delivered secrets. Nothing else is forwarded; a template that needs a runtime switch declares it as data (a consumer sub-table) or as a secret. S11.6.2 A `${VAR}` in the rendered compose that is not in that set is an ERROR (`[S11.6] compose references ${FOO}, which no source provides`).

### S11.7 Rendered artifacts and inspection
`ciu.compose.yml` (S2.2), `ciu.rendered/`, temp secret copies. `ciu render` writes them without deploying; `ciu up` writes them under the instance lock immediately before compose runs (S8.7 step 6). `ciu render --show-injected [--realization r]` prints, per service, the unified diff between the template's output and the final artifact, so the boundary between a template bug and a CIU bug stays inspectable (the reason v7 S8.1 preferred overlay files).

---

## S12 Hooks

### S12.1 Lifecycle
`[hooks] pre_secrets`, `pre_compose`, `post_compose`: lists of entries (S6.10), each an executable path relative to the stack directory (or absolute). Each runs, in list order, at its step of S8.7, as a subprocess with the working directory set to the stack directory, `stdin` = the context (S12.2), `stdout` = its outputs (S12.3), `stderr` passed through to CIU's log. A missing script or a non-zero exit aborts the run (`[S12.1] hook post_compose_vault.py exited 3`). The process environment of a hook is **clean**: exactly `PATH`, `HOME`, `LANG`, `LC_ALL`, `TZ`, `TMPDIR` from CIU's environment, `CIU_HOOK=1`, plus the variables named in `[hooks] env_allow = [...]` (a per-stack allow-list; `CIU_SECRET_*`, `VAULT_TOKEN`, `SSH_AUTH_SOCK`, `DOCKER_*` and `*_PROXY` variables are refused in it) — no ambient credential ever reaches a hook by accident; a hook MUST NOT read identity facts from it (I5). Each entry receives a context built when it starts, so entry `i+1` of one phase sees the `state` entry `i` emitted (S12.3); entries of one phase run strictly in list order and never concurrently.

### S12.2 Context
Hooks receive a JSON document on stdin (`api = "ciu/hook-context"`, `api_version = 1`) containing: `phase`; `project`, `instance` and `host` (S3.5.2–S3.5.3); `stack_dir`; `realization` (the name of the stack's Realization); `services` (this stack's services as the compose render sees them: identity, health, endpoints with `publications`, and every binding's resolution regardless of its delivery — a hook is not a container); `secrets` (**only** the keys the entry lists in its `secrets` list, S6.10, with their materialized values — for a `pre_secrets` entry, only the container-independent sources S6.10 permits there, materialized ahead of S8.7 step 3 for that entry; a listed key with `delivery = "native"` is an ERROR at check time because CIU materializes nothing for it; a `delivery = "hook"` secret is reachable only by entries that list it); `state` (S6.10); `deploy_set` (the merged view of every Realization in the deploy set, S3.6, read-only); and `resolved` — the whole derived table of S3.7 (waves, edges, selection, `networks` incl. proxy `fqdn`s, every binding resolution, and `bindings.ciu.vault`, CIU's own vault resolution from the host's vantage point, S7.8.7 — the address a hook that writes Vault uses). They MUST NOT read `ciu.env`, the process environment for identity facts, or the rendered global file directly — every fact they need is in the context.

### S12.3 Outputs
A hook MAY print a JSON document on stdout (`api = "ciu/hook-output"`, `api_version = 1`): `{"state": {...}}` merges into `<location>/ciu.state.toml` (S6.10; secret-shaped keys refused) and is visible to every later step of the same run (S8.7); `{"secrets": {"<key>": "<value>"}}` stores values under `[secrets.<realization>.<primary>.<key>]` with `source = "hook:<script>"` (S10.6) — the only way a hook writes a secret; a key that is declared with `from = "hook"` is thereby satisfied (from a `pre_secrets` hook, S8.7 step 2), a key declared with any other source is an ERROR (`[S12.3] hook emits 'redis_password' but its source is generate`), an undeclared key is a bootstrap value (S10.3.2); `{"facts": ["…"]}` asserts the entry's `provides` facts were created (recorded, then probed per S8.5). Anything else on stdout is an ERROR naming the hook (a hook that wants to log writes to stderr).

### S12.4 Validation hooks
S12.4.1 A hook script MAY support the argument `--validate`: `ciu check --validate-hooks` (stage 13, and the automatic check inside `ciu up`, which executes the same scripts moments later anyway — never a bare `ciu check`, which stays free of consumer code) runs every hook of every stack in the deploy set with `--validate` and the S12.2 context minus secret values on stdin, and expects `{"findings": [{"severity": "WARN"|"ERROR", "message": "…", "rule": "…"}]}` on stdout (an empty list or no output = no findings; a hook that exits non-zero under `--validate` is itself an ERROR). S12.4.2 Severities map through `ciu.exit_on` (S15.2). S12.4.3 Validation runs are expected to be side-effect-free, but CIU cannot enforce that for an arbitrary program: it runs them with the clean environment of S12.1, no secrets, a wall budget of 30 s each, and labels stage 13 output "executes consumer hook code"; a consumer whose hooks cannot honour the convention omits `--validate` support.

### S12.5 The helper library — `ciu.hookkit`
S12.5.1 CIU ships an importable Python package `ciu.hookkit` with no dependency on the rest of `ciu` beyond the standard library, so a hook that imports it runs wherever `ciu` is installed and can be vendored. It MUST provide at least: `context()` (parse stdin into an object with attribute access; `ctx.services.<svc>.identity.container_name`, `ctx.services.<svc>.binds.<local>.host`, `ctx.secrets["key"]`, `ctx.state`, `ctx.phase`); `emit(state=None, secrets=None, facts=None)` (write the S12.3 document once, at exit); `wait_healthy(service, timeout=None)` and `wait_tcp(host, port, timeout=None)` and `wait_http(url, timeout=None, expect=range(200, 300))` (bounded polls that raise with the last observed state — a hook MUST NOT hand-roll a poll loop, the CIU-4 rule); `docker_exec(service, argv, input=None)` (exec into one of this stack's containers by service key); `secret_file(key)` (the temp-copy path of a `file`-delivered secret); `is_validate()` and `findings()` (the `--validate` protocol). S12.5.2 The hook templates CIU ships (`ciu init --stack`) are written on `ciu.hookkit`. S12.5.3 `ciu hook run <realization> <phase> [--validate] [--dry-run]` runs one stack's hooks of one phase outside `ciu up` with the same context, for authoring and tests.

---

## S13 Resource governance

### S13.1 Vocabulary (shared by stacks and gate lanes)
The **resource key set** `RK` = `memory_max`, `memory_swap_max`, `memory_high`, `memory_low`, `memory_min`, `cpu_weight` (1..10000), `cpu_max` (`"<quota> <period>"` or `"max"`), `io_weight` (1..10000), `pids_max`. Sizes per S1.4. Every key is named after the cgroup-v2 controller file it governs (`_` for `.`); there are no alternative spellings — S13.3 is the only place a key is mapped onto a compose field.

### S13.2 `[governance]` (global base; per-stack `[governance]` shallow-merges over it)
Keys: `enabled` (bool, default false), `cgroup_parent` (slice name; `""` = `$CGROUP_PARENT_DEV_BACKGROUND`, required non-empty when enabled), `ksm_optin` (`builtin` | path), `exempt_services` (list of `<realization>.<svc>`), `memory_profile.default.ksm` / `memory_profile.services.<svc>.ksm` (`preload|wrapper|off`), all of `RK`, plus `io_read_iops_max`, `io_write_iops_max`, `io_read_bps_max`, `io_write_bps_max` (integers; `0` = derive from the baseline), `device` (block device; `""` = autodetect), `baseline_path` (path; `CIU_GOV_BASELINE_PATH` overrides). S13.2.1 `memory_min` is preflight-only: host `MemAvailable` below the sum of the deploy set's `memory_min` is an ERROR before compose runs. S13.2.2 Unknown keys are an ERROR (S3.8.1).

### S13.3 Application to containers
With `enabled = true`, CIU injects into every non-exempt service block the compose fields that Docker translates to the cgroup-v2 files — with the **conversions written here, because the compose fields are not the cgroup files**: `cgroup_parent`; `mem_limit` ← `memory_max`; `memswap_limit` ← `memory_max + memory_swap_max` (compose's field is the *total* of memory and swap, while `memory.swap.max` is swap alone; `memory_swap_max = "0"` therefore yields `memswap_limit = memory_max`, i.e. no swap; `memory_swap_max = "max"` with a finite `memory_max` yields `-1`; `memory_max = "max"` with a finite `memory_swap_max` is **not representable** by compose — `memswap_limit` needs a memory limit — so CIU injects no `memswap_limit` and writes `memory.swap.max` directly after start, exactly as `memory_high` below, never silently unlimited swap); `mem_reservation` ← `memory_low`; `cpu_shares` ← the exact integer inverse of Docker's cgroup-v2 mapping `weight = 1 + ((shares − 2) × 9999) div 262142`: `shares = 2 + ceil((weight − 1) × 262142 / 9999)`, clamped to 2..262144 — the **ceiling**, not the nearest integer, selects the first `shares` value of each weight's bucket (nearest rounding maps 4999 of the 10000 weights one too low, `cpu_weight = 100` to 99), and an implementation MUST prove by test that every weight 1..10000 maps back to itself through the forward map; `cpus` ← `cpu_max` (quota ÷ period as a decimal; `"max"` injects nothing); `blkio_config` ← `io_*`; `pids_limit` ← `pids_max`. `memory_high` has no compose field: CIU writes it to the container's cgroup immediately after start, and the interval between start and that write is uncapped by it (documented). `memory_min` is preflight-only (S13.2.1) and is never written to a cgroup. S13.3.2 After every start CIU reads back `memory.max`, `memory.swap.max`, `memory.high`, `memory.low`, `cpu.weight`, `cpu.max`, `io.weight`, `io.max` (every device tuple the `io_*` keys produced, S13.2), `pids.max` from the container's cgroup directory and records them as `applied` (S13.4). A read-back that differs from the declared value beyond rounding is, for every **hard cap** — `memory.max`, `memory.swap.max`, `pids.max`, `cpu.max`, and each `io.max` tuple — an ERROR that stops the deploy naming both values: a container running uncapped where a cap was declared is a wrong deployment on a shared host, not a warning; for the weights and protections `memory.high`, `memory.low`, `cpu.weight` and `io.weight` it is a WARN naming both. A controller the daemon or kernel does not provide is recorded `unsupported` (S13.4) — and when the declaration is a hard cap, `unsupported` is a refusal **before** the container starts (`[S13.3] io_read_bps_max declared but the io controller is not delegated on this host`), never an unenforced deployment. S13.3.1 KSM: `ksm_optin = "builtin"` builds the shim into `$XDG_CACHE_HOME/ciu/ksm/` and bind-mounts it; `wrapper`/`preload`/`off` per service (`CIU_KSM=off` disables for one run).

### S13.4 Effective governance (derived)
`[resolved.governance.<realization>.<svc>]` lists, per key of `RK`, `requested` (after merge and exemptions), `applied` (the cgroup read-back of S13.3.2, present after the container started) and `unsupported = true` where the daemon or kernel provides no controller — never a requested value presented as applied.

---

## S14 Instances

### S14.1 Lifecycle
`ciu instance init` → (`ciu check`, automatic) → `ciu up` → `ciu gate …` → `ciu down` | `ciu clean [--vanilla]`. S14.1.1 `init` writes the generated file and the host file (S14.2), creates `ciu.instance.toml` when absent (with `--layout`/`--bundles`/`--label` filling the operator table; a project with exactly one layout needs no `--layout`, S7.6.1), and on a git checkout writes the instance record (S14.7). S14.1.2 `up` renders, locks, creates the instance network (idempotent, labelled per S4.5), deploys per S8, writes the realness record (S9.4). S14.1.3 `down` stops containers and removes the temp secret copies; everything else stays on disk. S14.1.4 `clean` removes containers, named volumes labelled to the instance, temp secret copies, rendered artifacts (the S2.3 list minus the instance file, the generated file, `ciu.secrets.toml`, `ciu.hosts.toml`, `ciu.instance.json`, `ciu.state.toml`, `ciu-data/`), disconnects CIU's own container from the instance network when `auto_connect_network` attached it, and removes the network; `--vanilla` additionally clears the current layout's record, the store, `ciu.state.toml` files, `ciu-data/` (S6.8.3) and the instance record; it refuses while another instance's containers are attached to this instance's network (S9.5.6).

### S14.2 Instance file, generated file, host file
The **instance file** `ciu.instance.toml` is the operator's (CIU appends or replaces whole tables with a round-trip writer, S9.5.5, and never rewrites it whole):
```toml
[ciu.instance]
layout = "local"                # required for every mutating verb (S7.6.1)
bundles = ["all", "test"]       # default bundle selection for `ciu up`
label = "primary"               # unique per git family (S14.7.2); `ciu instance list` shows it; never part of an identity
[ciu.instance.host_ports]       # optional per-instance host-port overrides: "<realization>.<svc>.<endpoint>" = port
"cadvisor.cadvisor.http" = 18080
# plus [realization.<n>] kind = "joined" … and [service.<n>] <level> = "<n>" rows for joins (S9.5)
```
The **generated file** `ciu.instance.generated.toml` is CIU-owned, plain TOML, rewritten whole by CIU, merged after the instance file; it holds only facts that are true on every host of a layout, so it travels with a release unchanged (S17.3.1):
```toml
[ciu.instance.generated]        # instance IDENTITY — identical on every host of a layout
instance_id = "98535c"          # S4.1.1
owner_id = "3f9c…"              # the owner token, 32 hex (S4.1.1)
[ciu.instance.build]            # written by `ciu build` (S17.6): version, time, image ids and digests
build_version = "2026.09.02-9f3c1a2"
build_time = "2026-09-02T11:02:41Z"
```
The **host file** `ciu.host.toml` is CIU-owned, host-local, lives in the state root (S2.6) and is merged last:
```toml
[ciu.host]                      # HOST-LOCAL facts — written on each host by `ciu instance init [--host <h>]` (`prepare` on a release target, S17.4.1)
name = "rs1002"                 # which [hosts.<h>] this machine is (the `local = true` host without --host)
hostname = "rs1002"             # the machine's own hostname (lease holder, S14.6.2)
env_type = "native"             # devcontainer | native | github-actions
user_uid = 1000
user_gid = 1000
docker_gid = 988
# repo_root / physical_repo_root are NOT stored: they are the checkout root being run and the path the daemon
# sees for it, derived at every verb (S4.1.4 proves the mapping) — a release's directory changes per release
# and the host file is shared by all of them (S2.6.3).
```
The realness record (S9.4) is part of the instance record `ciu.instance.json` (S14.7.1), not of these files. S14.2.1 A hand edit to the generated file or the host file is overwritten without notice (both carry that warning). S14.2.2 `ciu instance init --host <h>` sets `[ciu.host].name`; without the flag, the host with `local = true` (S7.2.1) is assumed and MUST exist. S14.2.3 The instance file and the generated file ship with a release (S17.3) so `instance_id` and `owner_id` are identical across hosts; the host file never ships — each target writes its own into its state root during `prepare` (S17.4.1), and a release directory is never modified after verification (S2.6.1). S14.2.4 `require_fqdn = true` (S3.4.7) makes a rendered host without `fqdn` (S7.2) an ERROR at init.

### S14.3 Verb classes
- **Mutating (exclusive instance lock, then the stack locks of every Realization the verb touches — S14.4.9):** `instance init|add|remove|reap|lease|restore`, `lease acquire --exclusive` (instance level), `up`, `down`, `clean`, `render`, `build`, `push`, `activate`, `secrets reset|host|rotate-bootstrap`, `migrate`, `dev`.
- **Gate (shared instance lock for the duration of the lanes, plus exclusive stack locks for the exec target and `resources.shared`):** `gate`, `lease acquire --shared` (instance level) — several `ciu gate` processes coexist under S16.6 admission; a `sequence` lane runs its members in-process (S16.5.4), so no gate ever spawns another `ciu gate`.
- **Realization-only (no instance lock — the one exception, so that an external lease never blocks the whole instance):** `lease acquire --realization <r>` (S14.4.8).
- **Read-only (shared instance lock while reading the rendered file, none afterwards; no stack locks):** `check`, `env print`, `instance list|show|exec|backup`, `lease status|wait`, `diagnose`, `status`, `show`, `query`, `secrets show`, `provenance`, `schema`, `doctor`, `version`, `help`.
- **Lock-free:** every verb in a zero-instance project (S16.11); `init`; `schema`; `version`; `help`.

### S14.4 The instance lock
S14.4.1 The lock object is the **checkout root directory**: CIU opens it (`O_RDONLY | O_DIRECTORY`) and takes `flock(LOCK_EX)` for mutating verbs, `flock(LOCK_SH)` for gate and read-only verbs. The directory's inode is stable for the life of the checkout; no file CIU or git rewrites is ever the lock. S14.4.2 The rendered file is written by temp-file and atomic rename under `LOCK_EX`; a reader that finds no `ciu.resolved.toml` reports *not rendered* (read-only verbs say so; the gate refuses in a project with Realizations, S16.11). There is no completion marker and no torn state. S14.4.3 Lock order: before mutating, a verb resolves every instance whose lock it will need — its own and every joined reference (S9.5) — and acquires them in ascending `instance_id` order (its own exclusively, references shared), then the git-common-dir registry lock (S14.7), then the **stack-directory locks** of every Realization it touches (S14.4.9) in ascending realization name. Because every process acquires the same set in the same total order and the join graph is acyclic (S9.5.7), no cycle can form; `flock` itself detects no deadlock, so the order is the only protection and an implementation MUST NOT take a reference's lock lazily. The store (S10.6) is written under the instance lock; there is no separate secrets lock. The automatic `ciu check` inside a mutating verb reuses the verb's descriptors and takes no lock of its own. S14.4.4 Contention: fail fast (`[S14.4] instance 98535c is locked by another ciu process; pass --wait`), naming the holder's pid and verb when `/proc/locks` and `/proc/<pid>/cmdline` make them available; `--wait[=<duration>]` blocks. A dead holder releases the lock by itself (`flock` is kernel-owned); there is no `lock break` verb. S14.4.5 Deleting `ciu.resolved.toml` alone while an instance is up does not affect the lock; readers see *not rendered* until the next `ciu render`, which regenerates it identically from the surviving declaration files (S3.7.3). Deleting the non-regenerable files is destructive per S2.3.4; the lock survives that too, so a subsequent `ciu instance init` in the same checkout still serializes against a running verb. S14.4.6 On a filesystem where `flock` on a directory is unsupported (some network filesystems), CIU refuses every locking verb naming the filesystem (`[S14.4] cannot lock <root>: flock unsupported on nfs`) rather than running unlocked. S14.4.7 **Canonical keys.** The two lock objects of this specification — the checkout root directory (this rule: the instance) and the stack directory of a Realization in the checkout that owns it (S14.4.9: exclusive use of that Realization's containers and artifacts) — are the **only** keys CIU and its consumers use for mutual exclusion on an instance. A third party that wants to serialize against CIU MUST take the same `flock` on the same directory (`flock <checkout root> cmd`, `flock <stack dir> cmd` with util-linux) and MUST NOT derive a lock name of its own from identities, labels or paths: a directory git tracks is stable across renames of every derived name, exists before a render exists, and is the same inode for every process on the host — which is what nyxloom's `Stack: exclusive` and dstdns's caller-side `flock` re-derived from CIU's identity outputs until now. run-gate's self-serialization (RG-39) adopts the stack-directory key wherever it can read `ciu.resolved.toml` (`[realization.<R>] location` of the container's owner) and a container-name key only where no rendered file exists. S14.4.8 **`ciu lease` — the lock as a verb.** `ciu lease acquire (--exclusive | --shared) [--realization <r>] [--purpose <text>] [--wait[=D]] -- <cmd>` takes the instance lock (`--exclusive`: as a mutating verb, so every other gate and mutating verb blocks; `--shared`: as a gate) or, with `--realization`, **only** the stack-directory lock of `<r>` (exclusive or shared as asked; no instance lock — S14.3's one exception, so a tester lease never stalls the instance), runs `<cmd>` while holding it, and releases on exit. The holder is recorded without any lock as one atomic file `<state root>/ciu-leases/<pid>-<started>.json` (`api = "ciu/lease-record"`: `pid`, `started`, `verb`, `purpose`, `key = "instance" | "<realization>"`, `mode`), written by temp-file and rename and unlinked on release; two leases therefore never rewrite a shared record, and a record whose pid is gone is dropped by any reader because the kernel already released its lock. The held descriptors are inherited by `<cmd>` and named in its environment as `CIU_LEASE_FDS="<key>=<fd>,…"` (S18.2), so a CIU verb — or run-gate — that needs one of those keys inside `<cmd>` locks the inherited descriptor (the same open file description already holds it) instead of opening the directory anew and deadlocking with its own parent. `ciu lease status [--json]` lists the records plus `/proc/locks` where readable; `ciu lease wait [--realization <r>] [--timeout D]` polls with a non-blocking probe (`LOCK_NB`, released at once) and never holds the key it observes. An instance-level `--shared` lease is what an external gate runner takes; `--realization` is what "use this tester exclusively" means. Contention follows S14.4.4. There is no named lease and no lock key a consumer could spell differently (I1). S14.4.9 **Lock matrix — who takes the stack-directory lock.** The stack-directory lock of Realization `R` protects `R`'s containers, volumes and rendered artifacts, and it is taken by every operation that mutates or exclusively uses them: `up`, `down`, `clean`, `dev`, `render` and `build` for every Realization they act on (exclusive, after the instance and registry locks, in ascending name order — S14.4.3); a gate lane for its `exec_in` target and every `resources.shared` Realization (exclusive, S16.5.3, S16.5.7); `lease acquire --realization` (as asked). Read-only verbs, hooks (which run inside a verb that already holds the lock) and lanes that neither exec into `R` nor name it in `resources.shared` take nothing on `R`. Consequently a run-gate or `ciu lease` holder of `R`'s directory is never overrun by `ciu up` or `ciu clean`: they wait for `R` (or, with `--wait` absent, fail fast naming the holder, S14.4.4), while an `up` of an unrelated Realization proceeds.

### S14.5 Interleavings prevented
Two `up`; `up` ‖ `gate` (a gate lane exec-ing into a container being recreated); `instance init` ‖ render; `clean` ‖ `up`; two first-`up` realness selections; `migrate` ‖ `up`; a hook writing the store while `secrets reset` runs.

### S14.6 `[ciu.instances]` — budget and lease (one closed key set)
`max_concurrent` (integer ≥ 1, optional; ambient override `CIU_MAX_CONCURRENT_INSTANCES`), `lease_ttl_hours` (number > 0, optional; absent = no lease). S14.6.1 The budget counts instances of one git family (S14.7) with a live record; `up` beyond the budget is an ERROR naming the holders. S14.6.2 A lease is renewed by every mutating verb and expires after `lease_ttl_hours`; the holder is recorded as `ciu@<host.hostname>:<instance_id>`; `ciu instance lease --extend [D] | --perpetual | --release` manages it; `ciu instance reap` removes expired instances' containers and records (never their checkouts). S14.6.3 `ciu instance exec --env <e> -- <cmd>` runs a command in a gate environment (S16.4) after the environment's mount proof (S16.4.3) — there is no separate exec-target table.

### S14.7 Instance registry
S14.7.1 Every checkout in a git family registers `ciu.instance.json` (gitignored; in S2.3's list) with `api = "ciu/instance-record"`, `api_version`, `instance_id`, `path`, `label`, `created`, `lease_until`, `claims` (the socket claims held while up, S6.3.2), `realness` (the per-layout selection records of S9.4, `realness.<layout>.<logical> = <level>`); the record lives in the state root (S2.6) and lease holders are separate files (S14.4.8); the family is enumerated through the git common directory (a lock file `ciu-instances.lock` there serializes allocation and budget). The `instance_id` is duplicated from the generated file on purpose: the record is mutated by other checkouts (reap, lease), the generated file only by this checkout's CIU. S14.7.2 An instance label MUST be unique within a git family (`instance init`/`add` refuse a duplicate); a join's `instance` value (S9.5.1) resolves to a registered label, else to an absolute path; nothing else.

### S14.8 Backup and restore of the non-regenerable files
S14.8.1 `ciu instance backup [--to DIR] [--data]` copies, under the shared instance lock (writers hold it exclusively, so the copy is consistent), the files S2.3.4 calls non-regenerable — `ciu.instance.toml`, `ciu.instance.generated.toml`, `ciu.host.toml`, `ciu.instance.json`, `ciu.secrets.toml`, `ciu.hosts.toml` (when it is in the checkout), every `ciu.state.toml` of the deploy set (S2.6) — with their modes, into `DIR` (default `${XDG_STATE_HOME:-~/.local/state}/ciu/backups/<project>-<instance_id>-<created>/`, created 0700, refused when it already exists), and writes `ciu.backup.json` there (`api = "ciu/backup"`, `api_version = 1`: `instance_id`, `owner_id`, `physical_repo_root`, `created`, the file list with SHA-256 and mode). `--data` additionally copies `ciu-data/` through the privileged helper (S6.8.1), ownership preserved. Containers, named volumes and rendered artifacts are not part of a backup: a backup is the state `ciu render` cannot recreate, not the deployment. S14.8.2 `ciu instance restore --from DIR [--force] [--move]` verifies every digest in `ciu.backup.json`, refuses when any file it would write already exists (unless `--force`, which replaces them), refuses when the backup's `instance_id` differs from the id derived from this checkout's proven physical path (S4.1.1) unless `--move` is given — which follows S4.1.2 exactly: refused while any resource carries the backup's `owner_id`, else a new id and a new token — writes the files with their recorded modes under the exclusive instance lock, and never touches containers. S14.8.3 Both verbs print the file list and the destination; neither runs automatically.

---

## S15 `ciu check`

### S15.1 Invocation
`ciu check [--live] [--layout L] [--host H] [--bundles …] [--realness …] [--graph] [--gates] [--validate-hooks] [--json]`. Runs automatically before every mutating verb with the verb's selection (`--no-check` skips; the skip is printed). Side-effect-free except stage 15's live probes (network reads) and stage 13, which executes consumer hook code and therefore runs only with `--validate-hooks` or inside `ciu up` (S12.4.1).

### S15.2 Severities and exit
Findings are `ERROR`, `WARN` or `INFO`. `ciu.exit_on` ∈ `WARN|ERROR|NEVER` decides the exit code of the standalone verb: `WARN` → non-zero on any ERROR or WARN; `ERROR` → non-zero on errors only; `NEVER` → always zero (findings still printed). Inside a mutating verb, any ERROR aborts the verb and WARNs are printed; `ciu.exit_on` does not make a WARN abort a deploy. Every finding names its rule (`[S<n>.<m>]`), the file and table or line where applicable, and — for referential findings — the candidate names that would have matched (edit distance ≤ 2).

### S15.3 Stages
A stage runs unless a stage it depends on produced an ERROR (dependencies in the last column); independent stages run so that one typo does not hide unrelated findings.
| # | stage | rules | needs |
|---|---|---|---|
| 1 | files | S2.1–S2.3, S2.6 (gitignore list; instance file, generated file and host file present — skipped for `instance init` and in zero-instance projects; the state root resolvable and writable; no `.ciu/` — skipped for `migrate`; no `.j2` declaration files, S3.2.6) | — |
| 2 | parse | every declaration file parses as TOML; S2.4.1 secret-free scan of declarations and templates | — |
| 3 | schema | S3.3, S3.8 closed key sets, types, vocabularies, grammars, `revision` — every key, value, flag, state and reason against the one definition of S3.8.4 (guarded by the conformance test of S3.8.5); reserved names (S1.4); refused legacy keys (S6.2.3, S10.1.6, S10.2.8); `ciu.inherit` target exists, parses and is cycle-free (S3.1.5) | 2 |
| 4 | references | every name resolves: `realized_by`, variant `service`, binding `to` (service and endpoint), `depends_on` (siblings), `enabled` flags, `exec_in`, `image_from`, `requires.healthy`, `resources.shared`, `vault.service`, `network.realized_by`, `network.pki`, `allow_from`, `bundles.services`/`includes`, `layouts.hosts.*.bundles`, `layouts.hosts.*.reach`, joined `instance`/`service`, hook `service`, `secrets` and `inputs` (S6.10, incl. the `pre_secrets` source restriction); `location` exists under the checkout with both files and its root is `ciu_stack`; no shared `location` (S5.4.3); `build.context` inside the checkout and at most one `build` per image reference (S6.2); exactly one primary (S8.6.1); endpoint names unique per stack (S6.3.1); `listen` present exactly on host-network endpoints (S6.3); `unchecked`/`verify` for empty-contract prepared variants (S5.3.6); no unknown scalar on a service table (S6.2.1); `vault.paths` references (S10.1.5) | 3 |
| 5 | contracts | S5.3 conformance of every variant against the derived contract; minter resolution for every vault path (S8.3); provided-but-unconsumed facts (INFO) | 4 |
| 6 | graph | S8.4 cycles; waves; bindings to mocked services with data (S5.3.5); `requires`/`binds` local-name clashes (S6.4.4) | 4 |
| 7 | topology | S7.2.2 hosts exist; `local` uniqueness; addresses for every `address` network in `reach` (S7.6.4); placement (S7.6.3, S7.6.5); every resolution with data resolves (S7.8 step 5 refusals); proxy networks have `fqdn`, `realized_by`, and an `https` host-published endpoint; TLS networks have `pki` with `pki:issuer/<n>`; published (network, host_port) pairs unique per host (S7.4.5); push order consistent with cross-host edges (S17.2); `fqdn` present when required (S14.2.4) | 4 |
| 8 | identity | S4.3 uniqueness with the ambiguity message (S4.2.5); template `container_name`/`hostname` equal derived or absent; `ciu.*` labels (S4.5.2); compose keys qualified; name length (S4.3.3) | 4 |
| 9 | secrets | S10.8 | 4 |
| 10 | realness | S9.3.2; consistency with the layout's record (S9.4.2); joined references resolvable and level-consistent (S9.5.2, requires the reference's rendered file) | 4 |
| 11 | governance & resources | S13 keys and ranges; slice resolvable; `memory_min` headroom (WARN statically, ERROR at up); `exec` lane caps ≤ target governance (S16.6.4) | 3 |
| 12 | testing | S16: environments/requires/shared/binds resolve; a lane's `required_env` ⊆ its environment's **available environment** (S16.4.5: `forward_env` ∪ binding variables ∪ `CIU_*` ∪ the exec target's own variables) — ERROR for container environments, WARN for `host`; `sequence` members exist and are acyclic; every `host`, `ephemeral` and `exec` lane declares `memory_max` when its capacity object is finite (S16.6.1, S16.6.4); a `probe = "none"` binding only where S6.4 permits it and every other host-network `udp` binding refused (S8.5.2a); `[testing.judge]` present when an effective assay lane exists and its floor ≥ CIU's minimum (S16.3; an inherited floor without an assay lane is INFO); when the judge is reachable in an environment: `assay lanes --json` → every `assay_lane` exists, its `external_tools`/`argv0` are present in the environment, its `env_required` ⊆ the same available environment; `evidence_dir` ignored and writable; no inherited environment names `exec_in` (S3.1.5) | 3 |
| 13 | hooks | S12.4 `--validate` — only with `--validate-hooks` or inside `ciu up` (executes consumer code) | 4 |
| 14 | registry validator | `ciu.registry_validator` | 3 |
| 15 | live (`--live`) | S8.5 probes for the selected waves; cross-host reachability; joined references up; host ports free (S6.3.2); mount proof for `exec` environments (S16.4.3) | 7 |
In a zero-instance project (S16.11) only stages 1–3, 11, 12 and 14 run.

### S15.4 Output
Human-readable by default (grouped by stage, ERRORs first); `--json` emits the S18.4 envelope with `findings: [{stage, rule, severity, message, file, table, candidates}]` and the derived tables of S3.7 under `resolved`. `--graph` prints waves and edges (kind-annotated). `--gates` restricts to stage 12 plus the gate's own preconditions (S16.5). `--layout L` additionally prints the **publication table** (every endpoint published on every host of `L`, with the binding that caused it) and the **bundle table** (what `ciu push` would ship to each host, S17.3).

---

## S16 The gate — `ciu gate`

### S16.1 Model
A **lane** runs one command, one judge invocation, or a sequence of other lanes, in an **environment**, subject to **preconditions** (realness, service health, provenance), under **resource caps** enforced through cgroup v2 and an **admission** step, and produces a **LaneResult**. Lanes are declared in `ciu.toml`; assay lanes additionally name a lane in `assay.toml` (assay's own file and schema; CIU never parses it — it asks `assay lanes --json`, S16.7).

### S16.2 `[testing]`
`cgroup_slice` (slice name; default = `governance.cgroup_parent`), `evidence_dir` (directory for artifacts and verdicts, default `ciu-gate-evidence/` — gitignored; CIU verifies it is ignored), `history` (integer ≥ 1, default 20: complete runs and LaneResults kept per lane, S16.9.4). S16.2.1 **Inheritance** is `[ciu] inherit` (S3.1.5): `testing.environments`, `testing.judge` and `testing.cgroup_slice` are in its closed inheritable list, so a monorepo declares its shared tester image, judge floor and slice once; lanes, externals, `evidence_dir` and `history` are always the project's own. (draft.4's `[testing] inherit` is withdrawn, Appendix C.) S16.2.2 **Externals** — `[testing.externals.<name>]`: `url` (a literal, non-secret URL) **or** `env = { host = "<VAR>", port = "<VAR>", url = "<VAR>" }` (names of variables the caller must supply), plus `probe` (`tcp` default | `http:<path>` | `none`): a typed description of a test dependency the project does not deploy. A binding to `external.<name>` (S16.4.2) resolves to the URL or the variables' values; a missing variable is NOT_RUN/`external-missing`, a failed probe NOT_RUN/`external-down`.

### S16.3 `[testing.judge]`
`version` (`version-floor`; required when any effective lane has `kind = "assay"`; a floor declared or inherited (S3.1.5) by a project with no assay lane is permitted and unused — INFO, never an ERROR, so a monorepo root can carry the estate's floor for the children that judge). CIU itself has a **minimum judge** it can drive — the oldest assay whose CLI carries `lanes --json`, `--resume`, `--progress <path>` and `--require-judge-provenance`; CIU 8.0.0 declares `4.1.0` and `ciu version` prints it; a declared floor below it is an ERROR at stage 12 (`[S16.3] judge floor >=2.4 is below ciu's minimum 4.1.0`). S16.3.1 Before the first assay lane of a run in a given environment, CIU runs `assay --version` in that environment and refuses when the version does not satisfy the floor (`[S16.3] judge 4.0.2 < floor >=4.1`); the result is cached per environment image digest for the run. S16.3.2 Every verdict MUST carry `judge_provenance` (CIU passes `--require-judge-provenance`); a verdict without it is a lane ERROR. There is no opt-out.

### S16.4 `[testing.environments.<e>]`
`mode` (`ephemeral | exec | host`, required). `exec`: `exec_in` (LogicalService; the container is its variant service (S5.2) via the derived identity; NOT_RUN/`environment-down` when not healthy per S8.6.3). `ephemeral`: `image` (string) or `image_from` (LogicalService → its variant service's `image`); the container runs in the slice and, when an instance exists, on the instance network, under the **detached state machine** of run-gate R-15/R-17: `docker create` with a derived name (`<project>-<instance>-gate-<lane>-<run_id>`), `docker start`, logs followed on a separate stream into the run directory's `stdout.log` (S16.9.4), the exit status taken **only** from `docker wait`, evidence and artifacts collected into the run directory while the container still exists, and removal in a `finally` — never `--rm`, so a failed container's logs and files survive and a transport hiccup can never report a false status. `host`: a plain subprocess. Common: `forward_env` (list of env names allowed into the lane from CIU's environment), `extra_mounts` (list of `host:container[:mode]`), `workdir` (default: the checkout root inside the container), `binds.<local>` (bindings per S6.4 with `delivery = "env"` only — the variables are injected into the lane's process environment; `wait` is meaningless here and refused), `enabled`. S16.4.1 `host` is also available implicitly as an environment name when no environment table defines it. S16.4.2 In a zero-instance project (S16.11), `exec` environments are an ERROR, `ephemeral` containers attach to no network beyond Docker's default, and `binds` may target only **externals** (S16.2.2): `binds.<local> = { to = "external.<name>", delivery = "env", env_prefix = "…" }` — a typed, checked way for a library's integration lane to reach an externally managed database or API without owning an instance; LogicalService, instance-network, Vault and joined targets stay refused there. S16.4.3 **Mount proof.** Before running a lane in an `exec` environment, and before `ciu instance exec`, CIU MUST verify through `docker inspect` that the target container bind-mounts this checkout's `physical_repo_root` at `workdir`; a container that mounts another checkout (the primary's tree while a linked worktree is selected) → NOT_RUN/`environment-mismatch` naming both paths. S16.4.4 An environment provides a lane's dependency closure: an offline package cache (npm, Go modules) baked into the image or mounted via `extra_mounts`; assay's snapshot carries committed objects only, so a lane that needs generated dependencies rebuilds them offline from the committed lockfile or declares assay's `isolation.link_paths`. S16.4.5 The **available environment** of a lane is: `forward_env` ∪ the variables of the environment's `binds` ∪ the fixed `CIU_*` variables ∪ (for `exec`) the variables the target container carries from its own service's `env`-delivered bindings and secrets ∪ (for `host`) CIU's own process environment. A lane's `required_env` and an assay lane's `env_required` (from `assay lanes --json`, S16.7.1) MUST be subsets of that set — an ERROR at stage 12 for container environments (`[S16.4] lane sql-mutation needs SCHEMA_GATE_PG_DUMP; environment clean provides POSTGRES_HOST, POSTGRES_PORT, REDIS_HOST, REDIS_PORT`), a WARN for `host` (the set depends on the caller's shell).

### S16.5 `[testing.lanes.<l>]`
`kind` (`command | assay | sequence`, required), `environment` (name; required for `command` and `assay`; forbidden for `sequence`), `argv` (list, `command`; `{worktree}` is substituted with the checkout root as seen inside the environment), `assay_lane` (string, `assay`), `lanes` (list of lane names, `sequence`), `stop_on` (`FAIL` default | `never`, `sequence`), `description`, `clean_tree` (bool, default true — a dirty tree is NOT_RUN/`dirty-tree`; `--allow-dirty` overrides), `budget` (duration, enforced: the lane is killed and reported `BUDGET_EXCEEDED`), `required_env` (list; missing → NOT_RUN/`env-missing`), `artifacts` (list of paths copied into the run directory, S16.9.4), `requires = { realness = { <logical> = <level> }, healthy = [<logical>…] }`, `require_provenance` (bool, default false; S16.5.6), `resources = { <RK subset>…, shared = [<logical>…] }` (`memory_max` is required whenever the lane's capacity object is finite, S16.6.1), `enabled`. S16.5.1 `requires.realness` compares against the current layout's record (NOT_RUN/`realness-mismatch`). S16.5.2 `requires.healthy` requires each named service's variant service to be healthy per S8.6.3 (NOT_RUN/`service-down`). S16.5.3 `resources.shared` names LogicalServices whose Realizations the lane uses exclusively: lanes sharing a name serialize on an exclusive `flock` of the **stack directory** (`location`) of the owning Realization in the checkout that OWNS it (the reference checkout for `joined` Realizations) — a directory git tracks, whose inode no `git clean -x` unlinks — so worktrees sharing a database serialize against each other. There is no lock file. S16.5.4 A `sequence` lane runs its members in order **in the same process**, each with its own admission, preconditions and LaneResult; with `stop_on = "FAIL"` the first member whose outcome is not PASS ends the sequence; the sequence's outcome is PASS iff every member ran and passed, else the first non-PASS member's outcome; `--worktree`, `--base` and `--allow-dirty` apply to every member. A sequence MUST NOT contain itself transitively and MUST NOT name a lane of another project. S16.5.5 There is no `request_base` key: CIU passes `--request-base` exactly to lanes whose assay lane reports `base_source = "request"` in `assay lanes --json` (S16.7). S16.5.6 **Provenance.** For every service in `requires.healthy` and for an `exec` environment's target, CIU records in the LaneResult the running container's image revision (`org.opencontainers.image.revision`, stamped by `ciu build`, S17.6) against the checkout's `HEAD`; with `require_provenance = true` a mismatch or a `-dirty` revision is NOT_RUN/`provenance-mismatch`; without it, a mismatch is a WARN line in the result. S16.5.7 **An exec target is used by one lane at a time.** A lane in an `exec` environment holds, for its whole duration, the exclusive stack-directory lock (S16.5.3) of the Realization that owns `exec_in`'s variant service — the same lock `resources.shared` would take for that LogicalService and the same key `ciu lease acquire --realization` takes (S14.4.8) — so two gates that resolve to one container serialize on that container's owner, while gates against different instances' testers run concurrently; naming the target in `resources.shared` as well is redundant and merged silently. This is run-gate RG-39's mechanism, lifted, and the estate rule that mutual exclusion is a property of the container being judged. A project that wants two lanes inside one container at once has no way to say so — a container's working tree, temp directories and memory cap are one — and uses two `ephemeral` lanes or a `sequence` instead.

### S16.6 Admission and caps
S16.6.1 **Admission is a transaction keyed by the capacity object.** The capacity object of an `ephemeral` or `host` lane is the slice's cgroup directory (`<cgroupfs>/<slice>/`). CIU takes an exclusive `flock` on that directory — host-visible, stable, existing exactly when the slice does, and independent of every project, worktree or evidence-directory override — and, while holding it, reads the slice's `memory.max`, sums the `memory_max` of every live reservation in the ledger `${XDG_RUNTIME_DIR:-/tmp/ciu-<uid>}/admission/<sha256 of the cgroup path>/<pid>-<started>.json` (a reservation whose pid no longer exists, or whose recorded start time differs from the pid's, is stale and removed), and either writes its own reservation and releases the lock, or releases it and waits up to `--admission-wait` (default 10 m) before retrying; exhausted → NOT_RUN/`no-headroom`. A lane admitted against a finite slice MUST declare `memory_max` — stage 12 refuses one that does not (`[S16.6] lane unit declares no memory_max; slice dev-background.slice is finite (4G)`) — and an absent value is never treated as zero: there is no reservation without a declared size. The reservation is removed in a `finally`. Two lanes can therefore never both admit against the same headroom, whatever evidence directory or checkout they run from. The ledger is per uid: CIU does not sum gates of different users — the kernel's `memory.max` still bounds them together, so over-admission across users is bounded, not silent. A slice whose `memory.max` is `max` admits everything (admission is then a record, not a gate). S16.6.2 `ephemeral` lanes run with `--cgroup-parent <slice>` and their caps written to their cgroup before the process starts; `host` lanes run in a child cgroup of the slice. S16.6.3 `CIU_GATE_CGROUPFS_ROOT` overrides the cgroupfs mount for tests. S16.6.4 `exec` lanes run inside the target container's cgroup, and CIU cannot cap a single exec process below the container: their `resources` are **requested admission values only** — `memory_max` required whenever the container's `memory.max` is finite, validated to be ≤ the container's effective governance (S13.4; `[S16.6] lane 'unit' asks memory_max 3G but 'tester' is capped at 800M`) and reported as `resources_requested` with `resources_applied = null` (S16.9); because an exec target is used by one lane at a time (S16.5.7) there is no reservation ledger for containers — the static cap check is the whole admission, and two 3 G requests against one 4 G tester cannot run together because the second waits for the first; a lane that needs an enforced cap of its own uses an `ephemeral` environment. S16.6.5 A lane's LaneResult, `last` pointer and history are written under an exclusive `flock` of `evidence_dir/<lane>/` (temp file + rename), so two invocations of one lane never interleave their writes or their pruning; their run outputs never share a path at all (S16.9.4).

### S16.7 Assay invocation
S16.7.1 CIU's only interface to assay's lane file is `assay lanes --json --file assay.toml`, run in the lane's environment: it yields lane names, `base_source`, `external_tools`, `argv0`, `env_required`, `budget`. CIU MUST NOT parse `assay.toml` itself. S16.7.2 A lane runs `assay run <assay_lane> --file assay.toml --require-judge-provenance --verdict-json <run_dir>/verdict.json --resume --progress <run_dir>/progress.jsonl [--request-base <REF>]`, executed inside the environment with the checkout mounted at `workdir`, where `<run_dir>` is this invocation's run directory (S16.9.4). `--resume` is always passed: assay resumes by itself from its own state under the judged tree's gitignored `.assay/mutation-state/`, keyed by candidate content (assay `docs/CONSUMERS.md`), which CIU neither reads nor manages — a CIU run directory has nothing to do with it. `--progress` is assay's append-only telemetry stream (run-gate R-38); CIU points it at the run directory — a gitignored path outside the judged content that no concurrent invocation shares — rather than at the estate's run-gate convention `.assay/progress-<lane>.jsonl` (root `AGENTS.md`), because CIU creates no hidden directory (S2.3.3) and two invocations of one lane must never share a progress file; the LaneResult's `progress_path` is where a reader finds it. `--request-base` is passed exactly when `base_source = "request"`; `REF` is `--base` if given, else the merge-base of `HEAD` and the checkout's upstream branch (`[S16.7] no upstream; pass --base`). S16.7.3 The lane's outcome is the verdict's `outcome`; `judge_provenance`, `helpers` (when present) and the resolved `REF` are copied into the LaneResult verbatim.

### S16.8 Outcome vocabulary
`PASS`, `FAIL`, `ERROR`, `NOT_RUN` (with reason ∈ `realness-mismatch | service-down | environment-down | environment-mismatch | env-missing | external-missing | external-down | dirty-tree | no-headroom | judge-floor | judge-provenance | provenance-mismatch`), `BUDGET_EXCEEDED`. The reason vocabulary is closed and part of the S3.8.4 definition: a rule that adds a reason adds it here (`external-missing`/`external-down` are S16.2.2's). Exit codes of `ciu gate`: PASS 0, FAIL 1, ERROR 2, NOT_RUN 3, BUDGET_EXCEEDED 4 — the gate's table, distinct from S18.1.

### S16.9 LaneResult — `ciu.gate.<lane>.json`
`{ api: "ciu/lane-result", api_version: 1, operation: "gate", status: "ok" | "findings" | "error", lane, kind, environment, project, instance_id?, layout?, run_id, run_dir, started, ended, duration_s, outcome, reason?, exit_code, budget, resources_requested: {...}, resources_applied: {...} | null, members?: [<lane names>], request_base?, judge_provenance?, helpers?, verdict_path?, progress_path?, artifacts: [...], preconditions: {...}, provenance: { <service>: { image_revision, head, match } } }`. `status` is derived from `outcome`: `PASS` → `ok`; `FAIL`, `NOT_RUN`, `BUDGET_EXCEEDED` → `findings`; `ERROR` → `error` (S18.4). `resources_applied` carries the cgroup values read back after placement (S16.6.2) and is `null` for `exec` lanes, whose caps are requested only (S16.6.4). S16.9.1 The file is the last result; the same document is appended under `evidence_dir/<lane>/history/<run_id>.json`, pruned to `testing.history`. S16.9.2 `ciu gate --list` shows, per lane, the last outcome, duration and the median duration over the history. S16.9.3 A LaneResult and every artifact written for a non-PASS outcome are mode 0600 (evidence may contain secrets echoed by a failing test). S16.9.4 **Run directories.** Every invocation of a lane has a `run_id` of 32 random hex characters (128 bits) whose directory `<evidence_dir>/<lane>/runs/<run_id>/` CIU creates with an exclusive `mkdir` (an existing directory is a collision: a new id is drawn, never reused) and writes everything it produces there — `stdout.log`, `verdict.json`, `progress.jsonl`, copied `artifacts/`, the container's collected files, and `run.json` (`api = "ciu/run"`, `api_version = 1`: project, instance, lane, the digest of the lane's effective definition, `HEAD`, request base, environment image id, argv or assay lane, judge version) — so nothing is written to a path two invocations could share and every run states what it ran. On completion, under the lane lock (S16.6.5), the LaneResult (carrying `run_id` and `run_dir`) is written to `ciu.gate.<lane>.json` and appended to `history/`, the `<evidence_dir>/<lane>/last` symlink is replaced atomically, and `runs/` is pruned to the `testing.history` most recent **complete** run directories (a directory whose owning pid is alive is never pruned). There is no `ciu gate --resume`: every invocation is a fresh run, and the judge's own resumption (S16.7.2) does not depend on CIU's directories.

### S16.10 CLI
`ciu gate [<lane>…] [--list] [--dry-run] [--json] [--base REF] [--allow-dirty] [--check-env] [--worktree PATH] [--admission-wait D]`; `--worktree` runs against another checkout's instance (its instance file decides identity; the path charset rule of S16.12 applies); `--check-env` reports the environment/precondition state without running; `ciu gate doctor` reports the slice, cgroupfs writability, docker reachability, judge version and provenance status per environment.

### S16.11 Zero-instance projects
S16.11.1 A project whose declarations contain no `[realization]` table is a **zero-instance project**: it has no instance, no lock, no instance file, no generated file and no rendered file; `[service]`, `[network]`, `[bundles]`, `[layouts]`, `[realness]` and `[vault]` are an ERROR if present. `[governance]` MAY be declared in full: it governs no container here, but its `cgroup_parent` is the gate slice default (S16.2) and the whole table is what projects that name this file in `[ciu] inherit` receive (S3.1.5) — a monorepo root is typically exactly this: a zero-instance `ciu.toml` carrying the shared governance, tester environment and judge floor. S16.11.2 `ciu gate` reads `ciu.toml` (and `ciu.site.toml`) directly, runs `ephemeral` and `host` lanes, writes LaneResults and evidence, and needs `docker` only when an `ephemeral` environment is used and `assay` only in the environment of an assay lane (S18.3). S16.11.3 `ciu check` runs stages 1–3, 11, 12 and 14 (S15.3). S16.11.4 The minimal such project is `[project] name = "x" revision = 8` plus `[testing]` (Appendix B).

### S16.12 Rules lifted from run-gate (kept by reference)
- **Path charset** (RG R-04): a checkout path (or `--worktree` path) containing characters outside `[A-Za-z0-9._/-]` is refused before any container is started.
- **Git isolation** (RG R-19a): container lanes run with `GIT_CONFIG_SYSTEM=/dev/null` and `GIT_CONFIG_GLOBAL` pointing at a **private writable file** CIU creates per run (containing `safe.directory = *`, so a uid-mismatched checkout is not refused), unless `forward_env` names them.
- **Dual mount** (RG R-23): when the checkout's `physical_repo_root` differs from the path under which the lane container sees it (`workdir`), CIU bind-mounts the checkout at **both** paths, because a git worktree's `.git` file may name either namespace; an `extra_mounts` entry whose container path is `workdir`, the physical root, or a parent of either is an ERROR.
- **Detached execution** (RG R-15/R-17): S16.4's state machine.
- **Evidence on failure** (RG R-26): S16.9.3.
- **History** (RG R-36): S16.9.1–S16.9.2.
- **Override reachability** (RG R-25): moot — a sequence lane's members run in-process with the parent's flags (S16.5.4).
- **Container derivation from `ciu.global.toml`** (RG R-14a): retired — `exec_in` names a LogicalService; identity is derived (S4).

---

## S17 Remote deployment

### S17.1 Host inventory lookup
`ciu.hosts.toml` in the checkout root, else `CIU_HOSTS_FILE`, else `~/.config/ciu/hosts.toml`; the first found is used entirely (no merging).

### S17.2 Static checks per layout
For `--layout L`: every host named exists; addresses cover `reach`; every binding resolution with data resolves (S7.8); host declaration order is consistent with the cross-host init graph — a host whose bundles have an edge into a host declared after it is an ERROR (`[S17.2] rs1002 needs core on gstammtisch, declared later`).

### S17.3 Push: releases, manifests, and what travels
S17.3.1 **Release.** `ciu push --layout L [--host H] [--images auto|registry|archive|none]` builds, per target host `H`, a **release**: the closure of files the host needs, computed mechanically — every declaration file, the flattened inherited policy `ciu.inherited.toml` when `[ciu] inherit` is set (S3.1.5), `ciu.hosts.toml`, `ciu.instance.toml`, `ciu.instance.generated.toml` (immutable by construction: identity and build facts only, S14.2), and, for every stack directory placed on `H`, **every file under it** except those the S2.3 gitignore list names (a stack directory holds committed inputs — templates, hooks, config-file templates, init scripts, seeds — and CIU's own outputs, and the list is what separates them), plus every path a hook entry declares in `inputs` (S6.10). No state-root file (S2.6) is ever part of a release: the host file, the store, the record, the data and the state live on each host. Hooks are arbitrary programs, so nothing outside a stack directory travels unless declared: a hook that reads `fixtures/<host>.json` without an `inputs` entry fails on the target, and no static check can know that — the declared closure is the contract. A **manifest** `ciu.release.json` (`api = "ciu/release"`, `api_version = 1`) lists every file with its SHA-256 and mode, the images of the Realizations placed on `H` per S17.3.6, the layout, the instance id, and the git revision. The release digest is the SHA-256 of the manifest. `bundle_excludes` MAY subtract only files outside the closure; an exclusion that intersects it is an ERROR (`[S17.3] bundle_excludes drops infra/db-core/init-scripts, under stack infra/db-core`). S17.3.2 **Secrets capsule.** Alongside the release the sender builds, per host, a **capsule** `ciu.secrets.transport.toml` (0600) whose entries are derived per source for every secret of a Realization placed on `H` (stack-level and derived TLS secrets included): `from = "generate", store = "local"` and `from = "ask"` → the stored value, **materialized on the sender first when absent** (`generate`: generated and stored now; `ask`: `CIU_SECRET_<var>`, else a prompt, then stored), so the first push of a Realization the sender never ran itself still carries a value; `from = "file"` → the file is **read now on the sender** into the capsule (the store never held it); `from = "host"` → the entry `[secrets.hosts.<H>.<entry>]`, materialized on the CIU host first (S10.4); `from = "hook"` → the stored value if the sender's hook produced it, else nothing (the target's hook produces it); `from = "ephemeral"` and `delivery = "native"` → nothing; `from = "vault"` / `store = "vault"` → nothing when `H` has a derived resolution for the pseudo-consumer `ciu@H` to the `vault` LogicalService (the target fetches, S10.3), else the sender fetches the value **now** and puts it in the capsule — if the sender cannot reach Vault either, `push` refuses naming the host, the key and both failed reachabilities (`[S17.3] rs1002 has no route to vault and this host cannot reach it: db/postgres/controller_password cannot travel`). `ciu check --layout L` prints, per host, every capsule entry and why it travels (S15.4). S17.3.3 **Transfer.** Release and capsule are transferred with `push_mode` to `<bundle_dir>/releases/<digest>.staging/` on the target, in layout order; the target verifies every manifest entry (size, SHA-256, mode) and the manifest's own digest, refuses on any mismatch, renames the directory to `<bundle_dir>/releases/<digest>/` (atomic), imports the capsule into the state root's store (S2.6, S10.6) with `source = "transport:<from>[:<detail>]"` and `created` = the sender's timestamp — an imported entry is **never refreshed** on the target (S10.6.4 applies only to entries this host resolved itself) and is replaced only by the next capsule — loads or pulls the release's images and verifies them (S17.3.6), and finally replaces the symlink `<bundle_dir>/candidate` with the release directory (atomic rename): the one pointer `activate apply` reads. An interrupted transfer leaves a `.staging` directory that the next push overwrites; `current` and `previous` are never touched by `push`. S17.3.4 The instance file and generated file travel so that `instance_id` and `owner_id` are shared; the host file is written by `prepare` into the target's state root (S14.2.3) and the release directory stays byte-identical to its manifest for its whole life. S17.3.5 `ciu push --prune` removes staged releases and every release that is neither `candidate`, `current` nor `previous` on the target. S17.3.6 **Images travel by digest or by archive, never by name alone.** The manifest records, per image of the Realizations placed on `H`, the reference, the image id (content digest) and, when a registry is configured (`project.registry.url` non-empty, S3.4.2), the repository digest. `--images registry` (the default when a registry is configured): `ciu push` pushes every project-built image (S17.6) to the registry and the target pulls `<reference>@<digest>`; the compose rendered on the target references the digest. `--images archive` (the default when `url = ""`): the sender exports the project-built images with `docker save` into `<release>/images/<name>.tar`, listed in the manifest with their SHA-256; the target runs `docker load` during staging, verifies that the loaded image id equals the manifest's `expected_id`, and tags that id with a **release-unique immutable reference** `<name>:ciu-<first 12 hex of the release digest>` which the compose rendered on the target references — never the mutable tag the sender used, which another build could retarget between staging and apply. Vendor images (no `build`) are recorded by repository digest and pulled by digest on the target in both modes. `--images none` ships no image bytes and records the omission; before creating any container `activate apply` inspects each reference on the target and refuses unless its id equals the manifest's `expected_id` (`[S17.3] dstdns/controller:latest on rs1002 is sha256:b…, the release expects sha256:a…`) — presence of a tag is never enough. A `docker_optional` host (S7.2) receives no images. Every decision here reads the reference-level image map of S17.6.1, never a per-service guess.

### S17.4 Activate: atomic switch, receipts, rollback
`ciu activate --layout L [--host H] <plan | bootstrap | apply [--release <digest>] | health | rollback>` runs on every host of the layout over SSH, in layout order, stopping at the first failure. S17.4.1 **The activation manifest and the switch are CIU's.** `plan` writes `ciu.activation.json` (`api = "ciu/activation"`, `api_version = 1`) from the sender's files: a fresh `activation_id` (a UUID), `instance_id`, `layout`, `created`, and per host of the layout an expected entry `{ release_digest, selection }` — the digest of the release `push` builds (or would build) for that host and that host's selection (S9.3) — so that every host can check every other host's evidence against what the activation intended, never against itself. `apply` runs `plan`, then, per host: **(1)** the host's optional `[activate] bootstrap` command in `<bundle_dir>` (installing prerequisites; no release is needed); **(2)** `prepare`, CIU's own: `ciu instance init --host <H>` in the target release — the release `<bundle_dir>/candidate` points to, or the staged release `--release <digest>` names; neither present → `[S17.4] rs1002 has no candidate release; push first` — which writes the host file into the state root (S14.2, S2.6) and proves the release directory's physical path (S4.1.4), then `ciu check --layout L` in that release; **(3)** the host's `[activate] apply` command in that release with `--activation-manifest <file> --receipts <dir>` appended (S17.4.3); **(4)** the host's `[activate] health` command in that release; **(5)** the receipt is fetched, and only then CIU — never a host command — atomically switches `<bundle_dir>/current` to the release, records the former target as `<bundle_dir>/previous` and removes `candidate`. Any failure leaves the pointers unchanged; CIU reports what ran and what is running (a failed `apply` may have replaced containers, since a compose project is per instance, not per release) and never compensates on its own — `ciu activate apply --release <digest of current>` re-applies the current release, `rollback` the previous one. The host's commands are hooks around this state machine, not alternative definitions of it. `bootstrap` and `health` as stand-alone verbs run in `<bundle_dir>` and in `current`. `rollback` refuses when `previous` is absent (`[S17.4] no previous release on rs1002`), otherwise performs steps (2)–(5) for the `previous` release under a fresh activation manifest — prepare, apply, health, receipt, and only then the pointer swap: the previous release deploys itself, and a rollback that fails halfway leaves `current` where it was. There is no host-declared rollback command, and a bare `ciu down` is never a rollback. S17.4.2 Render-on-target: each host renders its own `ciu.resolved.toml` from the release with its state root's `[ciu.host].name = H`, so derived resolutions and publications are computed per host. S17.4.3 **Receipts.** A successful `ciu up` writes `ciu.receipt.json` (`api = "ciu/receipt"`, `api_version = 1`) into the state root. Its **subject** is the canonical JSON (sorted keys, no whitespace) of `{ activation_id, instance_id, layout, host, release_digest, selection }` and `subject_digest` its SHA-256: `host` is the host that produced it; `release_digest` is the manifest digest of the release it ran from — or, for a hand-started `up` in a checkout, the digest of the release `push` would build for this host from these files (`ciu activate plan` computes exactly that, S17.3.1's closure included, so hooks, templates and seeds are covered); `selection` is `[resolved.services]`; `activation_id` is the id of the manifest passed as `--activation-manifest` (absent when none was passed, and an absent id never matches anything). Nothing time- or host-local — `rendered_at`, uids, hostnames, physical paths, read-backs — enters the subject. The body carries, per enabled service brought up on this host, `{ state, container_id, image_digest, started_at }` (the container incarnation the evidence refers to); per completed `one_shot`, `{ container_id, exit_code, finished_at }`; per fact probed true (S8.5.2, S8.5.5), `{ fact, provider_service, observed_at }`; every socket claim published; the `probes` and `assumed` lists (S8.5.4); and `created`. `ciu activate apply` fetches the receipt after each host and passes the manifest and the receipts collected so far to the next host as `--activation-manifest <file> --receipts <dir>`; a hand-started multi-host `up` gets the same two inputs by hand (`ciu activate plan` on the sender, the receipts copied between hosts). S17.4.4 A receipt from provider host `P` is **valid** for a consumer when an activation manifest was given; the receipt's `api_version` is known; its `activation_id` is present and equals the manifest's; its `instance_id` and `layout` equal the manifest's; its `host` is `P`; and its `release_digest` and `selection` equal the manifest's entry **for `P`** — the consumer never compares a provider's digest, host or selection with its own, because every host runs its own release. The receipt is then usable for exactly the facts and completions it lists with their provider services. A receipt from another activation is stale by definition, whatever its timestamp, because the provider may have been reset and restarted from the same declarations since. An invalid receipt is refused naming the first failing check, and every fact it carried is treated as absent (S8.5.3, reason `invalid-receipt`). Receipts are unsigned: they travel over the authenticated SSH channel of the activation, and a pinned host key authenticates the source (operator decision, response 2026-09-03 §3 D).

### S17.5 Cross-host runtime and evidence
CIU on a host deploys only the Realizations placed on that host (S7.7). For a provider on another host it accepts a fact or a completed one-shot **only** from a valid receipt (S17.4.4) that lists it; reachability of the derived resolution is still probed (S8.5.3). A required remote fact or completion with no valid receipt entry — no activation manifest (`no-manifest`), no receipt for the provider host (`no-receipt`), a receipt that fails S17.4.4 (`invalid-receipt`), or one that does not list the fact (`not-in-receipt`) — is an ERROR by default (`[S17.5] pg:role/controller on gstammtisch: no receipt for activation 5d1c…`). `ciu up --allow-assumed` accepts reachability alone for that run, records every such fact as `assumed` with its reason in `[resolved.gates.<k>]` and in its own receipt (so the next host can see the hole in the evidence chain), and WARNs per fact. draft.4's opt-in `--require-receipts` is withdrawn: the strict behaviour is the default and the escape is the explicit flag. A host never opens SSH or `docker exec` to another host during `up`.

### S17.6 Build provenance
S17.6.1 **The image map comes first.** Before any per-service decision, CIU builds one reference-level table `[resolved.images.<reference>] ownership = "project" | "vendor", build_owner = "<R>.<svc>" (project only), expected_id, repository_digest?` over every `image` reference of the deploy set: a reference is `project` when exactly one service of the deploy set declares `build` for it (`build_owner`), `vendor` when none does; two differing `build` tables for one reference are an ERROR (S6.2). Every service that names a reference reads that row — there is no per-service "without `build` = pulled" classification (round-3 review T3-07). `ciu build` builds every `project` reference from its owner's context, stamps the image with `org.opencontainers.image.revision` = the git commit (`-dirty` appended when the tree is dirty) and `org.opencontainers.image.created`, tags it as the reference, records its id as `expected_id`, and records `[ciu.instance.build]` (`build_version` = `<date>-<git short sha>`, `build_time`, the map's ids and digests); every `vendor` reference is pulled and recorded by repository digest, never built. An `image` that no service builds but whose reference carries `project.registry.namespace` is a WARN (`[S17.6] dstdns/controller:latest is not built by any service; is a build table missing?`). `ciu provenance [--json]` compares every running container of the instance against `HEAD` (the v7 S17 verdict vocabulary `match | dirty | mismatch | vendor | unstamped`).

---

## S18 Command line

| verb | class | notes / v7 disposition |
|---|---|---|
| `ciu init [--project NAME] [--stack DIR (--image REF \| --from-compose FILE) [--service NAME]] [--test-argv ARG…]` | lock-free | scaffold (S19); v7 S19 kept |
| `ciu instance init [--host H] [--move \| --fresh] [--layout L] [--bundles …] [--label X]` | mutating | S14.1, S14.2 (replaces `ciu env generate`); `--move` is a cold move, `--fresh` discards a copied identity (S4.1.2) |
| `ciu instance list \| show [<label>] \| add --join <ref> --services … \| remove <label> \| reap \| lease --extend [D] \| --perpetual \| --release \| exec --env <e> -- <cmd> \| backup [--to DIR] [--data] \| restore --from DIR [--force] [--move]` | list/show/exec/backup read-only; others mutating | S14.6, S14.7, S9.5.5, S14.8 (`backup`/`restore`); `show` prints the state root (S2.6); `ciu worktree …` accepted as an alias for one release |
| `ciu lease acquire (--exclusive \| --shared) [--realization r] [--purpose TEXT] [--wait[=D]] -- <cmd> \| status [--json] \| wait [--realization r] [--timeout D]` | `acquire --exclusive` mutating; `acquire --shared` gate; `status`/`wait` read-only | S14.4.7–S14.4.8: the two canonical lock keys as a verb |
| `ciu query identity \| binding \| publications \| capability \| lanes … [--json]` | read-only | S3.7.6 |
| `ciu check …` | read-only | S15 |
| `ciu render [--layout L] [--host H] [--show-injected]` | mutating | S11.7 |
| `ciu up [--layout L] [--host H] [--bundles b,…] [--realness s=l,…] [--wait[=D]] [--probe-external] [--activation-manifest FILE] [--receipts DIR] [--allow-assumed] [--no-check]` | mutating | S8, S9; `--activation-manifest`/`--receipts`/`--allow-assumed` per S17.4–S17.5 |
| `ciu dev --realization r` | mutating | render + `compose up` of one Realization without waiting on its wave gate; refuses when a provider of an edge from `r` is not up (v7 S5a's dev loop) |
| `ciu down [--realization r]` | mutating | S14.1.3 |
| `ciu clean [--vanilla]` | mutating | S14.1.4 |
| `ciu status [--live] [--json]` | read-only | instance summary: layout, selection, waves, container state by label; `--live` runs the S8.5 predicates (absorbs v7 `health` and `status`) |
| `ciu show bundles \| layouts \| services \| realizations \| effective [--json]` | read-only | listings from the merged configuration (absorbs v7 `profiles`, `layouts`); `effective` prints the whole merged configuration with inherited tables marked by source (S3.1.5; replaces draft.4's `ciu testing flatten`) |
| `ciu gate …` | gate | S16.10 |
| `ciu secrets show [--values] \| reset <sel> \| host <h> <entry> \| rotate-bootstrap` | show read-only; others mutating | S10.6 (absorbs v7 `host-secrets`) |
| `ciu env print` | read-only | prints `export`-lines for `instance.*`, `host.*` facts and the instance network; the only producer of `ciu.env` |
| `ciu build [--realization r]` | mutating | S17.6 (v7 `bake`) |
| `ciu push [--layout L] [--host H] [--images auto\|registry\|archive\|none] [--prune]` | mutating | S17.3 |
| `ciu activate --layout L [--host H] plan \| bootstrap \| apply [--release DIGEST] \| health \| rollback` | `plan` read-only; others mutating | S17.4; `plan` writes the activation manifest, `apply` runs prepare → apply → health → receipt → switch per host |
| `ciu ssh <host> [-- <cmd>]` | read-only (no lock) | opens a shell or runs a command on an inventory host with its declared SSH facts (v7 S14.1) |
| `ciu provenance [--json]` | read-only | S17.6 (v7 S17.2) |
| `ciu diagnose [--json]` | read-only | container/log/health summary by labels |
| `ciu governance ksm \| iops-baseline …` | mutating | v7 `ksm`, `iops-baseline` under one verb |
| `ciu migrate [--check] [--secrets] [--hostdirs] [--config]` | mutating | Appendix A; `--check` reports what a migration would change (v7 `migration-check`) |
| `ciu hook run <realization> <phase> [--validate] [--dry-run]` | read-only for `--validate`/`--dry-run`, else mutating | S12.5.3 |
| `ciu schema [--json] [--file ciu.toml\|ciu.stack.toml\|…]` | lock-free | S3.8.4 (absorbs v7 `capabilities`) |
| `ciu doctor [--json]` | lock-free | environment report: docker, compose, cgroup v2, git, judge per environment, hookkit importable, filesystem lock support (S14.4.6) |
| `ciu version` | lock-free | prints the tool version, the `api_version` it implements per artifact (S18.4) and the minimum judge it can drive (S16.3) |
Global flags: `--json` where listed, `--layout`/`--host` default from the instance file and generated file, `--realization r` restricts a verb to one Realization, `--root DIR` (S1.5).

### S18.1 Exit codes
0 success; 1 runtime failure; 2 configuration or usage refusal (findings printed; `ciu check` policy); 3 environment bootstrap failure (a required tool or the daemon is missing, S18.3); 4 lock contention; 5 remote failure. Meanings 0–3 are v7 S10.3's. `ciu gate` uses S16.8's table.

### S18.2 Environment variables CIU reads
| variable | meaning |
|---|---|
| `CIU_EXIT_ON` | ambient fallback for `ciu.exit_on` (config wins) |
| `CIU_MAX_CONCURRENT_INSTANCES` | ambient budget override never written to a file (S14.6) |
| `CIU_SECRET_<VAR>` | `from = "ask"` non-interactive input (S10.1) |
| `VAULT_TOKEN` | Vault token source #1 (S10.3.3) |
| `CGROUP_PARENT_DEV_BACKGROUND` | governance parent / gate slice when `cgroup_parent = ""` (S13.2) |
| `CIU_HOSTS_FILE` | host inventory path (S17.1) |
| `CIU_SSH_TRANSPORT` | `openssh` (default) or `paramiko` for push/activate/ssh |
| `CIU_SSH_INSECURE_TOFU` | `1` = accept an unknown host key once (S7.2) |
| `CIU_KSM` | `off` disables KSM for one run (S13.3.1) |
| `CIU_GOV_BASELINE_PATH` | overrides `governance.baseline_path` (S13.2) |
| `CIU_SKIP_DOOD_PREFLIGHT` | `1` = skip the docker-outside-of-docker mount check (tests only) |
| `CIU_GATE_EXTRA_MOUNTS` | additional `host:container[:mode]` mounts for every container lane (S16.4), comma-separated |
| `CIU_GATE_MOUNT_ALIAS` | `host=container` path alias applied to `{worktree}` substitution when the checkout is mounted under a different path in the lane container |
| `CIU_GATE_EVIDENCE_DIR` | overrides `testing.evidence_dir` (S16.2) |
| `CIU_GATE_CGROUPFS_ROOT` | overrides the cgroupfs mount (S16.6.3) |
| `XDG_RUNTIME_DIR` | root of the admission ledger (S16.6.1); `/tmp/ciu-<uid>` when unset |
| `XDG_STATE_HOME` | root of the default backup destination (S14.8.1); `~/.local/state` when unset |
| `CIU_LEASE_FDS` | set by `ciu lease` for the command it runs: the held lock descriptors by key (S14.4.8); read only to reuse an inherited lock |
| `NO_COLOR`, `TERM` | output styling |
| `CIU_LOG_PREFIX_TIME_SHORT` | `1` = short timestamps in log prefixes |
| `HOSTNAME`, `REMOTE_CONTAINERS`, `WORKSPACE_DIR`, `GITHUB_ACTIONS`, `USER` | environment detection during `instance init` only (S14.2) |
No other variable influences behavior; none is a configuration source. There is no `CIU_SKIP_DEPENDENCY_CHECK`: preflights are per need (S18.3).

### S18.3 Preflights (per verb, per need — never at import time)
`docker` and `docker compose`: required by `up`, `down`, `clean`, `dev`, `build`, `status --live`, `diagnose`, `instance exec`, `instance reap`, and by `gate` only for lanes whose environment is `ephemeral` or `exec` (checked when the lane is admitted, reported as NOT_RUN/`environment-down` naming the missing binary). `git`: required by the instance registry (`instance init` on a git checkout), `clean_tree`, `--base` and provenance; a non-git checkout is an instance without a registry record (budget and leases do not apply). `assay`: required only inside the environment of an assay lane (S16.3.1). `ssh`/`rsync`: `push`, `activate`, `ssh`. Vault reachability: `up`/`render` only when a vault-sourced secret must be materialized. A missing prerequisite exits 3 naming the verb and the binary.

### S18.4 JSON envelope
Every machine artifact carries a two-field header naming **which** schema it follows and that schema's own version: `api` (`ciu/command-result` for every `--json` verb output, `ciu/lane-result` (S16.9), `ciu/resolved` (S3.7.1), `ciu/hook-context` and `ciu/hook-output` (S12), `ciu/receipt` (S17.4.3), `ciu/activation` (S17.4.1), `ciu/release` (S17.3.1), `ciu/instance-record` (S14.7), `ciu/lease-record` (S14.4.8), `ciu/run` (S16.9.4), `ciu/backup` (S14.8.1)) and `api_version` (an integer per `api`, starting at 1). `--json` verb outputs and LaneResults additionally carry `operation` and `status: "ok" | "findings" | "error"`. Compatibility policy: adding an optional field is not a version bump and readers MUST ignore unknown fields; removing or re-typing a field, or changing a vocabulary, bumps that artifact's `api_version` alone. A reader MUST refuse an `api` it does not know or an `api_version` above the one it implements, naming both.

---

## S19 Scaffolding — `ciu init`
S19.1 Bare `ciu init [--project NAME]` in a directory without `ciu.toml` writes a **zero-instance project** (S16.11): `ciu.toml` with `[project] name = "<NAME or directory basename>"`, `revision = 8`, and a `[testing]` skeleton containing one `host` lane (`[testing.lanes.unit] kind = "command" environment = "host" argv = [...]` — the `--test-argv ARG…` arguments when given, else a commented placeholder the author replaces); plus the S2.3 gitignore patterns (appended to `.gitignore`, comment-normalized, idempotent). No `[realness]`, `[layouts]`, `[bundles]` or `ciu.hosts.toml` are written, because without a Realization they would be errors (S16.11.1). S19.2 `ciu init --stack DIR (--image REF | --from-compose FILE) [--service NAME]` writes a **deployable project**: everything of S19.1 plus `[realness] default = "live"`, `[layouts.local] hosts.localhost = { bundles = ["all"], reach = ["instance"] }`, `[bundles.all] services = ["<name>"]`, `[service.<name>] live = "<name>"`, `[realization.<name>] kind = "ciu_stack" location = "<DIR>"`, `ciu.hosts.toml` with `[hosts.localhost] local = true`, `<DIR>/ciu.stack.toml` and `<DIR>/ciu.compose.yml.j2`. With `--image REF` the stack has one service `<name>` (default: the directory basename normalized to `name`; an unnormalizable basename is an ERROR) with that image and nothing else; with `--from-compose FILE` one `[ciu_stack.<svc>]` per compose service with its `image` copied and every prohibited stanza of S11.3 moved into a comment. Endpoints, healthchecks, bindings and lanes are the author's to add: `init` invents no facts (P3). S19.3 Every file `init` writes MUST pass `ciu check` (stages 1–3 and, for a deployable project, 4–8) before `init` returns 0; a scaffold that would not pass is a CIU defect. S19.4 `init` refuses to overwrite an existing declaration file (`[S19] ciu.toml exists`). Nothing is implicit: what `init` writes is what the project runs on.

---

## S20 Refusal catalogue (normative identifiers)
Every refusal is `[S<n>.<m>] <message>`; the message MUST name the offending file/table/key and, where a name failed to resolve, the closest candidates. Implementations MUST keep the rule identifier stable across releases; a rule that is withdrawn keeps its number retired. The catalogue is the set of `[S…]` identifiers in this document; `ciu check --json` emits `rule` for each finding so tests can assert on it.

---

## Appendix A — Adopting v8 from a v7 checkout (`ciu migrate`)
`ciu migrate --check` reports every step below without writing; `ciu migrate` performs the mechanical ones and prints the rest.
1. **Instance:** `ciu instance init --layout local` in every checkout (primary included); it writes the generated file and `ciu.instance.toml`; `ciu.env` becomes an export. Cockpit aliases switch to `eval "$(ciu env print)"`.
2. **Declaration files** (`--config`): `ciu.global.defaults.toml.j2` → `ciu.toml`, `ciu.global.toml.j2` → `ciu.site.toml`, `ciu.global.instance.toml.j2` → `ciu.instance.toml`, `<stack>/ciu.defaults.toml.j2` (+ `ciu.toml.j2`) → `<stack>/ciu.stack.toml`. Jinja in a declaration is resolved where it is data (`{% set %}` constants are inlined; `{{ vault.paths.x }}` becomes `path = "x"`) and reported as a residue where it is control flow (a `{% for %}` generating tables is expanded once and flagged). `revision = 8`; `[deploy]` → `[project]`; `[deploy.profiles]` → `[bundles]`; `[deploy.layouts]` → `[layouts]` (`reach = ["instance"]` added); `[deploy.realness]` → `[realness]`; remove `deploy.environment_tag`, `deploy.network_name`, `deploy.environment`, `deploy.labels`, `deploy.env.defaults`, `[deploy.phases]`, `[topology.*]`, `[service.<n>] type/location`, `[deploy.resources]`, `ciu.repo_root/physical_repo_root`, `[ciu.worktree.exec_targets]`; write `[service.*]` variants, `[realization]` (`per_host` for transport daemons), `[network.*]`, `[vault] service`, `[testing.*]`; rename `[governance]` keys to `RK`; `[ciu.worktree] max_concurrent_instances` → `[ciu.instances] max_concurrent`.
3. **Stack files:** re-root to `[ciu_stack.<svc>]`; `requires`/`provides` → bindings (`init_requires` → `requires` or `binds.<n>` with an endpoint; `uses` → `binds.<n> = { to = …, wait = "none", delivery = "template" }`; `after` → `requires`; `init_provides` → `provides`; `[hooks.provides.<svc>]` → `provides` on the hook entry with `service`); add `endpoints`; secrets: `ASK_VAULT:<p>[#f]` → `from = "vault", path = "<p>"[, field = "<f>"]`; `GEN_TO_VAULT:<p>` → `from = "generate", store = "vault", path = "<p>"`; `GEN_LOCAL:<n>` → `from = "generate"`; `ASK_EXTERNAL:<V>` → `from = "ask", var = "<V>"`; `ASK_FILE:<p>` → `from = "file", path = "<p>"`; `ASK_HOST:<e>` → `from = "host", entry = "<e>"`; `GEN_EPHEMERAL` → `from = "ephemeral"`; `consumed_by = "hook"` → `delivery = "hook"`; `produced_by` dropped; `delivery` (and `env_name`) added to every secret; consumer scalars into sub-tables; mark `primary`; replace hand-declared replicas with `instances`; drop `name`, `stack_name`, `image_name`/`image_tag`, `internal_port`, `[<svc>.ports]`, `[<svc>.resources]`, `[state]` (moves to `ciu.state.toml`).
4. **Compose and config-file templates:** remove `container_name`, `hostname`, `networks`, `secrets`, `depends_on`, `ports`, `expose`, `ciu.*` labels, healthcheck timing fields, `${VAR:-…}`/`${VAR:?…}` forms, `{{ env.* }}`; replace identity/topology reads with `ciu_stack.*.identity`, `ciu_stack.<svc>.binds.<local>.*`, `instance.*`, `host.*`; replicated services iterate `identity.replicas` with `replica`; config-file templates re-root the same way and every `secret()` call requires `delivery = "configfile"`; `ciu.rendered.<svc>.<cfg>` reads become paths under `ciu.rendered/<svc>/`.
5. **Hooks:** `run(config, ctx)` → a script on `ciu.hookkit` (S12.5); `apply_to_config` → `emit(state=…)` (visible to the same run's renders, S8.7); `persist: "secret"` → `emit(secrets=…)`; `validate_config` → `--validate`.
6. **Secrets state** (`--secrets`): imports `.ciu/secrets/*` and `[state]` Vault bootstrap values into `ciu.secrets.toml` (never re-minting), moves non-secret `[state]` into `ciu.state.toml`, deletes `.ciu/`.
7. **Host directories** (`--hostdirs`): S6.8.4.
8. **Gate:** `run-gate.toml` → `[testing.*]`: `environments.<n>.container_name` → `exec_in = "<LogicalService>"`; `image` kept or `image_from`; `lanes.<n>.memory` → `resources.memory_max`; `pins.*` → `[testing.judge] version` (a floor); `assay_command` dropped (derived); `request_base` never existed as a key; central config (R-22) → `[ciu] inherit` (S3.1.5); conjunction lanes → `kind = "sequence"`; `RUN_GATE_*` → `CIU_GATE_*`; `.assay/verdict-*.json` → `evidence_dir`; `assay.toml` `derived:` facts (if any) repointed at `resolved.bindings.env.<e>.<local>.*` or replaced by `required-env:` facts fed from environment `binds`.
9. **run-gate stays available** for projects that keep it; both tools read the same `assay.toml`.

## Appendix B — Worked examples
**Minimal (one stack, one host lane), `v8-dstdns-demo/examples/minimal/`:**
```toml
# ciu.toml
[project]
name = "hello"
revision = 8
[realness]
default = "live"
[service.web]
live = "web"
[realization.web]
kind = "ciu_stack"
location = "web"
[bundles.all]
services = ["web"]
[layouts.local]
hosts.localhost = { bundles = ["all"], reach = ["instance"] }
[testing.lanes.unit]
kind = "command"
environment = "host"
argv = ["pytest", "-q"]
```
```toml
# ciu.hosts.toml (gitignored; written by ciu init)
[hosts.localhost]
local = true
```
```toml
# web/ciu.stack.toml
[ciu_stack.web]
image = "nginx:1.27"
endpoints.http = { port = 80, protocol = "http", publish = "host", host_port = 8080 }
```
`ciu init && ciu instance init && ciu up && ciu gate unit`. A zero-instance project is the same `ciu.toml` without `[service]`, `[realization]`, `[bundles]`, `[layouts]`, `[realness]` and without `ciu.hosts.toml`.

**dstdns:** `v8-dstdns-demo/` renders dstdns (27 Realizations, 4 hosts, 4 layouts, 9 lanes) in this notation, including a hand-written excerpt of the derived tables. Its README lists the decisions the conversion took and where the demo deliberately deviates from the v7 sources.

## Appendix C — Rule map draft.2 → draft.3
S3.5.5 (two-pass render) withdrawn · S6.4 (init edges) → S6.4 (bindings) · S7.8 (routes) → S7.8 (binding resolution) · S8.2 init/after edges → bind edges · S8.6.3 (`[hooks.provides]`) → S6.10 · S9.3.1 precedence → S9.3.1/S9.4.2 (record as constraint) · S10.1 directives → S10.1 sources · S14.4 (rendered-file lock) → S14.4 (directory lock) · S16.5 `request_base` withdrawn → S16.5.5 · S16.11 rewritten · S18.1 renumbered to v7 · `deploy.*` → `project.*`, `bundles`, `layouts`, `realness` · `[deploy.hosts]` → `[hosts]` · `ciu.instance.resolved` → `resolved` · `owned-seeded` → `seeded`.

**draft.3 → draft.4:** `published_on` → `publications` (socket claims, S7.4.6) · `schema_version` → `api`/`api_version` per artifact (S18.4) · `[project] vendor_images` withdrawn → `build` on the service (S6.2) · `ciu.gate.shared-*.lock` withdrawn → directory lock on the owning stack directory (S16.5.3) · `ciu.secret-copy.<svc>.<key>` files → `ciu.secret-copy.<svc>/` directories (S10.7) · S8.5.5 (running check) → acceptance · S16.2.1 one-level inherit → recursive · S17.3–S17.5 rewritten (releases, capsule, receipts) · `[network.<n>] realized_by` now names a LogicalService (S7.3) · S2.4 renamed "secret lint" · `[testing.externals]`, `verify`, `hooks[].secrets`, `[hooks] env_allow`, `ciu query`, `ciu testing flatten`, `ciu instance backup|restore`, `ciu check --validate-hooks`, `ciu up --receipts|--require-receipts`, `ciu push --prune` added.

**draft.4 → draft.5:** `[testing] inherit` withdrawn → `[ciu] inherit` with a closed inheritable list (S3.1.5) · `ciu testing flatten` → `ciu show effective` · `ciu up --require-receipts` withdrawn → strict by default, `--allow-assumed` is the escape (S17.5) · `[hosts.<h>.activate] rollback` withdrawn → rollback is CIU's state machine (S17.4.1) · `bundle_dir` default `/opt/ciu/current` → `/opt/ciu` (S7.2) · socket claim `bind = "*"` withdrawn → declared `listen` (S6.3, S7.4.7) · the resolved-file digest in receipts withdrawn → subject with `plan_digest`/`release_digest` and `activation_id` (S17.4.3) · `evidence_dir/.admitted/` withdrawn → ledger keyed by the cgroup path (S16.6.1) · per-lane `stdout.log`/`verdict.json`/`progress.jsonl` → per-run directories (S16.9.4) · S8.5.5 acceptance → partition · S5.3.6 WARN → ERROR unless `verify`/`unchecked` · `ciu.owner` label and `owner_id` (S4.1.1, S4.5.3) · `[hooks]` entry `inputs`, `pre_secrets` source restriction (S6.10) · `[realization.<n>] probe` for `external` (S5.4) · `[ciu] secret_lint_allow` closed (S3.4.7) · `external-missing|external-down` in S16.8 · `ciu lease`, `ciu instance adopt`, `ciu gate --resume`, `ciu push --images`, `ciu activate apply --release`, `ciu init --image|--service|--test-argv` in S18 · S14.8 backup/restore contract · S17.3.6 image transport · `candidate` pointer (S17.3.3) · S13.3 ceiling inverse and direct `memory.swap.max` · S2.4.2 per-value exemption.

**draft.5 → draft.6:** the **state root** (S2.6): `ciu.host.toml` with `[ciu.host]` replaces `[ciu.host.generated]` inside the generated file (S14.2); the realness record moves from the generated file into `ciu.instance.json` (S9.4, S14.7.1); the store, `ciu-data/`, receipts, leases, evidence and — in a release — `ciu.state.toml` live in the state root · `ciu instance adopt --owner` withdrawn, live `--move` withdrawn → cold move + `--fresh` (S4.1.2, S4.5.3) · `plan_digest` and `--activation ID` withdrawn → the activation manifest `ciu.activation.json` (`ciu activate plan`, `--activation-manifest FILE`) and validation against the provider's entry (S17.4.1, S17.4.3–S17.4.4) · `bootstrap` in `current` → prepare → apply → health → receipt → switch, both directions (S17.4.1) · `[ciu] inherit` and `build.context` reach = the containing worktree; `ciu.inherited.toml` in a release (S3.1.5, S6.2, S17.3.1) · S16.3 inherited judge floor permitted unused · S14.4.9 lock matrix; lease records as files (`ciu-leases/`, `ciu/lease-record`); `CIU_LEASE_FDS`; the realization-only verb class (S14.3, S14.4.8) · `ciu gate --resume` withdrawn; run ids random 128-bit; `ciu/run` manifest (S16.9.4, S16.7.2) · `probes` rows and `probe = "none"` bindings (S8.5.2a, S8.5.4, S6.4) · `no-manifest`/`invalid-receipt` reasons · S17.6.1 image map; immutable release tags in archive mode; id check in none mode (S17.3.6) · `memory_max` required for admitted lanes (S16.6.1, S16.6.4) · `io.max` read-back, `cpu.max` hard (S13.3.2) · `XDG_RUNTIME_DIR`, `XDG_STATE_HOME`, `CIU_LEASE_FDS` in S18.2; `ciu/activation`, `ciu/lease-record`, `ciu/run`, `ciu/backup` in S18.4 · S3.8.6 documentation conformance.

## Appendix D — Changes in draft.4, draft.5 and draft.6 and why (third-party review rounds 1–3)
Each row names the finding, the rule(s) changed, and the reasoning; the full dispositions are in `CIU-V8-THIRD-PARTY-REVIEW-RESPONSE-2026-09-03.md` (§1 for round 1, §6 for round 2, §7 for round 3).

### D.3 draft.6 — round-3 findings T3-01..T3-10

| finding | rules | change and reasoning |
|---|---|---|
| T3-01 | S17.4.1, S17.4.3, S17.4.4, S17.5, S8.5.4 | a receipt was compared with the consumer's own host-specific release; `ciu activate plan` writes an activation manifest with a fresh id and, per host, the expected release digest and selection, and a receipt is validated against the **provider's** entry; an absent id never matches; `plan_digest` is withdrawn — a hand-started host computes the digest of the release `push` would build, which covers hooks, templates and seeds. |
| T3-02 | S2.6, S14.2, S9.4, S10.6, S6.8, S6.10, S17.3.1, S17.3.3, S17.3.4, S17.4.1, S17.4.2, S14.1.1 | the target's host facts would have modified a verified release — and so would the store, the data directories, the realness records and the hook state, all of which draft.5 kept under the checkout. Every instance now has a **state root** (the checkout itself for a checkout; `<bundle_dir>/state/` for a release) holding every mutable file; `[ciu.host.generated]` became `ciu.host.toml`, the realness record moved into the instance record, releases stay byte-identical, and activation is prepare → apply → health → receipt → switch in both directions with pointers untouched on failure. |
| T3-03 | S3.1.5, S6.2, S16.3, S17.3.1, S3.3, demo `examples/monorepo/` | `[ciu] inherit` and `build.context` may leave a child root but never the containing worktree (canonicalized); a release carries inherited policy flattened into `ciu.inherited.toml` with source digests; an inherited judge floor is permitted and unused without an assay lane; the monorepo fixture (zero-instance root, assay child, command-only child, sibling build context) is checked in. |
| T3-04 | S14.3, S14.4.3, S14.4.7, S14.4.8, S14.4.9, S14.7.1 | no mutator took the "canonical" stack lock; the lock matrix says every verb that mutates a Realization's resources takes its stack lock after the instance and registry locks; a realization-only lease is its own verb class (no instance lock); lease records are lock-free atomic files; the leased command inherits the held descriptors (`CIU_LEASE_FDS`) so a CIU verb inside it cannot deadlock with its parent; observers probe non-blocking. |
| T3-05 | S4.1.1, S4.1.2, S4.5.3, S14.8.2, S18 | labels are immutable and a token in a copyable file is copied with the tree; a move is **cold** (refused while any resource carries the old owner; then a new id and a new token), a copied tree is refused rather than adopted (`--fresh` makes it its own instance), `adopt --owner` and the "copied tree" claim are withdrawn. The narrow reopening is accepted as the author's call within the operator's posture. |
| T3-06 | S3.8.6, S8.5.4, S17.5, S18.2, S18.4 | `probes` rows with a closed result, `no-manifest`/`invalid-receipt` reasons, the `ciu/backup`, `ciu/activation`, `ciu/lease-record`, `ciu/run` APIs and `XDG_STATE_HOME`/`XDG_RUNTIME_DIR`/`CIU_LEASE_FDS` are in their sets; S3.8.6 makes the document's enumerations a tested output of the implementation. |
| T3-07 | S17.6.1, S6.2, S17.3.6 | ownership is decided once per reference in the image map; archive mode tags the verified id with a release-unique immutable reference that the compose references; none mode compares the id before any create. |
| T3-08 | S16.9.4, S16.7.2, S16.5, S16.10 | run ids are 128-bit random with exclusive creation and a `run.json` manifest; `ciu gate --resume` is withdrawn — assay's resume is content-keyed under its own `.assay/mutation-state/`, so a CIU resume would resume nothing; the progress path stays in the run directory, a stated deviation from the run-gate `.assay/progress-<lane>.jsonl` convention (no hidden directories, no shared files). |
| T3-09 | S8.5.2a, S8.5.4, S6.4 | host-network reachability is probed from the consumer's vantage (a probe container on the instance network with the same `extra_hosts`, a host-namespace helper, or the consumer host); a UDP host-network listener is admitted only with an explicit `probe = "none"` on the binding. |
| T3-10 | S16.6.1, S16.6.4, S16.5, S15.3 stage 12, S13.3.2 | every admitted lane declares `memory_max` (absence is never zero); every hard cap — `memory.max`, `memory.swap.max`, `pids.max`, `cpu.max`, each `io.max` tuple — aborts on read-back mismatch; a requested hard cap the host cannot enforce is refused before start. |
| audit T2-03, T2-09 | as T3-06, T3-10 | the incomplete rows close through the findings above. |
| demo | `ciu.instance.generated.toml`, `ciu.host.toml`, `examples/*`, `ciu.hosts.toml`, `ciu.resolved.toml.example` | the host-file split; the receipt comment shows manifest validation; the activation comment shows prepare → apply → switch; `examples/monorepo/` added. |

### D.2 draft.5 — round-2 findings T2-01..T2-10 and the disposition-audit corrections

| finding | rules | change and reasoning |
|---|---|---|
| T2-01 | S17.4.3, S17.4.4, S17.5, S8.5.3, S8.5.4, S3.7.3 | the resolved-file digest could never match across hosts (`rendered_at`, host-local facts); a receipt now has a canonical **subject** (`instance_id`, `layout`, `host`, `selection`, `release_digest`/`plan_digest`, `activation_id`) and a body with container incarnations, one-shot exits and per-fact observations; validity compares subjects and requires the consumer's own `activation_id` (a receipt from another activation is stale by definition); missing proof is an ERROR by default, `--allow-assumed` the explicit escape, `--require-receipts` withdrawn. |
| T2-02 | S8.5.5, S5.3.6, S5.2 | "every service healthy AND every one-shot exited" was unsatisfiable; acceptance is a partition (non-one-shots healthy, one-shots completed, nothing judged twice); an empty-contract seeded/simulated selection is an ERROR unless `verify` or the variant's `unchecked = true` says so — an acceptance choice, not a WARN. |
| T2-03 | S3.4.7, S6.10, S5.4, S16.8, S18, S8.5.4, S19, S15.3 stage 12, S3.8.5 | eight surfaces draft.4 required in one rule and refused in another: `secret_lint_allow` and `inherit` in `[ciu]`; `env_allow` in `[hooks]`; `probe` on `external`; `external-missing|external-down` in S16.8; `--receipts`/`--allow-assumed`/`--activation` on `up`, `--image`/`--service`/`--test-argv` on `init`; `assumed` with an element schema; stage 12 compares with the available environment. S3.8.5 makes a table-driven conformance test part of the definition so the class cannot recur. |
| T2-04 | S6.10, S8.7 step 2, S12.2 | a `pre_secrets` entry could list secrets materialized only after it ran; the phase/source matrix is stated: `pre_secrets` may list `file`, `ask`, `host` and local `generate` keys (materialized for it ahead of step 3), nothing else; later phases may list anything step 3 produced. |
| T2-05 | S17.3.1, S17.3.3, S17.3.6, S17.4.1, S7.2, S6.2, S3.4.3 | the closure was undecidable for arbitrary hooks and shipped no image bytes; the closure is every non-ignored file under each placed stack plus declared hook `inputs`; images travel by registry digest or `docker save` archive and are verified on the target; `push` writes a `candidate` pointer that `apply` consumes (or `--release`); CIU owns the `current`/`previous` switch; rollback refuses without `previous` and runs the previous release's `apply`; the host `rollback` command is withdrawn; shared image tags: one `build` per reference, others share it (the demo's `mock_targets.http`). |
| T2-06 | S6.3, S6.3.2, S7.4.7, S7.8 step 4a, S8.5.2a | `bind = "*"` evaded the overlap relation and the listener address was unproved; host-network endpoints declare `listen`, claims carry it, `0.0.0.0`/`::` are canonicalized to address sets (`::` conservatively covers IPv4), resolution is admitted only when the consumer can reach that address, and the gate probes it live. |
| T2-07 | S16.6.1, S16.6.4, S16.6.5, S16.5.7, S16.9.4, S16.7.2, S16.4 | admission was keyed by the evidence directory and runs shared paths; admission locks the slice's cgroup directory with a ledger keyed by the cgroup path; exec targets are used by one lane at a time (their stack-directory lock, S16.5.7), so their capacity needs no ledger; every invocation writes under `runs/<run_id>/`, with `--resume` for assay's progress. |
| T2-08 | S4.1.1, S4.1.2, S4.5.1, S4.5.3, S14.8.2, S18 | `--move` turned a detected collision into a takeover; a 128-bit `owner_id` is generated at init, stamped as `ciu.owner`, and is the proof `--move` needs; adoption without the token only when the daemon proves the path; `ciu instance adopt --owner` is the explicit lost-state recovery. Accepted as a cost of the in-checkout posture the operator chose: the token lives in the checkout and changes no identity. |
| T2-09 | S13.3, S13.3.2 | nearest rounding undershot 4999 weights; the inverse uses the ceiling and must round-trip all 10000 by test; `memory_max = "max"` with finite swap writes `memory.swap.max` directly instead of unlimited swap; a read-back mismatch on an enforceable cap aborts. |
| T2-10 | S2.4.2, S2.4.3, S10.2.6 | one configfile secret exempted the whole file; every file is compared with every store value and only the values `secret()` actually requested for that service are authorized, counted in the output. |
| audit T-06 | S18, S19 | `init`'s synopsis lacked `--image`, `--service` and the test-argv input (Appendix B was already valid TOML — `location` appears once — that part of the remark did not hold). |
| audit T-09 | S17.3.2 | absent local `generate`/`ask` values are materialized on the sender before the capsule is built. |
| audit T-11 | S14.8 | the backup contract: destination, manifest, modes, consistency boundary, refusal rules, `--move` on restore. |
| audit T-12 | S15.3 stage 12 | stage 12 compared with `forward_env` alone; it compares with S16.4.5's available environment. |
| audit T-19, T-32, T-33, T-08 | proposal | stale proposal spellings (old lock order, one-level inherit, `schema_version`, judge `>=2.4`) corrected in rev 3.2. |
| audit T-23, T-26, T-28, T-31, T-15, T-16, T-18 | as T2-03/04, T2-06, T2-10, T2-03, T2-02, T2-01, T2-08 | the "landed-and-broke" rows resolve through the findings above. |
| audit T-29 | S6.2, S3.4.3 | one `build` per image reference; other services share it. |
| demo | `examples/ciu.resolved.toml.example`, `ciu.hosts.toml`, stacks | S8.5.2 now states that minter and pki facts are gate facts, which makes the example's `gates.3` fact row correct by rule; `bundle_dir` is a parent directory; `rollback = "ciu down"` removed; the stale `realized_by = "tailscale_node"` comment fixed; header notation convention documented in the README. |
| operator, 2026-09-03 | S14.4.7, S14.4.8, S16.5.7, S14.7.1, S18 | the canonical lock keys and `ciu lease` (design answer A of the handoff note). |
| operator, 2026-09-03 | S3.1.5, S3.4.7, S16.2.1, S16.11.1, S1.5.2, S5.4, S6.2, S18 | `[ciu] inherit` for a closed list of policy tables; the shared tester is a per-project two-file stack whose `build.context` is the shared Dockerfile directory (a shared `location` was rejected: rendered artifacts and the lock live there); no meta-root, no `autostart` (design answer B). |

### D.1 draft.4 — round-1 findings T-01..T-35

| finding | rules | change and reasoning |
|---|---|---|
| T-01 | S4.3.1 | "every `compose_project` unique" contradicted the per-Realization derivation; the check is now per namespace (project among Realizations; names among services; keys/aliases on the network). |
| T-02 | S1.4, S7.2, S7.3 | `fqdn` was typed as a single label; a `dns_name` type was missing. |
| T-03 | S6.10 | the hook entry object is now closed and explicit (`run`, `service`, `provides`, `secrets`); `service` had been prose only. |
| T-04 | S7.3, S7.3.2, S7.6.5, S5.3.5 | `realized_by` names a LogicalService so its variant service is unique; a `requires` on a `per_host` capability targets the consumer host's copy; a mock never leaves a data binding unresolved (already refused, now also for `facts`, naming the layout). |
| T-05 | S8.5.1 | the wave gate evaluates the strongest predicate per edge; `started` is `Running`, not `healthy`. |
| T-06 | S19 | bare `ciu init` writes a zero-instance skeleton; `--stack` needs `--image` or `--from-compose`; scaffolds must pass `ciu check`; nothing invented. |
| T-07 | S16.9, S18.4 | LaneResults carry `status` (mapped from `outcome`) and the `ciu/lane-result` header. |
| T-08 | S16.3, S16.7.2 | `--progress` takes a path; CIU declares its minimum judge (4.1.0) and refuses a lower floor. |
| T-09 | S17.3.2, S17.3.3 | a per-host **capsule** replaces "the stored value travels": file values are read at push, vault values pre-fetched or refused, imports are `transport:*` and never refreshed on the target. |
| T-10 | S7.4.7, S7.8 4a, S11.4 | `host_network` services are never published and resolve via `host-gateway` (`ciu-host`) on the same host, host address cross-host. |
| T-11 | S2.3.4, S14.4.5 | the operator kept the in-checkout posture; the text now says plainly that deleting the non-regenerable files is destructive, adds `instance backup|restore` and container adoption by the `ciu.checkout` label. |
| T-12 | S16.4.5 | the lane's available environment is defined (forward_env ∪ binding variables ∪ container env for exec ∪ `CIU_*`); demo tester governance and `forward_env` fixed. |
| T-13 | S7.8 step 5, S7.4.4 | a cross-host resolution through a loopback-bound host publication is refused; `allow_from` is declared admission data and a WARN when no template consumes it; demo port collision and consul override fixed. |
| T-14 | S8.4.1 | the wave algorithm is written down (consumer→provider, sibling edges discarded, `wave = 1 + max(providers)`); the example waves are regenerated from it. |
| T-15 | S8.5.5, S5.3.6, S5.2 | acceptance: every selected service healthy, every one-shot completed, `verify` facts probed — consumers or not; empty derived contracts of seeded/simulated variants WARN. |
| T-16 | S8.5.3, S17.4.3–S17.5 | activation **receipts** carried by `ciu activate`; a remote fact is accepted only from a valid receipt, else `assumed` + WARN; external/joined facts are assertions. |
| T-17 | S4.3.1, S4.5.1, S4.5.3 | all namespaces checked at max replica; `ciu.checkout` label; ownership verified before deletion; adoption/refusal on init. |
| T-18 | S4.1.4, S4.5.3 | path hash stays (operator); the physical path is proven by a sentinel bind; moved checkouts adopt with `--move`. |
| T-19 | S14.4.3, S9.5.7, S16.5.3 | locks acquired in ascending instance-id order, join graph acyclic, gate shared locks on the owning stack directory (no unlinkable file). |
| T-20 | S16.4, S16.12 | detached create/start/wait/remove state machine instead of `--rm`; private writable git config with `safe.directory`; dual mount of physical and workdir paths. |
| T-21 | S16.6.1, S16.6.4, S16.6.5, S16.9 | admission is a locked transaction with reservations; exec caps are requested values (`resources_applied = null`); result/history writes serialized. |
| T-22 | S13.3, S13.3.2, S13.4 | the compose↔cgroup conversions are written (`memswap_limit` = memory + swap; `cpu_shares` inverse mapping); values are read back; `requested`/`applied`/`unsupported` split. |
| T-23 | S12.1, S12.2, S12.4, S6.10 | clean allow-listed hook environment; per-entry `secrets`; validation runs only with `--validate-hooks` or inside `up`; entry i+1 sees entry i's state. |
| T-24 | S10.7, S11.4, S2.2 | per-service secret directories mounted at `/run/secrets`; refresh by rename + fsync. |
| T-25 | S17.3, S17.4 | manifested releases staged and verified, atomic `current` switch, `previous`, real rollback. |
| T-26 | S6.3.2, S7.4.5, S7.4.6 | socket claims `(host, protocol, bind, port)` with wildcard overlap and daemon/registry reservation. |
| T-27 | S11.2, S11.3 | `network_mode/ipc/pid: service:` rewritten; `links`, `extends`, `volumes_from`, `build` forbidden in templates. |
| T-28 | S2.4 | renamed to a lint: high-confidence formats and store matches are errors, suffix heuristics warn with path suppressions, output names the comparisons. |
| T-29 | S6.2, S3.4.3, S17.6, S11.2 | `build` on the service declares project-built images; `vendor_images` withdrawn. |
| T-30 | S6.9.2, S6.8.5 | overlap checks per service; symlink note; `seed` as an idempotent transaction. |
| T-31 | S16.2.2, S16.4.2 | `[testing.externals]` for typed test dependencies in zero-instance projects. |
| T-32 | S16.2.1 | recursive, cycle-checked inheritance; paths relative to the declaring file; `ciu testing flatten`. |
| T-33 | S3.7.1, S12, S16.9, S17, S18.4 | per-artifact `api`/`api_version` and a compatibility policy. |
| T-34 | S7.5, S7.4.6, S8.6 refs | recursive includes stated once; structured publications; dangling `S8.6.4` references fixed. |
| T-35 | S3.7.1, S3.7.6, S18 | `resolved.capabilities` index and `ciu query`; names kept (operator-approved). |
