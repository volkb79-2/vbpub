# Prompt: round-4 (delta) review of the CIU v8 design set — rev 3.4 / draft.7, plus the host-enrollment design for both lines

*Hand this file, unchanged, to the reviewer — preferably the same reviewer as rounds 1–3. Round 1's prompt (`CIU-V8-THIRD-PARTY-REVIEW-PROMPT.md`) still applies for role, constraints, severity scale and tone; round 2's (`CIU-V8-THIRD-PARTY-REVIEW-PROMPT-ROUND2.md`) for the delta method and the output format. This file narrows the scope.*

## 1 What happened since your round-3 review

Your ten findings T3-01..T3-10 were verified against the text and all accepted; none went to the operator. T3-05's narrow reopening was taken as the author's call inside the operator's posture (a move is cold; a copy is refused). T3-02 was widened: the same reasoning applied to the store, the data directories, the realness records and the hook state, so draft.6 introduced a **state root** rather than a host-facts overlay. The result is `SPEC-V8.md` **8.0.0-draft.7** (draft.6 = round 3 folded, Appendix D §D.3; draft.7 = one addition on operator direction, §D.4), `CIU-V8-TESTING-GATE-PROPOSAL.md` **rev 3.4** (§4.3.15, §4.3.16, §4.7 X85–X95, §4.8, §4.10 items 23–27, §4.11 N23), the response document §7, the demo (`ciu.host.toml` split, `examples/monorepo/`, receipt and activation comments), and — new — a host-enrollment design accepted for **both lines**: `CIU-HOST-ENROLLMENT-PROPOSAL.md` **revision 2**, normative in v8 as S7.2.4 and in v7 as `SPEC.md` S14.7, with a cmru template item (cmru `KNOWN_ISSUES_TODO_BACKLOG.md` KI-24).

## 2 Scope of this round (do these five things; nothing else)

1. **Audit the round-3 dispositions** (response §7.1, Appendix D §D.3): one row per T3-01..T3-10 with `landed | landed-but-incomplete | not-landed | landed-and-broke-<rule>`. Where you disagree with a disposition that differs from your proposed fix — T3-02's `<bundle_dir>/state/` instead of a per-instance-per-host path, T3-05's cold move instead of an atomic ownership transfer, T3-08's withdrawal of `ciu gate --resume` instead of hardening it, T3-10's mandatory `memory_max` instead of a whole-capacity reservation — say why the reasoning given is wrong, not merely that you would have chosen otherwise.
2. **Read the rules draft.6 introduced as an implementer and as an attacker:** S2.6 (the state root: derivation from the release path, two releases sharing one root, a bundle directory that ever holds two instances, the first `prepare` creating the root, what `ciu instance show` prints); S14.2 (the host file; the derived-not-stored roots — is `physical_repo_root` derivable on every host shape, including a devcontainer release target?); S9.4/S14.7.1 (the realness record in the instance record: who writes it, under which lock, and what a `--vanilla` clean does now); S17.4.1 (the activation manifest and the prepare → apply → health → receipt → switch sequence — attack the pointer semantics under a crash between steps, the `candidate` removal, a second `push` during an activation, and the hand-started flow where `plan` and `push` must agree byte-for-byte on a release digest without a transfer); S17.4.3–S17.4.4 (manifest-entry validation: can a host's receipt be replayed for another host's entry?); S14.4.8–S14.4.9 (the lock matrix; lock-free lease records; `CIU_LEASE_FDS` across `sudo`, `ssh` and containerized commands — where does an inherited descriptor stop being inherited?); S4.1.2 (cold moves; what "live" means for a stopped container, a dangling volume, a network); S3.1.5/S6.2 (inherit and build-context reach = the containing worktree — a git worktree whose top level is the linked worktree, a submodule, a symlinked checkout); S8.5.2a (consumer-vantage probes — the probe container's lifetime, its own admission and governance, what it costs per wave); S16.9.4 (random run ids; pruning "complete" runs whose pid is alive on another host?); S17.6.1/S17.3.6 (the image map; the release-unique tag under `docker image prune`); S3.8.6 (is "the document's enumerations are a tested output" implementable as stated?).
3. **Review the host-enrollment design, both lines, as the primary new surface:** `CIU-HOST-ENROLLMENT-PROPOSAL.md` rev 2 §2–§8, v8 S7.2.4 + the S18 row, v7 `SPEC.md` S14.7a–d, cmru KI-24. Attack first: the trust story (`curl | python3` of a version-pinned release asset; the console fingerprint as the second channel for TOFU — what does the operator actually compare, and what does a man in the middle between step 1 and step 2 need to succeed; whether `--fingerprint` should accept more than the ed25519 key); the target-side privilege model and idempotency of `get.py enroll` (root, `useradd`, `docker` group, `from=` prefixes, a pre-existing `authorized_keys` with other keys, re-runs after a partial failure, `--no-install`); the round-trip writer for the inventory file (comments, other hosts, a `--replace` rotation while the old key is in use); the interaction with v8's state root and `docker_optional` hosts (`ciu version` as proof when ciu is installed under `/opt/ciu/current/`?); whether the v7 backport can be built without touching any existing S14 path; and what the design still leaves manual (`fqdn`, `addresses`, `bundle_dir`, `[activate]`). Say what is missing for an implementer to build both carves from the proposal's oracles O1–O6 alone.
4. **Re-derive the monorepo fixture** `v8-dstdns-demo/examples/monorepo/` against S1.5, S3.1.5, S6.2, S16.3, S16.6.1, S16.11.1 and S17.3.1: does every file pass the rules as written, does `libx` refuse anything, what would `ciu push` of `appy` flatten into `ciu.inherited.toml`, and is the sibling `build.context` inside the containing worktree under every git shape you can think of. Re-check the dstdns demo's changed files (`ciu.instance.generated.toml`, `ciu.host.toml`, the resolved example's merged-input block and receipt comment, `ciu.hosts.toml`'s activate comment) against S14.2, S17.4.3 and S17.4.1.
5. **List what rounds 1–3 missed** that draft.7 makes visible — at most ten new findings, each with a failure scenario; do not restate the open items of the response document §5–§7.3 or proposal §4.10.

Do **not** re-review the model, the naming, the plain-TOML decision, the in-checkout posture, manifested releases, the ciu8 subproject decision or the two design answers (lease, inherit) as such; those are settled unless you show a defect the operator did not weigh (state it as "new cost of a settled decision").

## 3 Files (repository `vbpub`)
`ciu/docs/SPEC-V8.md` (draft.7; read Appendix C's draft.5 → draft.6 → draft.7 maps and Appendix D §D.3–§D.4 first), `ciu/docs/CIU-V8-THIRD-PARTY-REVIEW-RESPONSE-2026-09-03.md` (§7), `ciu/docs/CIU-V8-TESTING-GATE-PROPOSAL.md` (rev 3.4: §4.1.3, §4.1.5, §4.1.9, §4.1.10, §4.3.1, §4.3.15, §4.3.16, §4.4 V8-26..V8-29, §4.7 X85–X95, §4.8, §4.10, §4.11), `ciu/docs/CIU-HOST-ENROLLMENT-PROPOSAL.md` (rev 2), `ciu/docs/SPEC.md` S14.7 (the v7 backport; S14.1–S14.6 for context), `cmru/KNOWN_ISSUES_TODO_BACKLOG.md` KI-24 and `cmru/templates/get.py.tmpl` (the installer the `enroll` subcommand joins), `ciu/docs/CIU-V8-THIRD-PARTY-REVIEW-ROUND3-2026-09-03.md` (your own round-3 document), `ciu/docs/v8-dstdns-demo/` (README D38–D43; `ciu.instance.generated.toml`; `ciu.host.toml`; `examples/ciu.instance.generated.joined.toml`; `examples/ciu.resolved.toml.example`; `ciu.hosts.toml`; `examples/monorepo/`).

## 4 Output format (binding)
As round 2 (`CIU-V8-THIRD-PARTY-REVIEW-PROMPT-ROUND2.md` §4): **1 Verdict** (is draft.7 implementable; is the enrollment design implementable in each line; what still blocks); **2 Round-3 fix audit** (a table, one row per T3-01..T3-10); **3 New findings** `T4-01`, `T4-02`, … in severity order with the round-1 fields — enrollment findings marked `[enroll v7]`, `[enroll v8]` or `[enroll both]` in `Where`; **4 Fixture and demo re-verification**; **5 Not verified**; **6 Machine summary** with the `dispositions` object. Cite rule ids for every claim; quote where wording matters; no finding without a failure scenario; inline `ASSUMPTION:` where the text does not answer, listed again in section 5.
