# --- CIU generated artifacts (added by `ciu init`) ---
# Machine identity layer (S2.7) — regenerated by `ciu env generate`, never commit.
ciu.env
# Rendered global config (S3.1): the .j2 source is tracked, its output is not.
ciu.global.toml
# Per-stack machine dirs: rendered ciu.toml, materialized secrets, overlays.
**/.ciu/
# CIU's rendered compose output for stacks whose template is .j2 (S8.1).
**/ciu.compose.yml
