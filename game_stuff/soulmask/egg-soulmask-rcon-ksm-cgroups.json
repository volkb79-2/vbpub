{
    "_comment": "egg-soulmask-rcon-ksm + 12 admin-only cgroup variables for patched Wings (vbpub/wings-cgroups). With docker.per_server_slices.enabled (patch 0004) Wings derives wings-<uuid32>.slice automatically, so WINGS_CGROUP_PARENT is only an explicit override/opt-out; the WINGS_CG_* variables carry optional per-server floors, ceilings and weights that override the node defaults in config.yml. Import over the existing egg (update-in-place) so running servers keep their egg association. Every value defaults to empty, so importing changes no behaviour by itself; values are validated and budget-checked by Wings and take effect at the next container (re)creation -- panel Stop then Start, not Restart. Stock Wings ignores all of them. Variable groups: placement (WINGS_CGROUP_PARENT); the steady memory band (WINGS_CG_MEMORY_MIN/_LOW/_HIGH/_MAX); the startup memory band (WINGS_CG_STARTUP_MEMORY_MIN/_LOW/_HIGH/_MAX, patch 0007, image cgroup.7+), applied while the server is loading and replaced by the steady band when the egg's startup 'done' string matches or docker.per_server_slices.startup_grace expires; and the weights (WINGS_CG_CPU_WEIGHT, and WINGS_CG_IO_BFQ_WEIGHT / WINGS_CG_IO_WEIGHT, which are mutually exclusive). Two facts worth knowing before setting values. (1) IO: patch 0005 added WINGS_CG_IO_BFQ_WEIGHT so the weight can be stated on BFQ's own 1-1000 scale; systemd compresses its own 1-10000 IOWeight into that range, so ratios above the default shrink by ~11x and WINGS_CG_IO_WEIGHT=1000 buys 1.8:1, not 10:1. Prefer the BFQ spelling on BFQ nodes; keep the systemd one for iocost/non-BFQ nodes. (2) MEMORY: memory.high reclaim ignores memory.min, so a ceiling below the server's LOAD-TIME peak evicts it through its own guaranteed floor and it does not recover without a restart -- if you set WINGS_CG_MEMORY_HIGH, set WINGS_CG_STARTUP_MEMORY_HIGH generously as well.",
    "meta": {
        "version": "PTDL_v2",
        "update_url": null
    },
    "exported_at": "2026-07-20T14:30:00+00:00",
    "name": "Soulmask updated for DLC+RCON+KSM+CGROUPS",
    "author": "Volker Badziong",
    "description": "Escaping a deadly sacrificial ritual, you find an ancient mystical mask on your journey. This mask holds potent knowledge, changing the world you knew. Face the harsh challenges of nature, survive, rally followers, and build your own tribe. Explore and unveil the truths behind the enigmatic mask. | KSM variant: starts the server with LD_PRELOAD=/home/container/ksm-optin.so which opts the process into Kernel Samepage Merging (dedups identical anonymous pages across multiple Soulmask instances; kernel >=6.4). Build/deploy the shim per game_stuff/soulmask/ksm-optin.c; if the .so is absent the loader prints a harmless warning and the server runs without KSM. | CGROUPS variant: adds 12 admin-only variables for per-server systemd slice placement and resource guarantees (WINGS_CGROUP_PARENT + WINGS_CG_MEMORY_MIN/LOW/HIGH/MAX + the startup band WINGS_CG_STARTUP_MEMORY_MIN/LOW/HIGH/MAX + WINGS_CG_CPU_WEIGHT + WINGS_CG_IO_BFQ_WEIGHT/WINGS_CG_IO_WEIGHT). Requires patched Wings (vbpub/wings-cgroups) with docker.per_server_slices.enabled; stock Wings ignores them all. Every variable defaults to empty = use the node default.",
    "features": null,
    "docker_images": {
        "SteamCMD": "ghcr.io/ptero-eggs/steamcmd:debian"
    },
    "file_denylist": [],
    "startup": "LD_PRELOAD=/home/container/ksm-optin.so /home/container/WS/Binaries/Linux/WSServer-Linux-Shipping {{MAP_TYPE}} -server -SteamServerName=\"{{SERVER_NAME}}\" -PSW={{SERVER_PASSWORD}} -saving={{SAVE_TIME}} -Port={{PORT_SERVER}} -QueryPort={{QUERY_PORT}} -EchoPort={{ECHO_PORT}} -forcepassthrough -adminpsw={{ADMIN_PASSWORD}} -MaxPlayers={{MAX_PLAYERS}} -gamedistindex={{REGION}} -backup={{BACKUP_TIME}} -log -UTF8Output -MULTIHOME=0.0.0.0 -rconport={{RCON_PORT}} -rconpsw={{RCON_PASSWORD}} -mod=\\\"{{MOD_ID}}\\\"",
    "config": {
        "files": "{}",
        "startup": "{\r\n    \"done\": \"Create Dungeon Successed\"\r\n}",
        "logs": "{}",
        "stop": "^C"
    },
    "scripts": {
        "installation": {
            "script": "#!/bin/bash\r\n# steamcmd Base Installation Script\r\n#\r\n# Server Files: /mnt/server\r\n# Image to install with is 'ghcr.io/ptero-eggs/installers:debian'\r\n\r\n\r\n# Install packages. Default packages below are not required if using our existing install image thus speeding up the install process.\r\n#apt -y update\r\n#apt -y --no-install-recommends install curl lib32gcc-s1 ca-certificates\r\n\r\n## just in case someone removed the defaults.\r\nif [[ \"${STEAM_USER}\" == \"\" ]] || [[ \"${STEAM_PASS}\" == \"\" ]]; then\r\n    echo -e \"steam user is not set.\\n\"\r\n    echo -e \"Using anonymous user.\\n\"\r\n    STEAM_USER=anonymous\r\n    STEAM_PASS=\"\"\r\n    STEAM_AUTH=\"\"\r\nelse\r\n    echo -e \"user set to ${STEAM_USER}\"\r\nfi\r\n\r\n## download and install steamcmd\r\ncd /tmp\r\nmkdir -p /mnt/server/steamcmd\r\ncurl -sSL -o steamcmd.tar.gz https://steamcdn-a.akamaihd.net/client/installer/steamcmd_linux.tar.gz\r\ntar -xzvf steamcmd.tar.gz -C /mnt/server/steamcmd\r\nmkdir -p /mnt/server/steamapps # Fix steamcmd disk write error when this folder is missing\r\ncd /mnt/server/steamcmd\r\n\r\n# SteamCMD fails otherwise for some reason, even running as root.\r\n# This is changed at the end of the install process anyways.\r\nchown -R root:root /mnt\r\nexport HOME=/mnt/server\r\n\r\n## install game using steamcmd\r\n./steamcmd.sh +force_install_dir /mnt/server +login ${STEAM_USER} ${STEAM_PASS} ${STEAM_AUTH} $( [[ \"${WINDOWS_INSTALL}\" == \"1\" ]] && printf %s '+@sSteamCmdForcePlatformType windows' ) +app_update ${SRCDS_APPID} +app_update 1007 $( [[ -z ${SRCDS_BETAID} ]] || printf %s \"-beta ${SRCDS_BETAID}\" ) $( [[ -z ${SRCDS_BETAPASS} ]] || printf %s \"-betapassword ${SRCDS_BETAPASS}\" ) ${INSTALL_FLAGS} validate +quit ## other flags may be needed depending on install. looking at you cs 1.6\r\n\r\n## set up 32 bit libraries\r\nmkdir -p /mnt/server/.steam/sdk32\r\ncp -v linux32/steamclient.so ../.steam/sdk32/steamclient.so\r\n\r\n## set up 64 bit libraries\r\nmkdir -p /mnt/server/.steam/sdk64\r\ncp -v linux64/steamclient.so ../.steam/sdk64/steamclient.so\r\n\r\n## add below your custom commands if needed\r\n## Maybe this path is just needed?\r\nmkdir -p /mnt/server/WS/Saved/Config/LinuxServer\r\n\r\n## install end\r\necho \"-----------------------------------------\"\r\necho \"Installation completed...\"\r\necho \"-----------------------------------------\"",
            "container": "ghcr.io/ptero-eggs/installers:debian",
            "entrypoint": "bash"
        }
    },
    "variables": [
        {
            "name": "App ID",
            "description": "Steam ID for the linux version of soulmask",
            "env_variable": "SRCDS_APPID",
            "default_value": "3017300",
            "user_viewable": false,
            "user_editable": false,
            "rules": "required|string|in:3017300",
            "field_type": "text"
        },
        {
            "name": "Auto Update",
            "description": "Auto update the server on start",
            "env_variable": "AUTO_UPDATE",
            "default_value": "1",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|boolean",
            "field_type": "text"
        },
        {
            "name": "Server Name",
            "description": "Specifies the name of the game instance displayed in the server list",
            "env_variable": "SERVER_NAME",
            "default_value": "Soulmask Server",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string|max:85",
            "field_type": "text"
        },
        {
            "name": "Server Password",
            "description": "Server password, private servers can specify a password, players must enter the password to enter/join the server.",
            "env_variable": "SERVER_PASSWORD",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string|max:32",
            "field_type": "text"
        },
        {
            "name": "Map Players",
            "description": "Specifies the maximum number of players the game instance can support.",
            "env_variable": "MAX_PLAYERS",
            "default_value": "60",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|numeric|between:1,70",
            "field_type": "text"
        },
        {
            "name": "Backup Interval",
            "description": "Specifies the interval for writing the game database to disk (unit: seconds).",
            "env_variable": "BACKUP_TIME",
            "default_value": "960",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|numeric",
            "field_type": "text"
        },
        {
            "name": "Save Time Interval",
            "description": "Specifies the interval for writing game objects to the database (unit: seconds).",
            "env_variable": "SAVE_TIME",
            "default_value": "600",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|numeric",
            "field_type": "text"
        },
        {
            "name": "Server Port",
            "description": "UDP, Game connections (using PORT_SERVER as SERVER_PORT is reserved)",
            "env_variable": "PORT_SERVER",
            "default_value": "8777",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|numeric|between:1024,65536",
            "field_type": "text"
        },
        {
            "name": "Query Port",
            "description": "Specifies the Steam query port, UDP, needs to be open to the public.",
            "env_variable": "QUERY_PORT",
            "default_value": "27015",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|numeric|between:1024,65536",
            "field_type": "text"
        },
        {
            "name": "Echo Port",
            "description": "Maintenance port, used for local telnet server maintenance, TCP, does not need to be open.",
            "env_variable": "ECHO_PORT",
            "default_value": "18888",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|numeric|between:1024,65536",
            "field_type": "text"
        },
        {
            "name": "RCON Port",
            "description": "Specifies the TCP port that the RCON listener will bind to. This is the port that RCON clients will need to connect to. If not specified, it defaults to port 19000.",
            "env_variable": "RCON_PORT",
            "default_value": "19000",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|numeric|between:1024,65536",
            "field_type": "text"
        },
        {
            "name": "RCON Password",
            "description": "If specified, enables RCON on the server, allowing RCON clients to connect and issue some admin commands. The value specifies the password that is required for RCON clients to connect. If this parameter is not specified, RCON will be disabled.",
            "env_variable": "RCON_PASSWORD",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string|max:32",
            "field_type": "text"
        },
        {
            "name": "Admin Password",
            "description": "GM activation password.Open GM Panel (gm key [password])",
            "env_variable": "ADMIN_PASSWORD",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string|max:32",
            "field_type": "text"
        },
        {
            "name": "Mods",
            "description": "Specifies the order and which mods are loaded. ModIDs need to be comma-separated such as: ModID1,ModID2,ModID3",
            "env_variable": "MOD_ID",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|regex:/^[0-9,]+$/",
            "field_type": "text"
        },
        {
            "name": "Region (gamedistindex)",
            "description": "1= Asia 2=North America 3=Europe 4=Mainland China 5=South America 6=Oceania 7=Africa",
            "env_variable": "REGION",
            "default_value": "3",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|numeric|between:0,7",
            "field_type": "text"
        },
        {
            "name": "Map Type",
            "description": "which map to load: Level01_Main (original), DLC_Level01_Main (DLC Shifting Sands)",
            "env_variable": "MAP_TYPE",
            "default_value": "Level01_Main",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string|in:Level01_Main,DLC_Level01_Main",
            "field_type": "text"
        },
        {
            "name": "Wings Cgroup Parent",
            "description": "Admin-only OVERRIDE -- normally LEAVE EMPTY.\n\nWith docker.per_server_slices.enabled, Wings already derives wings-<32-hex-uuid>.slice for every server, so this variable is only for exceptions. Set a wings-*.slice name to pin this server to a specific, hand-managed slice (it then receives ONLY the explicit WINGS_CG_* values below, never the node-wide defaults). Set it to exactly the node-wide docker.cgroup_parent value to opt this server out of per-server slices entirely.\n\nPanel-supplied values are not trusted: a value outside the allowed namespace (or the docker.allowed_cgroup_parents allow-list) is rejected with a logged warning and Wings falls back to the derived slice.\n\nPlacement is fixed at container-create time, so a change here only takes effect on the next container (re)creation -- panel Stop then Start, not Restart.",
            "env_variable": "WINGS_CGROUP_PARENT",
            "default_value": "",
            "user_viewable": false,
            "user_editable": false,
            "rules": "nullable|string|max:128",
            "field_type": "text"
        },
        {
            "name": "Wings Cgroup Memory Min",
            "description": "Admin-only: memory.min -- the GUARANTEED, never-reclaimed floor for this server's slice. Accepts 6G, 512M, or a plain byte count. Empty = the node default (docker.per_server_slices.defaults.memory_min).\n\nWhat it does: while this server is using less than this much memory, the kernel will not reclaim its pages under host-wide pressure -- it takes memory from unprotected cgroups instead, and would sooner OOM than break the floor.\n\nTwo things it does NOT do, both of which cause real confusion:\n1. It is not a reservation and it never pulls pages back. If the server has already been pushed into swap, raising the floor does not restore anything -- those pages return only as the workload faults them in again.\n2. It does NOT protect the server from its own WINGS_CG_MEMORY_HIGH. Reclaim triggered by a cgroup's own ceiling ignores the floor completely, which is the one way a server ends up pinned BELOW a floor that is correctly applied and fully effective.\n\nCapped by the parent slice: a floor larger than the node slice's own MemoryMin is arithmetically dead. Wings enforces this at apply time via docker.per_server_slices.memory_min_budget (clamp / refuse / distribute) and logs any reduction.",
            "env_variable": "WINGS_CG_MEMORY_MIN",
            "default_value": "",
            "user_viewable": false,
            "user_editable": false,
            "rules": "nullable|string|max:32",
            "field_type": "text"
        },
        {
            "name": "Wings Cgroup Memory Low",
            "description": "Admin-only: memory.low -- best-effort protection above the memory.min floor. Accepts 12G, 512M, or bytes. Empty = the node default.\n\nUnlike memory.min this is a hint, not a guarantee: the kernel reclaims unprotected cgroups first and only takes from here when it finds nothing else, rather than refusing outright.\n\nOnly meaningful BELOW this server's own memory.high -- a cgroup is never allowed to hold more than its ceiling, so any memory.low above it is decorative. With high=7G, setting low=12G behaves exactly like low=7G.",
            "env_variable": "WINGS_CG_MEMORY_LOW",
            "default_value": "",
            "user_viewable": false,
            "user_editable": false,
            "rules": "nullable|string|max:32",
            "field_type": "text"
        },
        {
            "name": "Wings Cgroup Memory High",
            "description": "Admin-only: memory.high -- a THROTTLE, not a hard limit. Accepts 7G, 512M, or bytes. Empty = the node default. Above this the kernel continuously reclaims the server back down (into zswap, then disk swap); allocations still succeed, the server just gets slow.\n\nREAD THIS BEFORE SETTING IT. Reclaim driven by memory.high IGNORES memory.min. This is the only mechanism that can push a server below its own guaranteed floor, and the damage does not heal: nothing faults those pages back except the workload itself, so the server can sit permanently below its floor until it is restarted. A ceiling and a floor are not independent knobs -- a ceiling set too low destroys the floor.\n\nTherefore size this against the LOAD-TIME PEAK, not the steady working set. A game server streaming in a world routinely peaks at several times what it settles at afterwards. If the peak is much higher than steady state, do not lower this value -- set WINGS_CG_STARTUP_MEMORY_HIGH instead, which applies a roomier ceiling only while the server is starting.\n\nSymptoms of a ceiling that is too low: high resident memory early, then a server pinned well under its floor with a large swap footprint, many memory.high events in memory.events, and steady refaults. Leaving this empty is a perfectly reasonable production choice -- WINGS_CG_MEMORY_MAX is the emergency backstop.",
            "env_variable": "WINGS_CG_MEMORY_HIGH",
            "default_value": "",
            "user_viewable": false,
            "user_editable": false,
            "rules": "nullable|string|max:32",
            "field_type": "text"
        },
        {
            "name": "Wings Cgroup Memory Max",
            "description": "Admin-only: memory.max -- the hard wall for this server's slice. The kernel reclaims first and then OOM-kills inside the cgroup. Accepts 20G or bytes. Empty = the node default.\n\nUsually leave this empty or set it only as a generous emergency backstop. The panel's own memory limit already caps the game container; a slice-level max additionally caps the installer container and every future container of this server.\n\nA value above the host's physical RAM can never be reached and does nothing.",
            "env_variable": "WINGS_CG_MEMORY_MAX",
            "default_value": "",
            "user_viewable": false,
            "user_editable": false,
            "rules": "nullable|string|max:32",
            "field_type": "text"
        },
        {
            "name": "Wings Cgroup Startup Memory Min",
            "description": "Admin-only, STARTUP PHASE: memory.min while the server is loading. A higher floor during world generation and asset streaming protects what is being paged in from host-wide pressure, at a time when the server is at its most memory-hungry and least tolerant of reclaim.\n\nApplies only while the server is STARTING. Wings swaps in the steady value above as soon as the server reports ready -- it reuses the egg's own startup 'done' console match, so no extra configuration is needed -- or, if that line never appears, once docker.per_server_slices.startup_grace elapses (default 15m). Both events are logged with the slice name and the exact values applied.\n\nRequires patched Wings 0007 (image cgroup.7+). Empty = the steady value applies from the very start, which is the behaviour of every earlier build.",
            "env_variable": "WINGS_CG_STARTUP_MEMORY_MIN",
            "default_value": "",
            "user_viewable": false,
            "user_editable": false,
            "rules": "nullable|string|max:32",
            "field_type": "text"
        },
        {
            "name": "Wings Cgroup Startup Memory Low",
            "description": "Admin-only, STARTUP PHASE: memory.low while the server is loading. Rarely needed -- set the startup floor and ceiling first.\n\nApplies only while the server is STARTING. Wings swaps in the steady value above as soon as the server reports ready -- it reuses the egg's own startup 'done' console match, so no extra configuration is needed -- or, if that line never appears, once docker.per_server_slices.startup_grace elapses (default 15m). Both events are logged with the slice name and the exact values applied.\n\nRequires patched Wings 0007 (image cgroup.7+). Empty = the steady value applies from the very start, which is the behaviour of every earlier build.",
            "env_variable": "WINGS_CG_STARTUP_MEMORY_LOW",
            "default_value": "",
            "user_viewable": false,
            "user_editable": false,
            "rules": "nullable|string|max:32",
            "field_type": "text"
        },
        {
            "name": "Wings Cgroup Startup Memory High",
            "description": "Admin-only, STARTUP PHASE -- THE ONE THAT MATTERS: memory.high while the server is loading. Set it generously (64G on a 16G host simply means 'no ceiling yet').\n\nThis variable exists because a steady-state ceiling is actively destructive during startup. A game server's load-time peak is routinely several times its settled working set, and memory.high reclaim ignores memory.min -- so a ceiling sized for the steady state evicts the server through its own guaranteed floor while it is still loading its world, and it never recovers, because nothing faults those pages back except the workload. The result is a server pinned far below a floor that is applied, effective and verified.\n\nIf you set WINGS_CG_MEMORY_HIGH at all, set this one too.\n\nApplies only while the server is STARTING. Wings swaps in the steady value above as soon as the server reports ready -- it reuses the egg's own startup 'done' console match, so no extra configuration is needed -- or, if that line never appears, once docker.per_server_slices.startup_grace elapses (default 15m). Both events are logged with the slice name and the exact values applied.\n\nRequires patched Wings 0007 (image cgroup.7+). Empty = the steady value applies from the very start, which is the behaviour of every earlier build.",
            "env_variable": "WINGS_CG_STARTUP_MEMORY_HIGH",
            "default_value": "",
            "user_viewable": false,
            "user_editable": false,
            "rules": "nullable|string|max:32",
            "field_type": "text"
        },
        {
            "name": "Wings Cgroup Startup Memory Max",
            "description": "Admin-only, STARTUP PHASE: memory.max while the server is loading. Take care -- this is the OOM boundary, and startup is when the server needs the most headroom. Almost always leave empty.\n\nApplies only while the server is STARTING. Wings swaps in the steady value above as soon as the server reports ready -- it reuses the egg's own startup 'done' console match, so no extra configuration is needed -- or, if that line never appears, once docker.per_server_slices.startup_grace elapses (default 15m). Both events are logged with the slice name and the exact values applied.\n\nRequires patched Wings 0007 (image cgroup.7+). Empty = the steady value applies from the very start, which is the behaviour of every earlier build.",
            "env_variable": "WINGS_CG_STARTUP_MEMORY_MAX",
            "default_value": "",
            "user_viewable": false,
            "user_editable": false,
            "rules": "nullable|string|max:32",
            "field_type": "text"
        },
        {
            "name": "Wings Cgroup CPU Weight",
            "description": "Admin-only: cpu.weight (1-10000, default sibling weight is 100) for this server's slice. Overrides the node default. Empty = node default. Weights are relative to SIBLINGS only, and compose down the tree: this server's share of the host is (this / sum of siblings inside wings.slice) x (wings.slice's CPUWeight / sum of the root slices). They are also work-conserving -- a weight never caps anything, it only decides who yields when everyone wants the CPU at once. CPU is the exact axis: systemd passes cpu.weight through unscaled, so 1000 vs a sibling's 100 really is 10:1 (unlike IO -- see the two IO variables).",
            "env_variable": "WINGS_CG_CPU_WEIGHT",
            "default_value": "",
            "user_viewable": false,
            "user_editable": false,
            "rules": "nullable|numeric|between:1,10000",
            "field_type": "text"
        },
        {
            "name": "Wings Cgroup IO BFQ Weight",
            "description": "Admin-only, PREFER THIS ON BFQ NODES: the IO weight on BFQ's own 1-1000 scale (default sibling weight is 100) for this server's slice -- i.e. the number the scheduler actually uses, which you can read back at /sys/fs/cgroup/wings.slice/wings-<uuid32>.slice/io.bfq.weight. Requires patched Wings 0005 (image cgroup.4+); Wings converts it to the equivalent systemd IOWeight (100 + 11 x (bfq - 100)) and logs both, e.g. IOWeight=5940(io.bfq.weight=540). Mutually exclusive with WINGS_CG_IO_WEIGHT: if both are set, NEITHER is applied and Wings logs the conflict. Empty = node default (docker.per_server_slices.defaults.io_bfq_weight). See wings-cgroups/CGROUP-SEMANTICS.md Rule 7.",
            "env_variable": "WINGS_CG_IO_BFQ_WEIGHT",
            "default_value": "",
            "user_viewable": false,
            "user_editable": false,
            "rules": "nullable|numeric|between:1,1000",
            "field_type": "text"
        },
        {
            "name": "Wings Cgroup IO Weight",
            "description": "Admin-only, LEGACY SCALE -- prefer WINGS_CG_IO_BFQ_WEIGHT: systemd's IOWeight (1-10000) for this server's slice. Overrides the node default. Empty = node default. CAUTION: on a BFQ node this number is NOT what you get. BFQ schedules on io.bfq.weight (1-1000) and never reads io.weight (that file belongs to iocost, inert unless io.cost.* is configured), and systemd compresses 100-10000 into 100-1000 on the way -- so 1000 buys 1.8:1 against a default sibling, not 10:1, and 4950 buys 5.4:1. Mutually exclusive with WINGS_CG_IO_BFQ_WEIGHT. Check the scheduler with: cat /sys/block/vda/queue/scheduler.",
            "env_variable": "WINGS_CG_IO_WEIGHT",
            "default_value": "",
            "user_viewable": false,
            "user_editable": false,
            "rules": "nullable|numeric|between:1,10000",
            "field_type": "text"
        }
    ]
}
