services:
  probe:
    image: fedora:43
    container_name: desktop-probe-nobara-fedora
    tty: true
    stdin_open: true
    command: ["bash", "-lc", "sleep infinity"]
    environment:
      - TARGET_DISTRO=nobara
