# Load the zstd compression module early in userspace so zswap can use it.
# NOTE: zswap's built-in init runs at ~0.55s (before initramfs modules load),
# so it ALWAYS falls back to lzo first. The real fix is zswap-config.service,
# which re-selects the compressor post-boot once this module is available.
zstd
