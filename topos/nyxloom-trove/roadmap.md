# topos roadmap — pointer.

The product roadmap lives at `docs/ROADMAP.md` (referenced in `nyxloom.toml`
[refs]). This file tracks nyxloom-facing self-dev milestones for the topos
project specifically (trove hygiene, dispatch adoption) — currently none; the
`nyxloom-trove/` layout migration itself (2026-07-16) is the only milestone so
far.
