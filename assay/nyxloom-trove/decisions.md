# assay — decisions

One line per decision, with the reason. Reasoning in depth lives in
`docs/DESIGN-GUIDE.md`; this file is the record. Append, never rewrite; when a
decision is reversed, add a new line that supersedes the old one by id.

Session of 2026-08-06, base revision `d87f028b`.

## Decided

| id | Decision | Reason |
|---|---|---|
| A-001 | Name is **`assay`**; distribution stays estate-local wheels | PyPI `assay` is a third-party empty reservation (v0.0, zero files, homepage misprinted as python-skyfield). The estate publishes nothing to PyPI and already ships `topos` against a *real* 5-release collision. If a public index name is ever needed, publish under a different distribution name and keep `assay` as the import name. |
| A-002 | Build as a **new standalone project**; nyxloom untouched | Removes the sequencing conflict that deferred P90 behind nyxloom's core redesign. nyxloom's own migration is P90c, later. |
| A-003 | **v1 includes mutation** | Owner's call over the recommendation to defer it. |
| A-004 | **Mutation is sequenced last** in the carved series | Mitigates A-003's risk structurally: the adapter protocol is settled by coverage and canary first, so the most language-idiosyncratic component must fit the protocol rather than reshape it. |
| A-005 | **Zero runtime dependencies** — stdlib only | Makes the standalone claim near-unfalsifiable and the scratch-venv O1 test trivially offline. assay consumes coverage.py's *output*; it never imports coverage.py or pytest. |
| A-006 | Coverage parsing lives in a **parser registry keyed by format**, not in the adapter | Format and language are independent axes: lcov is emitted by five languages, TypeScript emits three formats. Binding them copies the lcov parser into every adapter — the four-copies divergence one layer down. |
| A-007 | Lane **declares** the coverage format; assay **sniffs** the artifact and **refuses on mismatch** | §4.2a prefers DERIVE then READ. The format is a fact of the lane's own argv, so it is read; sniffing is the cross-check that catches a lane whose argv changed and was not updated. |
| A-008 | `FileCoverage.excluded` is `frozenset | None`; `None` means the **format cannot express exclusions** | Otherwise a Go lane reports "0 changed lines excluded — verified" when the format simply cannot say. NO-MEASUREMENT discipline one level down. |
| A-009 | `LanguageAdapter` protocol defined in v1; **Python + Go adapters**, Go proven by committed fixtures | Keeps `ast` and `.py` globs out of the core (P90 O3's negative), so a third adapter is additive. Go is the honest second because its profile is block-based, not line-based. |
| A-010 | `inject_import_break` / `inject_uncovered_line` are **pure** — `(text) → (text, description)` | nyxloom's versions write the file themselves; pure adapters are testable with no filesystem. A straight improvement on the extracted code. |
| A-011 | `generate_mutants` may return `UNSUPPORTED` → **`INCONCLUSIVE_NO_MUTANTS`, never green** | TESTING-METHODOLOGY already names this outcome for zero selected mutants; an adapter that cannot mutate inherits it rather than inventing one. |
| A-012 | **Delete** nyxloom's `_derive_test_command` (`src/<mod>.py → tests/test_<mod>.py`) | §4.2a anti-pattern #2 — the consumer inventing on absence. It is a naming convention, not a language fact. The lane declares the argv. The only place v1 deliberately does not take the union. |
| A-013 | Adapters **may shell out**, but must declare it in `external_tools`; a missing tool is NO_MEASUREMENT for that check | Go's `has_executable_code` genuinely needs a parser. Declaring makes prerequisites checkable up front instead of failing mid-run. |
| A-014 | Lane file is **`assay.toml`** at project root | ciu reads nothing today — `ciu test` is explicitly a sketch that "does not exist today". Naming for an absent consumer repeats the "implies capability it does not have" failure. Neutrality is preserved in the schema (`[…where]`), not the filename. |
| A-015 | Lane file structure **is D7's three-question split**: top level = WHAT, `[…judge]` = HOW, `[…where]` = WHERE | Makes tool independence visible in the file's shape. A project adopting only one tool writes only its table. |
| A-016 | `source_roots` is **required, no default**; a nonexistent root is `BAD_LANE_CONFIG` at load | All four copies ship §4.2a anti-pattern #1, and a typo'd root matches nothing → 0/0 PASS forever, a laundering gate none of them guards. |
| A-017 | **Declared rigor is enforced**: R1 requires coverage config, R2 requires mutation config, R3 requires canary config, or the lane fails to load | A lane claiming a rigor level it cannot exercise is the lane-table-implies-capability failure in data form. |
| A-018 | `allow_excluded` is a **required** per-lane field | Resolves a genuine policy contradiction in the union: topos's failure text tells users to *add* `# pragma: no cover`; nyxloom and dstdns fail the gate for it. Each project must state its position at adoption. |
| A-019 | Lane `env` is **declared-only**, with an explicit `env_passthrough` allowlist | "Augment the ambient environment" is incoherent at S3, where the container already does not inherit it — the same lane file would mean different things at different scopes. The base is a fact of WHERE. |
| A-020 | `budget` exceeded is its **own outcome** (`BUDGET_EXCEEDED`, exit 4), not NO_MEASUREMENT | Different diagnosis, different remedy, different retry policy: NO_MEASUREMENT is deterministic and must never be auto-retried; a budget overrun is load-dependent and may be. |
| A-021 | **Six exit codes**: 0 PASS / 1 FAIL / 2 ERROR / 3 NO_MEASUREMENT / 4 BUDGET_EXCEEDED / 5 INCONCLUSIVE | Each already exists as a concept in the estate; none invented. 2–5 are all not-a-pass and all block a merge. |
| A-022 | A **required `reason_code`** enum names the cause on every non-PASS outcome | Gives cause-level specificity without proliferating exit codes. Answers "DIRTY_TREE vs BASE_IS_HEAD vs EMPTY_COVERAGE" without renaming the outcome class. |
| A-023 | Rollup precedence `ERROR > NO_MEASUREMENT > BUDGET_EXCEEDED > FAIL > INCONCLUSIVE`; PASS only if every declared claim passed | ERROR and NO_MEASUREMENT invalidate everything computed beneath them; a truncated run's FAIL may be partial. |
| A-024 | Artifact carries **one `claims[]` entry per declared rigor level**, not a flat verdict | "Record the evidence actually obtained rather than reducing quality to one coverage percentage." Also gives Tier 2/3 evidence somewhere to live. |
| A-025 | **No percentage is emitted when no measurement happened** — the coverage block is omitted, not zeroed | Emitting `pct: 100.0` beside NO_MEASUREMENT rebuilds the original ambiguity inside the artifact for any consumer that reads `pct` and ignores `outcome`. |
| A-026 | A **legitimate 0/0 pass emits the numbers plus a `considered` count** | srdm's fix: an empty denominator must explain itself in prose rather than being textually identical to a broken measurement. |
| A-027 | Verdict artifact is **emitted on every outcome, including ERROR** | Precedent: the remote-mutation-audit worker emits `summary.json` "even when a baseline, mutant, or teardown fails". Without it, "assay errored" and "assay never ran" are indistinguishable. |
| A-028 | Artifact is written **only when an explicit path is given** (`--verdict-json PATH`, or `-` for stdout) | No invented paths, no uninvited writes into a repo. The exit code always works standalone, so a consumer without the flag still gates correctly. |
| A-029 | Consumers read a **JSON Schema shipped as data**; nothing links against assay | D7's "judged-result contract, so `ciu test` can surface a verdict without linking against it". nyxloom's `GateResult` is a strict subset — six keys. |
| A-030 | **assay and ciu stay separate tools**; `ciu test` (if ever built) wraps assay via the verdict artifact | The boundary is topological, not preferential: assay runs **inside** the test container next to the coverage artifact; ciu runs **outside** issuing `docker run`. Merging ships an orchestrator into every test image or splits back in half at runtime. Ergonomics are identical either way. |
| A-031 | assay ships its **own CLI** (`assay run/verify/mutate/lanes`) | Forced by P90 O1 (a CLI invocation is the oracle), by all four existing copies, and by self-hosting, which cannot route through ciu without circular adoption. |
| A-032 | Evidence is carried in **three tiers**: COMPUTED / ADJUDICATED / ATTESTED | Keeps §0 intact while letting the whole evidence catalogue be expressed. Tier 2's judgement is the tool's; Tier 3 is never verified by assay. |
| A-033 | **Tier 3 (attested) slot, commit-binding and staleness check ship in v1** | The slot must exist from day one or retrofitting means a schema bump across every adopted consumer. Staleness is the cheap half and the half that gives an attestation teeth: assay cannot judge a review, but it can prove one is stale. |
| A-034 | **Tier 2 (adjudicated) slot ships in v1 with zero integrations** | Same schema-shape reasoning; costs almost nothing to reserve. A missing or crashed scanner must be ERROR or NO_MEASUREMENT, never PASS. |
| A-035 | Two guards **beyond the four-way union** ship in v1: `EMPTY_COVERAGE` and load-time `source_roots` validation | A zero-file coverage artifact currently renders 0% → FAIL, a *wrong diagnosis* ("add tests" meaning "your `--cov` path is broken"). A typo'd source root passes forever. Both were found by applying the §4.2a test to the existing copies. |
| A-036 | Runner flags **may be appended by the caller** and are recorded as `argv_declared` / `argv_appended` / `argv_effective`; `argv_modified: true` is set, and the lane must carry `allow_argv_append` | Owner's call, with transparency as the condition. assay still **never derives** flags — deriving them is impact-based selection, which is the caller's domain. |
| A-037 | Adoption order **topos → ciu → dstdns**; netcup-api-filter, cmru, mdt later | topos is the only migration with a baseline: old and new gate run on the same commits and must agree, so union fidelity becomes mechanical rather than a review question. |
| A-038 | **Adoption declares and verifies; it does not remediate** | Changed-line coverage is a ratchet, not an audit — netcup can adopt `fail_under = 100` at 30.17% global coverage. Conflating adoption with test-writing turns each consumer into an open-ended project instead of a bounded swap. |
| A-039 | Canary results at adoption are **diagnostic output**, and feed separate per-project remediation packages | A LAUNDERS finding measures a project's floor; fixing it is a different job with a different owner. |
| A-040 | **One gated lane, `tester-unified`**; the standalone claim is proven by a scratch-venv test *inside* it | "Greens from the interactive cockpit are explicitly not a ship signal." A bare-pytest lane manufactures exactly that pathway. Bare `pytest` stays a documented developer convenience, explicitly not evidence. |
| A-041 | The **independent oracle is pytest over expected-verdict fixtures**, not assay | Breaks the self-hosting circularity: a universal-PASS bug fails every fixture assertion at once. `assay verify` is a second, explicitly non-independent layer. |
| A-042 | Fixtures are **hello-world Python and Go projects** with committed pre-generated coverage artifacts; git-state fixtures materialised in `tmp_path` | Parsing profiles and injecting into Go source are pure text operations, so the suite needs no Go toolchain — mandatory, since this devcontainer has none. A regeneration script is documented for a host that has Go. |
| A-043 | **Go stays out of the devcontainer.** Promote srdm's `gate/Dockerfile` `unit` target to `vbpub/tester-unified-go` | The cockpit having no Go toolchain means it "cannot even pretend" to produce a ship signal — a property adding Go would destroy. The `unit` target is generic (pinned `golang:1.25`, run-uid identity, prewarmed std cache, `GOTOOLCHAIN=local`, caches outside the bind mount); the `e2e` target (systemd-in-Docker) is srdm-specific and layers on top. `tester-unified` untouched, as its own reasoning demands. |
| A-044 | A **`tools/go` wrapper** against `tester-unified-go` gives local ergonomics at the image's pinned version | Recovers the entire agent-productivity argument for a devcontainer toolchain, without the cockpit hazard, and makes the version explicit at the call site rather than ambient. |
| A-045 | A **matrix lane is two lanes**, not a `matrix` field | netcup's `[gates.unit]` loops one argv over two images. They have different WHERE and can render different verdicts; a loop that hides which image failed is the opacity being removed. |
| A-046 | **A lane file declares what exists, not a target architecture** | dstdns's five-lane table is aspirational; only two lanes are real. Writing the other three would make the file claim capability the project does not have. |
| A-047 | Docs split follows ciu: `nyxloom-trove/decisions.md` (record) + `docs/DESIGN-GUIDE.md` (reasoning) | The split already exists in ciu (`docs/DESIGN-NOTES.md` + trove decisions) and is the estate pattern. |

## Corrections to prior documents

| id | Correction | Evidence |
|---|---|---|
| A-C01 | The P90 handoff attributes `--allow-excluded` and the NO-MEASUREMENT guard **uniquely to dstdns**. Both are in nyxloom today; NO-MEASUREMENT is also in srdm's Go copy. | `git log src/nyxloom/coverage_gate.py`: `e03b0715` (pragma on changed line fails), `c7da4416` (NO MEASUREMENT, exit 3). File is 472 lines, not the 455 recorded. srdm: `checkMeasurable`, `exitNoMeasurement = 3`. |
| A-C02 | topos is described as "oldest/thinnest" with the implication it has nothing to contribute. **It is the sole holder of two checks.** | `_rel_to_source` directory-boundary matching (`topos/src/topos` must not match `topos/src/topos_evil`); `_validate_cov_record` (malformed coverage JSON raises rather than yielding a silent green). |
| A-C03 | The union contains a **policy contradiction**, not only feature gaps. | topos's failure text: *"or mark a genuinely unreachable line with `# pragma: no cover`"* — the exact act nyxloom and dstdns fail the gate for. Resolved by A-018. |
| A-C04 | "Adapter validated" and "consumer migrated" are conflated in the P90 consumer table. | The Go adapter is provable against committed fixtures with no srdm involvement; srdm's migration hinges on whether Python enters its toolchain, which should not gate the boundary proof. Resolved by A-042. |

## Open — NOT decided, and what blocks each

| id | Open question | Blocked on |
|---|---|---|
| A-O01 | Does `assay` get registered as a nyxloom project (`nyxloom.toml`, trove, gate id) before or as part of P01? | Chicken-and-egg: P01's own oracles name a gate that P01 creates. Needs a one-line ruling on whether the trove lands as a pre-package chore. |
| A-O02 | Does `tester-unified` need rebuilding to carry assay's test closure? | Likely **no** — assay has zero runtime deps (A-005) and needs only `pytest`/`pytest-cov`, which the image already has; probably only a `PYTHONPATH`. Needs confirming against the image before P01's gate is declared, because a rebuild re-risks ciu/cmru/topos/nyxloom's gate. |
| A-O03 | TypeScript adapter and the lcov/Istanbul parsers | Deferred. Unblocks when dstdns's `webapp-ui-react` is in scope for a lane; needs a decision on whether the TS adapter shells out to `tsc`/`node` or stays text-only. |
| A-O04 | Whether srdm migrates to assay at all | Depends on whether Python enters srdm's toolchain/container. Independent of the Go adapter (A-042). |
| A-O05 | Whole-project / sampled mutation, `--jobs` cap, budget-exhausted reporting | v2+. TESTING-METHODOLOGY already specifies the shape ("Suggested nyxloom enhancements"); needs a budget decision before it can be scheduled. |
| A-O06 | `fail-before/pass-after` as a computed (Tier 1) method | v2. Mechanically in reach — the canary machinery inverted (build the pre-fix commit, require the new test to fail there and pass at HEAD). Needs a package. |
| A-O07 | Serial/parallel coverage parity as a computed R3 method | v2. Cheap (run the lane serial and `-n auto`, compare executed-line sets) and reuses everything; the estate has never had it. Needs a package. |
| A-O08 | ClusterFuzzLite (or equivalent) as an async Tier 3 producer | Idea list. Maps onto the existing retroactive model — commit-addressed queue, `PENDING` claim status, claim-level enforcement. Needs the claim-level `enforcement` field designed. |
| A-O09 | Claim-level `enforcement` (a lane gating on R1 while an async claim is advisory) | Surfaced by A-O08; not needed until the first async producer exists, but the artifact schema should be checked for room before v1 freezes. |
| A-O10 | Which Tier 2 integration is built first, and its threshold vocabulary | Deferred by A-034. Needs a target tool chosen (SAST vs SBOM) before exit-code semantics and threshold policy can be designed. |
| A-O11 | Whether nyxloom's P90c migration reuses this decisions record or gets its own | Blocked on nyxloom's CORE REDESIGN reaching CR-14. |
| A-O12 | Provenance verification for S3/S4 lanes (image revision matches commit under test) | ciu's, not assay's. assay records `declared_unverified`; the check needs `org.opencontainers.image.revision` stamped at bake time, which ciu does not do today (D7 lists it as a gap). |
| A-O13 | mdt (`modern-debian-tools-python-debug`) adoption | Unconfirmed: no `tests/` tree found. Needs a look at whether it has a testable surface at all before it is placed in the adoption order. |
| A-O14 | Does `runner.write_verdict` need a new closed `ReasonCode` for output-artifact write failures (e.g. `--verdict-json` naming a path whose parent directory does not exist), or is "the caller ensures the output path is writable" an acceptable contract with the uncaught `OSError` left as the operator's own bug? | Found reviewing P04: no existing `ERROR` code fits "cannot write my own output" — `UNREADABLE_ARTIFACT` is about reading an input artifact (coverage, attestation), not writing the verdict itself. Low real-world severity (a caller typo, not an assay-correctness issue); not fixed now, since A-050 says stop and ask rather than invent a code alone. |

## Decided — session 2, 2026-08-06 (from P01's pre-flight)

The P01 pre-flight returned NOT READY with three blockers and seven ambiguities,
all of them defects in the specification rather than in the plan. Every entry
below is a controller ruling closing one of them. This is the propagate step in
`WORKFLOW.md` working as intended: the cost of finding these was one orientation
turn, against an implementation plus a review plus a rework.

| id | Decision | Reason |
|---|---|---|
| A-048 | The five `judge` fields are **conditionally** required (R1 declared), not unconditionally. An R0-only lane has no `[judge]` table and therefore no `source_roots`/`allow_excluded`. | P01's O1 listed them in a flat required list while Work item 6 required assay's own R0 lane to omit them — the package's own deliverable was a lane its own loader had to reject. A-018's "required per-lane" meant *per-lane rather than a global CLI flag*, never "at the lane's top level". |
| A-049 | `source_roots` resolve against **the directory containing `assay.toml`** (the project root), never the repo root. | The lane file must not need to know where it sits in a monorepo, and a project vendored one level deeper must not silently start measuring nothing. Reconciling git's two spellings (`--relative` is cwd-relative, `status --porcelain` is always repo-top-relative) is the core normaliser's job. Freezes the answer for P02/P04/P08, which is why it could not stay implicit. |
| A-050 | `BUDGET_EXCEEDED`'s `reason_code` is `LANE_TIMEOUT`. The reason_code enumeration is **closed**; an implementer needing an unlisted code stops and asks. | A-022 requires `reason_code` on every non-PASS outcome, but the DESIGN-GUIDE table omitted exit 4 entirely — the only occurrence in the estate was inside P07's O1. P01 would have had to either invent an enum member or contradict its own O4. |
| A-051 | `PASS` **omits** `reason_code` rather than carrying null. | A pass has no cause to name, and `null` invites a consumer to treat absence and emptiness as different states. |
| A-052 | `budget` is **parsed** at load, not merely checked for presence. | A malformed duration discovered at run time in P07 is a failure the config layer could have caught, which is the whole argument for a strict loader. |
| A-053 | Closed vocabularies for `scope` (S0–S4), `rigor` (R0–R3), `enforcement` (`gate`/`advisory`). | Neither `enforcement` nor the ladders had a stated vocabulary anywhere in assay's own docs; a loader cannot reject what was never enumerated. The ladders come from TESTING-METHODOLOGY §Axis 1/2 and are now cited in DESIGN-GUIDE §12. |
| A-054 | `assay lanes` renders NO verdict artifact. It raises a typed error carrying the reason code; the CLI maps it to an exit code. Artifact emission belongs entirely to P07. | A-027 ("emitted on every outcome") governs a lane RUN. `assay lanes` does not run a lane, and A-028 makes emission conditional on an explicit path that `assay lanes` has no flag for. |
| A-055 | P01 defines the claim **envelope** only (`source: computed\|adjudicated\|attested`, `status`, `verified_by_assay`); kind-specific payloads are P09's. | P09's `scope.touch` already includes `verdict.py` and `config.py`, so the two packages were about to design the same shape twice. The envelope is what all three tiers share and what the schema needs on day one; the payloads are not. |
| A-056 | `jsonschema` is added to the **test** extra. A-005 constrains RUNTIME dependencies only. | O4 needs a validator, `jsonschema 4.26.0` is already in `tester-unified:local`, and hand-rolling one would mean the test proves the hand-rolled checker rather than the schema. |
| A-057 | `pyproject.toml` sets `fallback_version` in `[tool.setuptools_scm]`, and `.gitignore` covers build residue. | Verified empirically in the gate image: with `root = ".."` and no `assay-v*` tag in the repo, installing from a copied tree fails with `LookupError: setuptools-scm was unable to detect version`, and installing in place writes `build/` and `src/assay.egg-info/` into the bind-mounted worktree — which would interact with P02's DIRTY_TREE tests and P11's self-hosting. |
| A-058 | Handoff `tier` is `implement-2` throughout. Which MODEL is dispatched is `WORKFLOW.md`'s table, not the tier field. | `sonnet5-high` is a **stale** tier name — the B16 rename moved the ladder to capability bands (`implement-1`, `implement-2`, `implement-1-free`, `review-3`), and `routes.host.toml` has no Opus *implementation* band at all (`claude-opus-high` sits under `review-3`, which is review and carve authority). Naming a band that does not exist, or borrowing a review band for implementation, would both be worse than stating the band honestly and letting the dispatcher record the model. |
| A-059 | P01 is **split** into P01a (config loader + CLI) and P01b (verdict model + JSON Schema). | Estimated at ~410–520 changed executable lines, at or over the series' ~400 carving limit, and the two halves share no code. More importantly they have different claims to attack: "does the config contract refuse to invent?" and "does the schema REJECT a malformed verdict?" — and the second is where the package's hollowness risk concentrates. |
| A-060 | Static dependency-purity checks are made by **AST walk** against `sys.stdlib_module_names`, never by grep. | P01's O5 grep was verified VACUOUS: `grep -rn` prefixes every line with a path containing "assay", and the `-v` alternation contains `assay`, so every line was filtered regardless of content. It passed clean on a file importing `requests`, `flask` and a function-level `boto3`. Two further defects even once the path issue is fixed: the `re` alternative matches any line containing that substring, and `^import` misses indented imports. An oracle that cannot fail is worse than no oracle. |

## Decided — session 3, 2026-08-06 (ratified from P01a's implementation and brief)

Batched deliberately. `decisions.md` is read inside the orientation snapshot, so
every edit here invalidates it; rulings therefore accumulate in the brief chain
and land together at a rebuild point. This is the first such point — the
snapshot does not exist yet, so applying them now is free.

| id | Decision | Reason |
|---|---|---|
| A-061 | Judge requirements are **per declared rigor level, not a cumulative ladder**. R2 and R3 additionally require `language` + `source_roots`. | `rigor` is a LIST of independent methods, and §6 gives one claim per declared level — so an R3-without-R2 lane must not be forced to declare mutation config it never runs. Requiring a fact nothing reads is the same hazard as inventing one. Ratifies P01a's reading; `JUDGE_FIELDS_BY_RIGOR` is the implementation. |
| A-062 | **Judge config for an UNDECLARED rigor level is refused.** | Overrides P01a's first reading, which allowed it on the grounds that the rigor list is the claim so surplus config claims nothing. True of the data, false of the reader: `fail_under = 100` on an R0 lane reads exactly like a coverage floor and is not one — nothing consumes it, so if it is wrong nothing fails (§4.2a). This project's subject *is* a declaration implying absent capability; dstdns's five-lane table over two real lanes is the specimen and this is its field-level twin. Also aligns the loader with §12, which already said an R0-only lane "has no `[judge]` table at all". |
| A-063 | An **empty** `[judge]` table is the only one an R0 lane may carry. | The exact boundary A-062 draws: an empty table declares nothing, so there is nothing surplus to refuse. Found while closing A-062's branches, having first been dismissed as a harmless edge. |
| A-064 | The loader additionally refuses: empty `rigor`, a duplicated rigor level, empty `argv`, empty `source_roots`, an absolute `source_root`, a file with no lanes, and unknown keys in a lane or `judge` table. `where`, `mutation` and `canary` sub-table CONTENTS stay opaque. | All correct and all previously uncited, so a later package could have diverged. The opacity carve-out is §7 holding: `where` is data assay parses and never interprets, and `mutation`/`canary` internals belong to P10/P08. |
| A-065 | `schema_version` is required in `assay.toml` and must equal `LANE_SCHEMA_VERSION` (1). It is **distinct** from the verdict artifact's own `schema_version`. | Two independently versioned contracts. Conflating them would couple a lane-file format change to every emitted artifact. |
| A-066 | **House style, binding on every later package**: `errors.py` owns `Outcome`, `ReasonCode`, `EXIT_CODES`, `REASON_CODES` — import them, never redefine. One test module per oracle, its docstring naming the oracle *and* the negative it defends. | P01a set these as the first package; nine follow, and consistency is cheaper to mandate than to retrofit. `errors.py` imports nothing, so anything may depend on it. |
| A-067 | **A test that proves a property must itself be checked by breaking the property.** | P01a's own dependency-purity test was partially vacuous — it installed with a `PYTHONPATH` exposing host site-packages to pip's resolver, so declaring `requests>=2` still installed clean. That is A-060 one level up, found only by mutating the thing the test existed to catch. Generalised here because A-060 was written about one grep and this is the class. Also: compare round-trips against an INDEPENDENT parser (`tomllib`), never against assay's own. |
| A-068 | **P03 owes the `judge.coverage.format` cross-check.** The loader accepts any non-empty string. | The vocabulary is "a key the parser registry knows" and the registry is P03's. Hardcoding a format list in the loader would recreate the four-copies divergence one layer down. Recorded as a debt rather than left as a hope. |
| A-069 | A-057's `fallback_version` is **declared but unexercised**, and built wheels version as `0.0.0`. | `setuptools_scm` is present in no interpreter in `tester-unified:local` (only `setuptools`, and only under `sys.base_prefix`), so the plugin never loads and the `LookupError` A-057 records is not reproduced by the suite. P11's scratch-venv proof depends on this; it is a known gap, not a surprise to discover there. |
| A-070 | Offline install in the gate image is **two steps in two environments**: `pip wheel --no-build-isolation --no-deps` with `PYTHONPATH=<setuptools dir>`, then `pip install --no-index <wheel>` with a CLEAN environment. | Putting that `PYTHONPATH` on the *install* leaks host site-packages into pip's resolver, which is precisely how A-067's vacuity happened. Recorded because P11 must repeat this exactly. |

| A-071 | **P04, P05, P08 and P10 each own their claim payload**: `src/assay/verdict.py` and `src/assay/schemas/**` are in their `scope.touch`, added as ADDITIVE branches following the pattern P01b states in the schema's own `$comment`. Widening the claim object to `additionalProperties: true` is forbidden — it deletes P01b's guard. | Found by P01b's readiness pass: only P01b and P09 could touch those files, while four later packages need fields there (P02 explicitly *forbids* `verdict.py`). Supersedes A-055's "payloads are P09's" — P09 owns attestation and adjudication, not coverage. A package that adds a rigor method should own its claim payload; forbidding that forces either speculative up-front design or a permissive schema, and both are worse. |
| A-072 | **A ruling delivered only in an agent message is not applied.** Every ruling lands in `decisions.md` and, where it changes scope or oracles, in the handoff files, before the next dispatch. | A-071 was ruled in P01b's go-message and reached exactly one agent; the four handoffs it governs were never edited, and the defect would have resurfaced at P04 as if it had never been diagnosed. Batching ratifications is correct; *deferring* them to a message is not the same thing, and the difference was invisible until checked. |

## Decided — session 4, 2026-08-07 (P01c contract repair)

| id | Decision | Reason |
|---|---|---|
| A-073 | An ordinary declared command that runs and exits non-zero is `FAIL/COMMAND_FAILED`; `EXEC_FAILED` remains inability to start the process. | R0 is itself a judged rigor claim: the command ran but did not pass. `TESTS_FAILED` would falsely make assay a test runner even though argv may name any tool. Supersedes A-050's closed reason set by adding the missing cause; A-021's six outcomes and exit codes do not change. |
| A-074 | Verdict schema v2 separates computed `claims[]` from sibling `declared_evidence[]` / `evidence[]`, whose exact identity is `(source, key)`. | Provenance (computed/adjudicated/attested) and rigor (R0-R3) are orthogonal. This supersedes A-024's claim that Tier 2/3 live in rigor-keyed claims and the remaining shared-envelope part of A-055. A-032's three tiers and A-024's exact coverage rule survive on their correct axes. |
| A-075 | An attested commit may equal the commit under test or be its ancestor. Ancestor evidence is current only when every declared reviewed path is byte-identical; changed reviewed paths render `NO_MEASUREMENT/STALE_ATTESTATION`, and absence renders `NO_MEASUREMENT/MISSING_ATTESTATION`. | Exact equality made staleness unreachable: there can be no changed interval after an attestation that is required to name HEAD. Assay records the evidence obtained and can reject stale evidence without pretending to judge the review. |
| A-076 | A bare `argv[0]` requires `PATH` in lane `env` or `env_passthrough`; an argv containing `/` does not. | `subprocess` with `env={}` may still resolve a bare executable through an implementation default. Without load-time rejection the lane depends on a fact it never declared, contradicting A-019. Assay's own lane explicitly passes PATH through. |
| A-077 | Assay's nyxloom gate reads `CGROUP_PARENT_DEV_BACKGROUND` and verifies the named host cgroup has a configured resource knob before launching Docker; unset, missing and unlimited/transient-looking slices refuse. | A hardcoded slice shadows the estate's authoritative environment, while an unknown slice name fails open into an unlimited transient unit. The verifier follows srdm's host-cgroup probe and has no slice-name fallback. |
| A-078 | Tier 2's sibling evidence shape is reserved in schema v2, but no adjudicator registry or unknown-adjudicator oracle ships until the first real integration. | A zero-integration registry would be decorative and could only validate its own empty set. The stable `(source, key)` envelope makes the first integration additive without claiming capability that does not exist. Supersedes the mechanism implied by A-034 while preserving its schema-compatibility goal. |

## Decided — session 5, 2026-08-07 (adversarial re-carve)

| id | Decision | Reason |
|---|---|---|
| A-079 | The P02–P11 handoffs at input `d87f028b` are withdrawn and superseded by P02–P14 at repaired input `9bd7d206`. | The prior series contained confirmed unfalsifiable/hollow oracles, scope contradictions, missing dependencies, stale references, and bundled claims. Dispatching it would knowingly waste implementation work on the pre-v2 contract. |
| A-080 | Independent expected-verdict conformance begins with the first producer in P04 and is an obligation of every later package that adds a producer branch; P14 audits completeness rather than introducing the discipline. | Deferring A-041 would leave most of the series protected only by assay's own model. Before P04, domain components do not emit verdicts and instead use independent literal input/output fixtures. |
| A-081 | Canary construction and execution remain one package, and success requires a passing control plus a valid known-bad case rejected for the configured cause. | These are two observations of one attackable claim. Splitting them produces two hollow halves; accepting arbitrary failure permits syntax/config breakage to masquerade as gate integrity. |
| A-082 | Mutation construction and execution are separate claims. The `jobs` bound is proved without wall-clock timing at the executor boundary: the actual executor factory must receive `max_workers=jobs` and all work must be submitted through it. | A valid mutant can exist without being run honestly, while a runner cannot repair an invalid experiment. Serial/parallel equality alone does not prove the cap; timing is machine-dependent. The stdlib executor owns the runtime concurrency guarantee once assay demonstrably supplies the declared bound. |
| A-083 | Standalone packaging and self-hosted conformance are separate packages. | A clean wheel can work while self-gating is circular, and a source checkout can self-test while the wheel is unusable. Each has an independent negative and distinct allowed files. |
| A-084 | The adapter protocol may gain statement-span, canary, and mutation capability only in the package that first proves each need; adding Go after spans must not edit the base protocol or evaluator. | Speculating all capabilities up front makes structural tests decorative. A fake adapter proves the initial language-free boundary; Python proves fidelity; Go is the first mechanical additivity test. Canary injection and mutation are capability-specific operations, not evaluation policy. |
| A-085 | P10 loads only attested evidence into the schema-v2 sibling arrays; adjudicated evidence remains reserved with no registry or loader. | Preserves A-034's additive consumer shape and A-078's refusal to ship an unattackable zero-integration mechanism. |
| A-086 | P08 adds `MISSING_EXTERNAL_TOOL` to the NO_MEASUREMENT reason set when it first ships an adapter with a declared parser prerequisite. | DESIGN-GUIDE §11 already requires a missing adapter tool to be NO_MEASUREMENT, but the closed A-050/A-073 vocabulary has no truthful cause for it. `EMPTY_COVERAGE` and `EXEC_FAILED` would both lie. The additive reason and its independent full artifact belong to the package that first makes the state reachable. |
| A-087 | **Supersedes A-086 before dispatch:** P08 ships no external Go prerequisite. Its narrow `has_executable_code` lexer/parser recognizes function bodies and returns strict `true` on malformed or unknown input; it is proven from committed text with no Go toolchain. | A packaged `go run` helper would be uncompiled and therefore unproved in tester-unified, merely moving the gap. The semantic question is much narrower than compiling Go and can be parsed conservatively: false is returned only for confidently function-body-free files. No missing-tool state becomes reachable, so adding its reason would be decorative. |
| A-088 | The two landed packages are renamed **`assay-P01a` → `assay-P00`** and **`assay-P01b` → `assay-P01`** (handoffs, LOGs, BRIEFs, and every `depends_on`). Letter-suffixed package ids are not permitted. | nyxloom's `handoff-frontmatter.schema.json` enforces `id` matching `^[a-z][a-z0-9]*-P[0-9]{2,4}(-[a-z0-9-]+)?$` — no letter after the digits. The split invented `P01a`/`P01b` on the strength of the nyxloom-P90 handoff *proposing* "P90a/P90b" in prose; the prose is not the schema. Both were merged non-conformant. Renumbering to P00/P01 rather than shifting the reissued series keeps thirteen just-written packages untouched, which is the cheaper and lower-risk direction. |
| A-089 | **The trove's own linter is part of review, not a formality.** Every reissued or renamed handoff is validated against nyxloom's real frontmatter schema before merge, and dangling `depends_on` is checked across the whole series. | A-088's defect existed for the entire life of the split and was invisible because the linter was never run — by the carver, the two implementers, or the controller. It surfaced only when an external reviewer ran it and the controller re-ran it to verify the claim. A project whose subject matter is "gates that do not actually check" shipped a trove that failed its own tool's schema. |

## Decided — session 6, 2026-08-07 (P02 readiness pass)

| id | Decision | Reason |
|---|---|---|
| A-090 | **P05 owns wiring P02's `DIRTY_TREE`/`BASE_IS_HEAD` guards and P03's `EMPTY_COVERAGE` guard ahead of the four-way coverage evaluation, proven end to end against hand-written expected-verdict fixtures.** P05's handoff gains an O4 requiring this; scope is unchanged since it already touches `runner.py` and depends on P02+P03+P04. | The P02 readiness pass found that `git.py`/`measurability.py` appear in no other package's `scope.touch`, and no package's oracle set exercises the guards they build: P04's O3 only proves the artifact writer accepts an already-constructed `NO_MEASUREMENT` verdict; P05's O3 as written names only PASS/FAIL. A capability built with no package scoped to consume it is the same shape of gap A-071 closed one layer up, and it is cheaper to fix in the carving than to discover at P14. |
| A-091 | **P02's `parse_added_lines` and the measurability guard return frozen, `kw_only` dataclasses — never bare `dict[str, set[int]]` or the sibling copies' primitive shapes — and raise `AssayError` directly, never a locally-defined subclass.** | A-080 already calls these "typed domain results"; A-066 already sets frozen-dataclass house style project-wide (`Lane`, `JudgeConfig`, `Claim`) and mandates importing `errors.py`'s vocabulary rather than redefining it. The handoff's oracles cite three sibling implementations that all return bare dicts, and `errors.py` sits outside P02's `scope.touch`, so an implementer copying the cited code literally would produce a shape inconsistent with the rest of the codebase without this made explicit. |

## Decided — session 7, 2026-08-07 (P03 readiness pass)

| id | Decision | Reason |
|---|---|---|
| A-092 | **A-091 generalizes to every remaining package, not only P02.** Every typed domain function returns frozen `kw_only` dataclasses, never a bare `dict`/Go-map/primitive shape lifted from a cited sibling implementation, and raises `errors.AssayError` directly. A locally-defined `AssayError` subclass is legitimate only inside a package whose `scope.touch` includes `errors.py`, AND only to collapse call-site repetition of an EXISTING `(Outcome, ReasonCode)` pair — the `LaneConfigError` precedent — never to introduce a new pairing or bypass the closed vocabulary. | P03's readiness pass re-found A-091's exact shape independently: every one of its cited sibling implementations (dstdns's `_load_coverage -> dict[str, dict]`, topos's `_validate_cov_record` on plain `dict`, srdm's `FileCoverage{Executed map[int]bool, ...}`) uses the forbidden shape, and A-091 was worded around P02's two named functions rather than as a project rule. Left implicit, this same finding would be rediscovered at every remaining package's readiness pass instead of settled once. |
| A-093 | **P03 owes a named, independently callable `EMPTY_COVERAGE` guard** — same shape as P02's `check_dirty_tree`/`check_base_is_head`: raises `AssayError(NO_MEASUREMENT, EMPTY_COVERAGE)` on the adverse case, returns a typed value (or `None`) on the clear case, and lives somewhere P05 can call it directly, not folded as an internal branch of a combined parse-and-evaluate entry point. | P05's O4 (A-090) already requires calling "P03's empty-coverage guard" ahead of the four-way union, the same way it calls P02's guards. P03's own O4 describes only the OUTCOME, not an interface — as literally written, an implementer could satisfy it inside a single `load_coverage()` call P05 has no way to invoke standalone, reproducing the exact "capability with no scoped consumer" gap A-090 closed for P02, this time caught before implementation. |

## Decided — session 8, 2026-08-07 (P04 readiness pass)

| id | Decision | Reason |
|---|---|---|
| A-094 | **P04's `runner.py` exposes the R0 command-execution step as a discrete function, separable from final verdict assembly** — not one monolithic function that runs the command and immediately builds a `Verdict`. | P05's own handoff already assumes this: it depends on P04, touches `runner.py`, and its work item 3 inserts an R1 evaluation step (P02's/P03's guards, then the four-way union) "into the runner" between command execution and verdict construction (A-090). Nothing in P04's own O1-O4 requires this separability, so as specified an implementer could satisfy every P04 oracle with a single non-extensible function P05 would then have to restructure — the same "capability built with no scoped consumer" gap A-090 and A-093 already closed twice at this exact P0x→P05 seam, caught here before implementation rather than during P05's. |
| A-095 | **Rejecting an `argv` append made without the lane's `allow_argv_append` is `ERROR`/`EXEC_FAILED`.** The process never starts, which is exactly A-073's definition of `EXEC_FAILED` ("inability to start the process"), and O2's own text already says the rejection happens "before execution." | The closed reason vocabulary (A-050) has no code that names "caller requested an unpermitted append" directly, and P04's handoff's own `escalate_if` anticipates exactly this gap ("an outcome cannot be represented by the closed reason vocabulary"). `EXEC_FAILED` is the only existing code whose definition already covers it; inventing a new one is unnecessary. |

## Decided — session 9, 2026-08-07 (P05 readiness pass)

| id | Decision | Reason |
|---|---|---|
| A-096 | **The R1 claim's `Coverage` payload gains two additive fields**: `missing_lines: Mapping[str, frozenset[int]]` (changed executable, non-excluded lines that were not executed, keyed by path — same shape as P02's `AddedLines.by_file`) and `files_missing_coverage: tuple[str, ...]` (changed files under the source roots contributing executable lines with no entry at all in the coverage artifact, sorted). Both are always present on an R1 claim (possibly empty), never conditionally omitted. | O3 requires "missing locations" in the R1 claim, but `Coverage`/the schema's `coverage` `$def` carry no such field — the `9bd7d206` repair deleted the schema's own `$comment` naming this exact gap (*"P04 and P05 extend this object additively (files_missing_coverage, excluded, unclassified)"*) without adding a replacement, leaving no sanctioned shape to build against (§4.2a forbids inventing one). All three cited sibling gates already carry this concept (`uncovered: dict[str, set[int]]`, `files_missing_coverage: list[str]`), confirming it is real prior art, not a P05 invention. |
| A-097 | **P05's `LanguageAdapter` protocol (`adapters/base.py`) defines exactly** the five non-method attributes DESIGN-GUIDE §11 lists (`name`, `source_globs`, `excluded_dir_names`, `requires_span_attribution`, `external_tools`) and three methods (`is_test_path`, `has_executable_code`, `normalize_coverage_key`) — never `statement_spans` (P07), `inject_import_break`/`inject_uncovered_line` (P09), or `generate_mutants` (P11), each added only by the package that first needs it (A-084). | Confirmed by auditing every later package's `scope.touch`: P06 and P08 (the two real adapters) never touch `adapters/base.py` at all, so whatever protocol P05 ships is final until P07/P09/P11 extend it. An implementer following only §11's flat seven-capability list, or work item 1's "smallest... protocol needed," without this audit could over-build (violating A-084) or under-build (blocking P06/P08, which have no scope to add a missing method themselves). |

## Decided — session 10, 2026-08-07 (P06 readiness pass)

| id | Decision | Reason |
|---|---|---|
| A-098 | **P06's O1 drops "multiline statements" from its construct list.** Genuine multi-line statement interior-line attribution is P07's `statement_spans` addition (A-084) and structurally unreachable within P06's frozen protocol (A-097 — no scope to touch `adapters/base.py`). P06 proves single-line-reported executable/excluded classification for decorators, async/compound statements, docstrings, comments, and pragma tokens. | `evaluate.py`'s own docstring already states this ("No multi-line statement attribution... both are P07's addition"); O1 as carved asked for something P06 cannot deliver without exceeding scope, tripping its own `escalate_if`. Checked against the three cited reference gates: only dstdns achieves multiline attribution, via exactly the AST `statement_spans` mechanism reserved for P07; topos and nyxloom both drop those lines the same way P05's core already does by design, so P06 can at most match the coarser two, never dstdns. |
| A-099 | **P06's O2 is specifically about `LanguageAdapter.normalize_coverage_key`'s own prefix-strip boundary safety** — never a retest of `evaluate.py`'s already-proven, adapter-independent source-root boundary matching (`_is_considered`'s `Path.is_relative_to` check). A test satisfying O2 must be one whose mutation lives inside `python.py`; a mutation the implementer cannot even attempt confirms O2 was read correctly. | Read as a core retest, O2 is vacuous for P06's own diff: P05's own merged test already proves the sibling-prefix property (`pkg` vs `pkg_evil`) using a `FakeAdapter`, with `evaluate.py` — which P06 cannot touch — doing all the work. Any adapter, correct or buggy, would pass that reading regardless of what `python.py` does, the same A-060-class vacuity this project specifically watches for. The reading that survives matches DESIGN-GUIDE §11's own split: boundary reconciliation is the core's job, the language-specific prefix STRIP is the adapter's. |

## Decided — session 11, 2026-08-07 (P07 readiness pass)

| id | Decision | Reason |
|---|---|---|
| A-100 | **Genuinely unattributable, overlapping, or malformed statement spans render `FAIL`/`UNCLASSIFIED_LINES`** — the existing, currently-unused reason code — never a new `INCONCLUSIVE` pairing. | P07's own O2 named "INCONCLUSIVE/UNCLASSIFIED", a pairing that does not exist: `UNCLASSIFIED_LINES` is closed to `FAIL` only, `INCONCLUSIVE` is closed to `NO_MUTANTS`/`CANARY_INCONCLUSIVE` only (`errors.py`'s `REASON_CODES`), and `errors.py` sits outside P07's `scope.touch` — a new pairing would need a forbidden-file edit on top of a vocabulary invention A-050 already forbids without asking. `FAIL` fits the outcome semantics better: DESIGN-GUIDE §6 defines `INCONCLUSIVE` as "ran; instrument rendered no judgement" (the whole claim mechanism produces nothing — `NO_MUTANTS`, `CANARY_INCONCLUSIVE`), while span attribution renders a real judgement for the overwhelming majority of lines and only flags a subset — structurally identical to `EXCLUDED_LINES`, not to "no judgement at all". dstdns's own cited reference implementation agrees: its `unclassified` bucket is an unconditional `FAIL` ("diff-coverage FAIL: ... could not be attributed"), with zero occurrences of "INCONCLUSIVE" anywhere in the file — the handoff's own behavioral reference contradicts O2's literal text. |
| A-101 | **`statement_spans` returns `tuple[StatementSpan, ...] \| None`**, where `StatementSpan` is a new frozen `kw_only` dataclass (`start_line: int, end_line: int`) added to `adapters/base.py` alongside the method — never dstdns's bare `list[tuple[int, int]]` (A-092's project-wide convention, not the cited sibling's literal shape). `None` means the adapter performs no span attribution, paired with `requires_span_attribution=False`; `evaluate.py` calls this method only for an adapter whose `requires_span_attribution` is `True`. "Overlapping" spans (O2) is a defensive guard against an adapter's own analysis being internally inconsistent, not a naturally-occurring real-Python case — correct nesting is resolved deterministically by containment, never flagged as ambiguous — so its test is a hand-built list of spans fed directly to the pure attribution function, not derived from parsed source. | P07's own protocol addition (the one deliberate post-P05 extension, A-097) had no pinned return shape anywhere — DESIGN-GUIDE §11 only sketches `statement_spans(text) -> spans \| None` with "spans" left untyped. Left unpinned, an implementer would either invent a shape nothing later reviews against, or copy dstdns's bare-tuple-list shape, repeating the exact class of gap A-091/A-092 already closed twice. Confirmed real, natural "unattributable" trigger exists (a changed interior line whose enclosing span is itself untracked, dstdns's own documented case) — only "overlap" needed this clarification. |

## Decided — session 12, 2026-08-07 (P08 readiness pass)

| id | Decision | Reason |
|---|---|---|
| A-102 | **Go declares `requires_span_attribution=False`.** Its coverprofile format is block-based (`covergate/profile.go`'s `ParseCoverProfile` expands every line in `[start, end]` individually into `Executed`/`Missing`, matching P03's own `go_cover.py`), so it structurally lacks Python's "interior line of a multi-line statement vanishes" gap — confirmed directly against the real parser, not assumed. `evaluate.py`'s span-attribution path (and `FAIL`/`UNCLASSIFIED_LINES`) is therefore never reached for Go; "an unknown Go region" is instead proven through O4's fail-closed `has_executable_code` (already correctly specified there), which the corrected O2 now points to explicitly instead of duplicating. | P08's original O2 required "an unknown Go syntax region becomes UNCLASSIFIED", but `unclassified_lines` is populated in exactly one place in `evaluate.py` (rule 3b), gated on `requires_span_attribution`. With it correctly `False`, that clause is undeliverable; declaring `True` to force it would require manufacturing a synthetic ambiguity Go's real format never produces (misusing `adapters/base.py`, off-limits per P08's own scope) and would fail O1's own "narrow deterministic parser, proven from committed text" standard. P07's own successor brief already worked out and justified this exact answer — P08's handoff just never cited it (see A-103). |
| A-103 | **P08's "Context to read first" is corrected**: item 2's "`adapters/base.py`... A-097 — exactly five attributes, three methods" is stale (P07/A-101 already extended the protocol to four methods) and its `assay-P05-BRIEF.md` pointer is superseded by `assay-P07-BRIEF.md`, written specifically for P08 and already containing the verified Go-block-format finding behind A-102. | Found reviewing the handoff for the A-102 question: the citation was correct when P08 was originally carved (before P07 landed) and was never refreshed. An implementer following it literally would miss the one document that already solves A-102's question, compounding the O2 defect rather than resolving it. |
| A-104 | **P08's Go lexer/parser must correctly skip line comments (`//`), block comments (`/* */`), interpreted strings (`"..."`, with escapes), raw strings (`` `...` ``), and rune literals (`'...'`) before searching for a word-boundary `func` token, and must not misclassify a generics type-parameter bracket** (`func f[T any](...)`) as anything else. Walk only TOP-LEVEL declarations, matching `PythonAdapter.has_executable_code`'s own `tree.body`-only convention. | Sol's own flagged lowest-confidence area in this project (STATE.md): "a thin fixture corpus would make it correct only on toy Go." Go generics syntax is not hypothetical — it already exists in this very estate's own `shared-ramdisk-depot-manager/tools/covergate/profile_test.go:137` (`func keysOf[T any](...)`) — and none of A-087's prior text names it. Enumerating the hazard list explicitly, the same way A-098 enumerated Python's construct family for P06, is cheap now and costly to discover mid-implementation for the package this project's own external review already flagged as its weakest spot. |

## Decided — session 13, 2026-08-07 (P09 readiness pass)

| id | Decision | Reason |
|---|---|---|
| A-105 | **P09's "Context to read first" is corrected**: drop the `shared-ramdisk-depot-manager/tools/covergate/` pointer for "canary behavior" (zero occurrences of "canary" anywhere in that directory — verified by grep) and cite `/workspaces/vbpub/nyxloom/src/nyxloom/gate_canary.py` instead, which already implements `inject_import_break`/`inject_uncovered_line` with the exact mechanisms A-010/DESIGN-GUIDE §11 gesture at. | nyxloom's versions are IMPURE (write the file directly via `path.write_text`) — exactly what A-010 forbids P09 from copying — but the insertion mechanics (module-docstring/`__future__`-aware insertion point for the import-break; a minimal, side-effect-free appended function for the uncovered-line) are the real, directly reusable prior art an implementer following the handoff's original citation would never find, and would likely reinvent from nothing instead. |
| A-106 | **`config.py` is removed from P09's `scope.touch`.** `JudgeConfig.canary: Mapping[str, Any] \| None` already exists, predates P09, and the loader already fully parses and round-trips it as an OPAQUE table (verifies only that it is a table) — confirmed by the already-passing `CANARY_TABLE` fixture in `tests/test_config_rigor.py`. `canary.py` interprets whatever shape it needs from that opaque mapping itself; nothing in O1-O3 requires `config.py`'s own schema to change. | The same class of accidental over-scoping A-090's own readiness pass found in `registry.py` for P06/P08 — `scope.touch` correctly PERMITS but does not by itself justify touching a file; verifying against the actual, already-merged loader shows this one is unnecessary. |
| A-107 | **P09's Go-language canary proves R1 (coverage-evaluation) sensitivity ONLY** — two committed, pre-generated coverprofiles (a clean control and one with the transform's specific line(s) newly marked missing) fed through the real `evaluate_coverage`/`evaluate_r1` pipeline with the real `GoAdapter`. It does NOT attempt a real R0 (command-execution) run for Go. Python's canary proves the FULL real pipeline, R0 (a genuine `execute_command` pytest run) plus R1, since pytest is genuinely available in `tester-unified`. `GoAdapter.inject_import_break` needs only a structurally-valid, best-effort implementation to satisfy the protocol's shape — it is not exercised by this R1-only Go canary, the same way `statement_spans` exists on `GoAdapter` but is never called (A-102). | No Go toolchain exists anywhere in this devcontainer (A-042/A-043/A-087) — a real R0 Go run is categorically unavailable, and scripting/injecting a fake R0 result via `execute_command`'s `process_runner` seam would substitute a hand-picked expected value for a genuine measurement, undermining O1's own negative ("accepting any non-zero as success lets an unrelated syntax/config error satisfy the oracle") one layer up: a scripted result IS an unrelated, non-measured signal. Matches the established, already-proven "Go fixtures ship pre-generated, no toolchain needed" doctrine (A-042), applied to the canary's own proof rather than to coverage parsing. |
| A-108 | **The canary result attaches as `Claim.canary: CanaryResult \| None`**, gated to `rigor == "R3"` — the exact pattern `verdict.py` already enforces for `Claim.coverage`/`rigor == "R1"`. `CanaryResult` is a new frozen `kw_only` dataclass (A-092) carrying at minimum: control outcome, transformed outcome, transform identity, and expected vs. observed reason (O2's own four named fields) — exact field names are the implementer's call, the attachment point and rigor-gating are not. | O1-O3's text never says "R3"/"rigor" explicitly, but DESIGN-GUIDE §12 already requires `judge.canary` for R3 specifically, and A-024's "one claim per declared rigor level" makes R3 the only coherent home — leaving this unpinned risked two implementers building incompatible shapes (O2's "an independently written schema-valid artifact", singular, is equally consistent with a shape OUTSIDE `Verdict`/`Claim` entirely), the same class of gap A-096 closed for P05's R1 payload. |
| A-109 | **O3's three named cases, plus O1's own fourth ("fails for the wrong reason"), map onto the two already-existing reason codes**: a malformed transform or a no-op transform (nothing to judge) render `INCONCLUSIVE`/`CANARY_INCONCLUSIVE`; a bad case that unexpectedly passes, OR fails for a reason other than the configured expected one, both render `FAIL`/`CANARY_SURVIVED` — "survived" means the specific intended defect was never proven caught, whether by passing outright or by failing for an unrelated cause. | Gives both existing, previously-unused codes a clean, non-overlapping job matching their own established semantics elsewhere (`CANARY_INCONCLUSIVE` mirrors `NO_MUTANTS`'s "nothing to judge"; `CANARY_SURVIVED` mirrors `MUTANTS_SURVIVED`'s "wasn't caught"), and closes the wrong-reason case O1's own negative names but O3's text never explicitly covers — the same class of gap A-100 closed for P07's `UNCLASSIFIED_LINES` question. |
