# Assay B092+B098 adversarial review — R1 — 2026-09-13

Verdict: **ACCEPT**

Reviewed implementation commit `176a06a5e095e92cc94a72fc36658c5e2e343c11`
in `/workspaces/vbpub/.worktrees/assay-b092-b098`. This was a review-only
round: no product files were changed, no merge was performed, and no mutation
R2 campaign was started.

## Evidence

- `git rev-parse HEAD` returned
  `176a06a5e095e92cc94a72fc36658c5e2e343c11`. The only pre-existing worktree
  change was the untracked review handoff named in the request. The complete
  commit diff was reviewed, including implementation, tests, README,
  DESIGN-GUIDE, CONSUMERS, CHANGES, backlog, and the schema-v2 brief.
- `git diff --check 176a06a5e095e92cc94a72fc36658c5e2e343c11^ 176a06a5e095e92cc94a72fc36658c5e2e343c11`
  produced no output.
- Focused serial tests:

  ```text
  python -m pytest -q tests/test_config_mutation.py \
    tests/test_config_ingested_mutation.py tests/test_mutation_judge.py \
    tests/test_mutation_judge_identity.py \
    tests/test_docs_examples_and_vocabulary.py
  192 passed in 9.54s
  ```

- Live loader probes from the reviewed worktree produced:
  - `identity_exclude = ["nyxloom-trove//**"]` normalized to
    `('nyxloom-trove/**',)`.
  - Omitted `identity_exclude` loaded as `None`.
  - Ingested R2 rejected `identity_exclude` with the expected
    `identity_exclude ... forbidden on an INGESTED lane` diagnostic.
  - String, non-string, empty, absolute, backslash, `..`, `.`, and NUL
    patterns all refused with `LaneConfigError`.
- Live digest probes produced:
  - `legacy.matches_parent_formula True`.
  - `filtered.excluded_only_unchanged True`.
  - `filtered.included_change_differs True`.
  - `filtered.case_sensitive True`.
  - `filtered.omitted_excluded_unchanged True`.
  - `filtered.omitted_included_differs True`.
  - `explicit_empty_distinct True`.
  - `non_tree_input_still_identity True`.
  These establish that `None` preserves the legacy digest, an explicit empty
  domain is separate, only matching tree/omitted paths are filtered, matching
  is case-sensitive, and argv identity remains in the final judge digest.
- Live B098 probe constructed one outcome in each member of the shipped
  `MUTATION_BUCKETS` vocabulary and returned `b098.score 50.0`, confirming
  `killed/(killed+survived)` with one killed and one survived outcome. The
  docstring probe returned `True` for each excluded canonical bucket:
  `crashed`, `budget_exceeded`, `equivalent`, and `hung`.
- Schema-v2 examples in README, DESIGN-GUIDE, and CONSUMERS were exercised by
  the shipped documentation test and all passed loader parsing. An independent
  local-link probe using the repository's GitHub-slug rule checked 51 local
  anchor links across those three documents and found no dangling anchors.

## Contract assessment

B092 preserves the pre-feature hash byte-for-byte when the key is omitted,
defines a distinct versioned filtered domain for declared patterns, filters
both included and omitted manifest paths without filesystem access, and keeps
the same resolved judge identity for resume reads and writes. Loader scope is
native R2 only; malformed patterns and ingested declarations fail loudly.

B098 leaves the arithmetic unchanged while documenting the complete excluded
bucket vocabulary. The tests exercise vocabulary membership, arithmetic, and
terminal-bucket behavior. No review defect or release blocker was found.
