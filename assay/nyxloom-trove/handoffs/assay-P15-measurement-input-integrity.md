---
schema_version: 1
id: assay-P15-measurement-input-integrity
project: assay
title: "Repository and coverage inputs preserve every identity exactly once"
tier: implement-2
input_revision: "9782ddf906bbaead3966934bb7ea143761064f77"
source: {kind: product-goal, ref: "docs/DESIGN-GUIDE.md"}
stack: none
depends_on: []
session: resume:assay-v11-integrity
scope:
  touch: ["src/assay/config.py", "src/assay/diff.py", "src/assay/git.py", "src/assay/measurability.py", "src/assay/evaluate.py", "src/assay/coverage_parsers/**", "tests/**"]
  forbid: ["src/assay/cli.py", "src/assay/verdict.py", "src/assay/verify.py", "src/assay/schemas", "src/assay/mutation.py", "src/assay/canary.py", "src/assay/attestation.py"]
oracles:
  - id: O1
    observable: "Real Git fixtures containing a source line beginning '++', a no-newline marker, renames, spaces, tabs, Unicode, and an embedded newline produce the exact new-side line and dirty-path identities"
    negative: "Restoring display-format parsing or treating any hunk-body '+++' line as a file header loses or shifts at least one named identity"
    gate: tester-unified
  - id: O2
    observable: "Overlapping coverage buckets and two raw keys normalizing to one repo path are rejected before judgment; reversing input order cannot change a verdict"
    negative: "Last-key-wins normalization or executed-wins overlap makes one paired fixture PASS"
    gate: tester-unified
  - id: O3
    observable: "A source root resolving outside project_root and an env name declared in both env and env_passthrough fail lane loading"
    negative: "Removing either containment or collision check loads the adversarial lane"
    gate: tester-unified
gates: ["tester-unified"]
escalate_if:
  - "lossless Git path transport requires changing the public verdict schema"
  - "a legal coverage format cannot distinguish data that the common model requires to be disjoint"
mutexes: []
---

# P15 — measurement input integrity

The claim to attack: **repository and coverage inputs preserve every identity exactly once before assay judges them.**

## Worktree and branch

Work only in `/workspaces/vbpub/.worktrees/assay-P15-measurement-input-integrity`
on branch `feat/assay-P15-measurement-input-integrity`.

## Context to read first

1. `docs/DESIGN-GUIDE.md` §§5, 6, 11, and 12; decisions A-006, A-008, A-016, A-019, A-041, A-049, A-067, A-080, A-090–A-102.
2. `src/assay/diff.py`, `src/assay/git.py`, and `src/assay/measurability.py`; mirror the real-git-fixture pattern in `tests/test_git.py`, `tests/test_diff.py`, and `tests/test_measurability.py` rather than mocking Git output.
3. `src/assay/coverage_parsers/model.py`, every parser beneath `src/assay/coverage_parsers/`, and `src/assay/evaluate.py`; read `tests/test_evaluate_language_free.py` and parser tests for the current common-model contract.
4. `src/assay/config.py`'s `_resolve_source_root` and lane environment validation, plus `src/assay/runner.py`'s `resolve_command_plan` for the overwrite this package prevents without editing runner.
5. `nyxloom-trove/reports/assay-v1-post-series-review-sol.md` findings 3–5 and 9. Reproduce the recorded false-PASS/line-shift cases before changing code.
6. `/workspaces/vbpub/nyxloom/reference/DOCTRINE.md` gate, evidence, defaults, and mutation-proof rules.

## Work

1. Replace `parse_added_lines`' display-line heuristic with an explicit unified-diff state machine. Recognize file headers only in header state, hunk additions only in hunk state, and Git's no-newline marker as metadata that advances neither side.
2. Add real Git fixtures proving exact new-side line numbers for a valid added source line whose content begins `++`, an EOF no-newline replacement, deletions, renames, and multiple files/hunks. Expected line maps must be written independently, never generated by `parse_added_lines` itself.
3. Add a lossless Git-output boundary for pathname-bearing commands. Use NUL-delimited status/diff forms and a deliberate decode-or-reject policy; do not parse Git's quoted display syntax. Prove staged, unstaged, untracked, renamed, spaced, tabbed, Unicode, and newline-containing paths against a real repository.
4. Enforce the common coverage model before evaluation: line numbers are positive; executed, missing, and known excluded buckets are pairwise disjoint; and normalizing two distinct raw keys to the same repository path is an error, never last-key-wins. Preserve `excluded=None` as distinct from an empty known set.
5. Reject every declared source root whose resolved path is not beneath-or-equal to the resolved project root, including `..` and symlink escape. Do not replace that fact with a lexical-prefix check.
6. Reject a lane that declares the same environment name in both fixed `env` and ambient `env_passthrough`; do not choose precedence.
7. Break the diff state, NUL path transport, coverage disjointness, normalized-key collision, root containment, and environment collision protections one at a time. Run the real gate after each controlled break and record the exact failure count under A-067.

## Test constraints copied from AUTHORING.md §3b

**A. Nothing may make the verdict depend on how fast the machine is.** (L20)
- ✗ `deadline = time.monotonic() + N` followed by an assertion. A time budget is
  a proxy for "eventually" and is hardware-dependent by construction.
- ✗ `time.sleep(N)` to "let the thread get there", then assert.
- ✗ Asserting on elapsed time, or on how many iterations something completed.
- ✓ Wait on a **real synchronization point**: `join()` a process/thread, block on
  an `Event` the code under test sets, drain a queue.
- ✓ **Best: remove the wait.** Extract the pure per-iteration step and call it
  directly from the main thread. Deterministic *and* trivially coverable.
- ✓ A timeout is legal ONLY as a failsafe against hanging the suite forever
  (make it generous — 60s, not 3s). It must never be the thing that decides
  pass/fail. If shrinking the timeout could flip the result, it is an oracle.
- **Rule: a test that fails when the machine is slow is a TRUE red — a real race
  the slow host revealed. Fix the test. Never widen a timeout, and never raise a
  cgroup weight / add CPU to make a suite pass.**

**B. Nothing may depend on test order, worker assignment, or a sibling test.**
- ✗ Mutating **process-global** state (logging config, `os.environ`, module
  attributes, singletons) without restoring it. Under `pytest-xdist` the damage
  lands in whichever test shares that worker. (PL7 §5)
- ✗ `monkeypatch.setattr` on an object that synthesizes attributes via
  `__getattr__` (lazy proxies, `SimpleNamespace` façades, ORM rows). Teardown
  *materializes* the patched attribute as a permanent instance attribute and
  pins it forever. Patch the **namespace that owns it** instead. (L19)
- ✗ Teardown that destroys shared state rather than restoring the prior value.
- ✓ Fresh `tmp_path` per test; assert cleanup actually restored what it found.
- When a test fails only in the full parallel suite, ask **"what did an earlier
  test leave behind?"** before "what raced?" — pollution is more common than a
  race and reproduces deterministically once you know the pair.

**C. No hollow tests.** (§3 above, and DOCTRINE's review checklist)
- ✗ A test body that is `pass`, or asserts only that nothing raised.
- ✗ Asserting implementation trivia (a call count, a private attribute, a log
  string) instead of the behavioral contract.
- ✗ Weakening or deleting an assertion to get past a failure.
- ✓ Assert the **contract**: given this input/state, this observable outcome.
- ✓ Where a check guards a real crash, add a test proving the crash is real —
  it ties the check to reality instead of to a style rule.

**D. No coverage evasion.** (L11, GA2b)
- ✗ A no-cover exclusion pragma on changed lines. nyxloom's gate **rejects**
  them, and note it matches the literal token anywhere on a line — including in
  a comment that merely *describes* the rule.
- ✗ Excluding an `except` body and assuming the `except` clause is covered too —
  it is not; that off-by-one killed a diff-coverage floor once already. (L11)
- ✓ If a line is genuinely unreachable, restructure so it does not exist.

**E. Network, clock, and filesystem are inputs — control them.**
- ✗ Real network calls, real registries, real model endpoints in a unit test.
- ✗ `datetime.now()` / `time.time()` where the assertion depends on the value.
- ✓ Inject or mock the boundary; make offline the default path.

**Author's check:** for every test you specify, ask *"could this flip its verdict
on a slower machine, in a different worker, or in a different order?"* If yes,
it is not an oracle yet.

## Package-specific test emphasis

**A. No speed-dependent verdicts.** Git process completion and exact parsed identities decide; no polling or elapsed-time oracle.

**B. No order/worker dependence.** Every repository and coverage profile is fresh per test; the collision pair is exercised in both insertion orders.

**C. No hollow tests.** Compare complete independently-written path/line maps and exact error outcomes; never assert merely that a helper was called.

**D. No coverage evasion.** The project gate remains 100% statement and branch, and every controlled production break has a recorded real failure count.

**E. Control inputs.** Use real local Git repositories and in-memory coverage text; no network, ambient repository, or process-global environment mutation.

## Scope / forbid

This package repairs pre-judgment input integrity only. It must not change the CLI, verdict/schema contract, verifier, mutation, canary, or attestation semantics. A need to represent a new artifact field belongs to P16 and is a BLOCKED result here.

## BLOCKED rule

If a named contract cannot be met as specified, or scope requires a forbidden file, STOP — write `BLOCKED: <reason>` to the log, commit, and exit. Do not improvise a workaround.
