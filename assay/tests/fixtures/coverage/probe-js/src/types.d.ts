// A TypeScript declaration file: zero executable code by the language's own
// grammar. Neither coverage provider reports it at all.
export type UserRole = 'analyst' | 'power-user' | 'admin'

export interface Pageable {
  page: number
  size: number
}
