# CIU Workspace Environment (Auto-generated)
# Do not edit manually - regenerated by ciu --generate-env

# Environment type flags
# Runtime environment: devcontainer | native | github-actions
export ENV_TYPE="devcontainer"
# 1 when running inside a devcontainer
export IS_DEVCONTAINER="1"
# 1 when running in GitHub Actions
export IS_GITHUB_ACTIONS="0"
# 1 when running on a native host
export IS_NATIVE="0"

# User identity
# Current user name
export USER_NAME="vscode"
# Current user ID
export USER_UID="1003"
# Current user primary group ID
export USER_GID="1003"

# Container identity
# UID used for container user mapping
export CONTAINER_UID="1003"
# GID used for container user mapping
export CONTAINER_GID="994"
# UID for Docker socket access (defaults to USER_UID)
export DOCKER_UID="1003"
# Docker group GID for host volume permissions
export DOCKER_GID="994"
# Detected devcontainer name (if applicable)
export DEVCONTAINER_NAME="dstdns-devcontainer-vb"

# Repository paths
# Repo root path inside the current environment
export REPO_ROOT="/workspaces/vbpub"
# Host-visible repo root for Docker bind mounts
export PHYSICAL_REPO_ROOT="/home/vb/volkb79-2/vbpub"
# Host path of the persisted /tmp bind mount, so sibling containers (e.g. the test-runner) can bind the same /tmp; empty on native host
export HOST_MDT_TMP="/home/vb/mdt--mounted-folders/tmp"

# Docker network identity
# Lowercase repository name for network naming
export REPO_NAME="vbpub"
# Deterministic instance ID from repo path
export INSTANCE_ID="fae1b8"
# Internal Docker network name
export DOCKER_NETWORK_INTERNAL="vbpub-fae1b8-network"

# Public endpoint detection
# Detected public IP address (may be empty)
export PUBLIC_IP="152.53.179.117"
# Detected or configured public FQDN
export PUBLIC_FQDN="gstammtisch.dchive.de"
# TLS certificate PEM path (S2.3: required only when require_certs=true)
export PUBLIC_TLS_CRT_PEM=""
# TLS private key PEM path (S2.3: required only when require_certs=true)
export PUBLIC_TLS_KEY_PEM=""

# Service profiles (Seam 4 / S7.5)
# export CIU_SERVICES_PROFILE="core,db,worker-io"  # ordered profile list for this host

# Resource governance (S15)
# Derived read-IOPS governance cap (fallback default (no io-baseline file found; searched: /var/lib/ciu/io-baseline.env, /var/lib/gstammtisch/io-baseline.env))
export CIU_GOV_READ_IOPS="200"

# Python tooling
# Python interpreter for CIU subprocesses
export PYTHON_EXECUTABLE="/workspaces/dstdns/.venv/bin/python"
# pip executable resolved on PATH
export PIP_EXECUTABLE="/workspaces/dstdns/.venv/bin/pip"
