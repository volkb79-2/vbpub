# tester-unified closure — the SUM of every vbpub Python project's runtime +
# test dependencies, GROUPED BY THE STACK that requires each line so the reason
# for every dependency is legible. Each group is derived directly from that
# project's pyproject.toml [project.dependencies] + [project.optional-dependencies]
# (or its requirements.txt). All projects declare requires-python >=3.11.
#
# This RETIRES the per-worktree ephemeral venv (e.g. groop's old
# `pip install -e groop[dev]` gate step): the deps are baked here once.
# When a project adds/bumps a dependency, update ITS group below.

# ─── shared test tooling (every stack gates with these) ───────────────────────
pytest>=8.0            # groop declares >=8.0; root scripts declare >=7.4 (covered)
hypothesis>=6          # handoffctl2 property tests (tests/test_properties.py)
coverage>=7            # coverage reporting across stacks

# ─── ciu  (deploy tool) ───────────────────────────────────────────────────────
#     pyproject [project.dependencies] + [optional-dependencies.ssh]
Jinja2>=3.1.2
PyYAML>=6.0.1          # highest PyYAML floor; also covers handoffctl2 (>=6) & root (>=6.0)
tomli_w>=1.0.0
paramiko>=5.0          # ciu [ssh] extra (remote deploy)

# ─── cmru ─────────────────────────────────────────────────────────────────────
#     pyproject [project.dependencies] = []  (stdlib only — nothing to add)

# ─── groop  (host memory/observability TUI) ───────────────────────────────────
#     pyproject [dependencies] + [optional-dependencies.{zstandard,mcp}]
#     (groop[dev] = groop[zstandard] + groop[mcp] + pytest, all folded in)
textual>=8.2.8
zstandard>=0.22
mcp>=1.28.0

# ─── handoffctl2  (control plane) ─────────────────────────────────────────────
#     pyproject [project.dependencies]  (PyYAML already pinned under ciu above)
jsonschema>=4

# ─── root scripts  (vbpub/requirements.txt) ───────────────────────────────────
requests>=2.32.3
telethon>=1.38.1
